diff --git a/accel/tcg/cpu-exec.c b/accel/tcg/cpu-exec.c
index d93cd17..0753fcf 100644
--- a/accel/tcg/cpu-exec.c
+++ b/accel/tcg/cpu-exec.c
@@ -47,6 +47,12 @@
 #include "tb-context.h"
 #include "tb-internal.h"
 #include "internal-common.h"
+#include "../../ui/xui/debug-tools/addons/memory-tools/guest-input-profiler-hook.h"
+#include "../../ui/xui/debug-tools/addons/diagnostics/guest-input-consumer-trace.h"
+#include "../../ui/xui/debug-tools/addons/diagnostics/guest-timer-deadline-trace.h"
+#include "../../ui/xui/debug-tools/addons/diagnostics/ptimer-irq-delivery-trace.h"
+#include "../../ui/xui/debug-tools/addons/diagnostics/host-stall-trace.h"
+#include "../../ui/xui/debug-tools/addons/diagnostics/pgraph-flip-delay-trace-hook.h"
 
 /* -icount align implementation. */
 
@@ -801,6 +807,15 @@ static inline bool icount_exit_request(CPUState *cpu)
 static inline bool cpu_handle_interrupt(CPUState *cpu,
                                         TranslationBlock **last_tb)
 {
+    /* Patch 305.2 diagnostic-only PTIMER delivery sampling. The recorder is
+     * inert unless a PTIMER IRQ-service window is active and throttles its
+     * own TB-boundary samples. */
+    xemu_debug_tools_ptimer_irq_delivery_note_tcg_check(cpu);
+
+    /* Patch 305.4 diagnostic-only host stall sampling. Windows-specific
+     * counters are read only at the throttled sampling cadence / on a stall. */
+    xemu_debug_tools_host_stall_note_vcpu(cpu);
+
     /*
      * If we have requested custom cflags with CF_NOIRQ we should
      * skip checking here. Any pending interrupts will get picked up
@@ -985,6 +1000,29 @@ cpu_exec_loop(CPUState *cpu, SyncClocks *sc)
                 break;
             }
 
+            /* Info: Disable direct TB chaining only during active input diagnostics. */
+            const bool guest_input_profile_active =
+                unlikely(xemu_debug_tools_guest_input_profiler_active());
+            const bool guest_input_consumer_capture_active =
+                unlikely(xemu_debug_tools_guest_input_consumer_trace_capture_active());
+            const bool guest_timer_deadline_single_step =
+                unlikely(xemu_debug_tools_guest_timer_deadline_trace_should_single_step(
+                    (uint32_t)s.pc));
+            const bool pgraph_flip_delay_guest_pc_sample_active =
+                unlikely(xemu_debug_tools_pgraph_flip_delay_guest_pc_sampler_active());
+            if (guest_input_profile_active || guest_input_consumer_capture_active) {
+                s.cflags |= CF_NO_GOTO_TB | CF_NO_GOTO_PTR;
+                last_tb = NULL;
+            }
+            if (guest_timer_deadline_single_step) {
+                /* Diagnostic-only exact-PC capture: split only the two watched
+                 * guest pages into one-instruction TBs while the recorder is
+                 * enabled. The rest of TCG keeps its normal TB sizing. */
+                s.cflags = (s.cflags & ~CF_COUNT_MASK) |
+                           CF_NO_GOTO_TB | CF_NO_GOTO_PTR | 1;
+                last_tb = NULL;
+            }
+
             tb = tb_lookup(cpu, s);
             if (tb == NULL) {
                 CPUJumpCache *jc;
@@ -1020,6 +1058,20 @@ cpu_exec_loop(CPUState *cpu, SyncClocks *sc)
                 tb_add_jump(last_tb, tb_exit, tb);
             }
 
+            if (guest_input_profile_active) {
+                xemu_debug_tools_guest_input_profiler_record((uint32_t)s.pc);
+            }
+            if (guest_input_consumer_capture_active) {
+                xemu_debug_tools_guest_input_consumer_trace_note_title_tb(
+                    (uint32_t)s.pc);
+            }
+            if (guest_timer_deadline_single_step) {
+                xemu_debug_tools_guest_timer_deadline_trace_note_pc(
+                    cpu, (uint32_t)s.pc);
+            }
+            if (pgraph_flip_delay_guest_pc_sample_active) {
+                xemu_debug_tools_pgraph_flip_delay_note_guest_pc((uint32_t)s.pc);
+            }
             cpu_loop_exec_tb(cpu, tb, s.pc, &last_tb, &tb_exit);
 
             /* Try to align the host and virtual clocks
diff --git a/accel/tcg/cputlb.c b/accel/tcg/cputlb.c
index b9ae474..30420ce 100644
--- a/accel/tcg/cputlb.c
+++ b/accel/tcg/cputlb.c
@@ -52,6 +52,7 @@
 #endif
 #include "tcg/tcg-ldst.h"
 #include "backend-ldst.h"
+#include "../../ui/xui/debug-tools/addons/diagnostics/mmio-trace-hook.h"
 
 
 /* DEBUG defines, enable DEBUG_TLB_LOG to log to the CPU_LOG_MMU target */
@@ -1998,9 +1999,18 @@ static uint64_t do_ld_mmio_beN(CPUState *cpu, CPUTLBEntryFull *full,
     section = io_prepare(&mr_offset, cpu, full->xlat_section, attrs, addr, ra);
     mr = section->mr;
 
+    XemuDebugToolsMmioTraceScope mmio_trace_scope;
+    uint64_t result;
+    hwaddr physaddr = full->phys_addr | (addr & ~TARGET_PAGE_MASK);
+
     BQL_LOCK_GUARD();
-    return int_ld_mmio_beN(cpu, full, ret_be, addr, size, mmu_idx,
-                           type, ra, mr, mr_offset);
+    xemu_debug_tools_mmio_trace_begin(
+        &mmio_trace_scope, mr, mr_offset, addr, physaddr, size,
+        XEMU_DEBUG_TOOLS_MMIO_ACCESS_READ);
+    result = int_ld_mmio_beN(cpu, full, ret_be, addr, size, mmu_idx,
+                            type, ra, mr, mr_offset);
+    xemu_debug_tools_mmio_trace_end(&mmio_trace_scope);
+    return result;
 }
 
 static Int128 do_ld16_mmio_beN(CPUState *cpu, CPUTLBEntryFull *full,
@@ -2019,11 +2029,18 @@ static Int128 do_ld16_mmio_beN(CPUState *cpu, CPUTLBEntryFull *full,
     section = io_prepare(&mr_offset, cpu, full->xlat_section, attrs, addr, ra);
     mr = section->mr;
 
+    XemuDebugToolsMmioTraceScope mmio_trace_scope;
+    hwaddr physaddr = full->phys_addr | (addr & ~TARGET_PAGE_MASK);
+
     BQL_LOCK_GUARD();
+    xemu_debug_tools_mmio_trace_begin(
+        &mmio_trace_scope, mr, mr_offset, addr, physaddr, size,
+        XEMU_DEBUG_TOOLS_MMIO_ACCESS_READ);
     a = int_ld_mmio_beN(cpu, full, ret_be, addr, size - 8, mmu_idx,
                         MMU_DATA_LOAD, ra, mr, mr_offset);
     b = int_ld_mmio_beN(cpu, full, ret_be, addr + size - 8, 8, mmu_idx,
                         MMU_DATA_LOAD, ra, mr, mr_offset + size - 8);
+    xemu_debug_tools_mmio_trace_end(&mmio_trace_scope);
     return int128_make128(b, a);
 }
 
@@ -2539,9 +2556,18 @@ static uint64_t do_st_mmio_leN(CPUState *cpu, CPUTLBEntryFull *full,
     section = io_prepare(&mr_offset, cpu, full->xlat_section, attrs, addr, ra);
     mr = section->mr;
 
+    XemuDebugToolsMmioTraceScope mmio_trace_scope;
+    uint64_t result;
+    hwaddr physaddr = full->phys_addr | (addr & ~TARGET_PAGE_MASK);
+
     BQL_LOCK_GUARD();
-    return int_st_mmio_leN(cpu, full, val_le, addr, size, mmu_idx,
-                           ra, mr, mr_offset);
+    xemu_debug_tools_mmio_trace_begin(
+        &mmio_trace_scope, mr, mr_offset, addr, physaddr, size,
+        XEMU_DEBUG_TOOLS_MMIO_ACCESS_WRITE);
+    result = int_st_mmio_leN(cpu, full, val_le, addr, size, mmu_idx,
+                            ra, mr, mr_offset);
+    xemu_debug_tools_mmio_trace_end(&mmio_trace_scope);
+    return result;
 }
 
 static uint64_t do_st16_mmio_leN(CPUState *cpu, CPUTLBEntryFull *full,
@@ -2559,11 +2585,20 @@ static uint64_t do_st16_mmio_leN(CPUState *cpu, CPUTLBEntryFull *full,
     section = io_prepare(&mr_offset, cpu, full->xlat_section, attrs, addr, ra);
     mr = section->mr;
 
+    XemuDebugToolsMmioTraceScope mmio_trace_scope;
+    uint64_t result;
+    hwaddr physaddr = full->phys_addr | (addr & ~TARGET_PAGE_MASK);
+
     BQL_LOCK_GUARD();
+    xemu_debug_tools_mmio_trace_begin(
+        &mmio_trace_scope, mr, mr_offset, addr, physaddr, size,
+        XEMU_DEBUG_TOOLS_MMIO_ACCESS_WRITE);
     int_st_mmio_leN(cpu, full, int128_getlo(val_le), addr, 8,
                     mmu_idx, ra, mr, mr_offset);
-    return int_st_mmio_leN(cpu, full, int128_gethi(val_le), addr + 8,
-                           size - 8, mmu_idx, ra, mr, mr_offset + 8);
+    result = int_st_mmio_leN(cpu, full, int128_gethi(val_le), addr + 8,
+                             size - 8, mmu_idx, ra, mr, mr_offset + 8);
+    xemu_debug_tools_mmio_trace_end(&mmio_trace_scope);
+    return result;
 }
 
 /*
diff --git a/build.sh b/build.sh
index 312f36c..8e3dd63 100755
--- a/build.sh
+++ b/build.sh
@@ -5,6 +5,7 @@ set -o pipefail # Will return the exit status of make if it fails
 set -o physical # Resolve symlinks when changing directory
 
 project_source_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"
+[[ "${XEMU_DEBUG_TOOLS_BUILD_WRAPPED:-0}" == "1" || ! -f "${project_source_dir}/ui/xui/debug-tools/tools/build-xemu.sh" ]] || exec bash "${project_source_dir}/ui/xui/debug-tools/tools/build-xemu.sh" "$@"
 
 target_arch=$(uname -m)
 
diff --git a/config_spec.yml b/config_spec.yml
index c933b54..74e5b8b 100644
--- a/config_spec.yml
+++ b/config_spec.yml
@@ -7,6 +7,23 @@ general:
       type: bool
       default: true
   screenshot_dir: string
+  video_recording:
+    output_dir: string
+    format:
+      type: integer
+      default: 0
+    fps:
+      type: integer
+      default: 0
+    audio_gain:
+      type: number
+      default: 1.0
+    show_active_codes:
+      type: bool
+      default: false
+    panel_position:
+      type: integer
+      default: 0
   games_dir: string
   skip_boot_anim: bool
   # throttle_io: bool
@@ -304,6 +321,25 @@ display:
       advanced_tree_state:
         type: bool
         default: false
+    memory_tools:
+      disassembly_pane_height:
+        type: number
+        default: 320.0
+      disassembly_full_page:
+        type: bool
+        default: true
+      disassembly_instruction_count:
+        type: integer
+        default: 32
+      follow_eip:
+        type: bool
+        default: true
+      labels_enabled:
+        type: bool
+        default: true
+      register_view:
+        type: integer
+        default: 0
   setup_nvidia_profile:
     type: bool
     default: true
@@ -364,6 +400,11 @@ sys:
     flashrom_path: string
     eeprom_path: string
     hdd_path: string
+    hdd_type:
+      type: enum
+      values: [image, folder]
+      default: image
+    hdd_folder_path: string
     dvd_path: string
 
 perf:
diff --git a/debian/control b/debian/control
index 3f58074..e9821fa 100644
--- a/debian/control
+++ b/debian/control
@@ -23,6 +23,11 @@ Build-Depends: debhelper (>= 11),
  libusb-1.0-0-dev,
  libcurl4-gnutls-dev,
  libpipewire-0.3-dev,
+ libavcodec-dev,
+ libavformat-dev,
+ libavutil-dev,
+ libswresample-dev,
+ libswscale-dev,
 Standards-Version: 3.9.8
 Homepage: https://xemu.app
 XS-Debian-Vcs-Browser: https://github.com/mborgerson/xemu
diff --git a/hw/i386/x86-cpu.c b/hw/i386/x86-cpu.c
index a26055f..b39e06f 100644
--- a/hw/i386/x86-cpu.c
+++ b/hw/i386/x86-cpu.c
@@ -30,6 +30,7 @@
 #include "hw/intc/i8259.h"
 #include "hw/irq.h"
 #include "system/kvm.h"
+#include "ui/xui/debug-tools/addons/diagnostics/ptimer-irq-delivery-trace.h"
 
 /* TSC handling */
 uint64_t cpu_get_tsc(CPUX86State *env)
@@ -46,11 +47,16 @@ uint64_t cpu_get_tsc(CPUX86State *env)
 static void pic_irq_request(void *opaque, int irq, int level)
 {
     CPUState *cs = first_cpu;
+    CPUState *trace_cs = cs;
     X86CPU *cpu = X86_CPU(cs);
+    uint32_t interrupt_request_before =
+        qatomic_read(&trace_cs->interrupt_request);
+    bool apic_path = cpu_is_apic_enabled(cpu->apic_state) &&
+                     !kvm_irqchip_in_kernel() &&
+                     !whpx_apic_in_platform();
 
     trace_x86_pic_interrupt(irq, level);
-    if (cpu_is_apic_enabled(cpu->apic_state) && !kvm_irqchip_in_kernel() &&
-        !whpx_apic_in_platform()) {
+    if (apic_path) {
         CPU_FOREACH(cs) {
             cpu = X86_CPU(cs);
             if (apic_accept_pic_intr(cpu->apic_state)) {
@@ -64,6 +70,9 @@ static void pic_irq_request(void *opaque, int irq, int level)
             cpu_reset_interrupt(cs, CPU_INTERRUPT_HARD);
         }
     }
+    xemu_debug_tools_ptimer_irq_delivery_note_pic_cpu_line(
+        trace_cs, level, interrupt_request_before,
+        qatomic_read(&trace_cs->interrupt_request), apic_path ? 1 : 0);
 }
 
 qemu_irq x86_allocate_cpu_irq(void)
diff --git a/hw/ide/atapi.c b/hw/ide/atapi.c
--- a/hw/ide/atapi.c
+++ b/hw/ide/atapi.c
@@ -30,6 +30,8 @@
 #include "scsi/constants.h"
 #include "ide-internal.h"
 #include "trace.h"
+#include "../../ui/xui/debug-tools/addons/file-replace/file-replace-disc-overlay.h"
+#include "../../ui/xui/debug-tools/addons/file-replace/file-replace-block.h"
 
 #define ATAPI_SECTOR_BITS (2 + BDRV_SECTOR_BITS)
 #define ATAPI_SECTOR_SIZE (1 << ATAPI_SECTOR_BITS)
@@ -99,11 +101,11 @@
 
     switch (s->cd_sector_size) {
     case 2048:
-        ret = blk_pread(s->blk, (int64_t)s->lba << ATAPI_SECTOR_BITS,
+        ret = xemu_file_replace_pread(s->blk, (int64_t)s->lba << ATAPI_SECTOR_BITS,
                         ATAPI_SECTOR_SIZE, s->io_buffer, 0);
         break;
     case 2352:
-        ret = blk_pread(s->blk, (int64_t)s->lba << ATAPI_SECTOR_BITS,
+        ret = xemu_file_replace_pread(s->blk, (int64_t)s->lba << ATAPI_SECTOR_BITS,
                         ATAPI_SECTOR_SIZE, s->io_buffer + 16, 0);
         if (ret >= 0) {
             cd_data_to_raw(s->io_buffer, s->lba);
@@ -138,6 +140,7 @@
     }
 
     block_acct_done(blk_get_stats(s->blk), &s->acct);
+
 
     if (s->cd_sector_size == 2352) {
         cd_data_to_raw(s->io_buffer, s->lba);
@@ -324,7 +327,7 @@
 static void ide_atapi_cmd_read_pio(IDEState *s, int lba, int nb_sectors,
                                    int sector_size)
 {
-    assert(0 <= lba && lba < (s->nb_sectors >> 2));
+    assert(0 <= lba && lba < (xemu_file_replace_disc_sectors((uintptr_t)blk_bs(s->blk), s->nb_sectors >> 2)));
 
     s->lba = lba;
     s->packet_transfer_size = nb_sectors * sector_size;
@@ -424,7 +427,7 @@
 static void ide_atapi_cmd_read_dma(IDEState *s, int lba, int nb_sectors,
                                    int sector_size)
 {
-    assert(0 <= lba && lba < (s->nb_sectors >> 2));
+    assert(0 <= lba && lba < (xemu_file_replace_disc_sectors((uintptr_t)blk_bs(s->blk), s->nb_sectors >> 2)));
 
     s->lba = lba;
     s->packet_transfer_size = nb_sectors * sector_size;
@@ -491,7 +494,7 @@
                 if (layer != 0)
                     return -ASC_INV_FIELD_IN_CMD_PACKET;
 
-                total_sectors = s->nb_sectors >> 2;
+                total_sectors = xemu_file_replace_disc_sectors((uintptr_t)blk_bs(s->blk), s->nb_sectors >> 2);
                 if (total_sectors == 0) {
                     return -ASC_MEDIUM_NOT_PRESENT;
                 }
@@ -982,7 +985,7 @@
     unsigned int nb_sectors, lba;
 
     /* Total logical sectors of ATAPI_SECTOR_SIZE(=2048) bytes */
-    uint64_t total_sectors = s->nb_sectors >> 2;
+    uint64_t total_sectors = xemu_file_replace_disc_sectors((uintptr_t)blk_bs(s->blk), s->nb_sectors >> 2);
 
     if (buf[0] == GPCMD_READ_10) {
         nb_sectors = lduw_be_p(buf + 7);
@@ -995,11 +998,15 @@
     }
 
     lba = ldl_be_p(buf + 2);
-    if (lba >= total_sectors || lba + nb_sectors - 1 >= total_sectors) {
+    if (lba >= total_sectors || (uint64_t)lba + nb_sectors - 1 >= total_sectors) {
         ide_atapi_cmd_error(s, ILLEGAL_REQUEST, ASC_LOGICAL_BLOCK_OOR);
         return;
     }
 
+    if (lba > INT32_MAX || nb_sectors > INT32_MAX / 2048) {
+        ide_atapi_cmd_error(s, ILLEGAL_REQUEST, ASC_INV_FIELD_IN_CMD_PACKET);
+        return;
+    }
     ide_atapi_cmd_read(s, lba, nb_sectors, 2048);
 }
 
@@ -1008,7 +1015,7 @@
     unsigned int nb_sectors, lba, transfer_request;
 
     /* Total logical sectors of ATAPI_SECTOR_SIZE(=2048) bytes */
-    uint64_t total_sectors = s->nb_sectors >> 2;
+    uint64_t total_sectors = xemu_file_replace_disc_sectors((uintptr_t)blk_bs(s->blk), s->nb_sectors >> 2);
 
     nb_sectors = (buf[6] << 16) | (buf[7] << 8) | buf[8];
     if (nb_sectors == 0) {
@@ -1017,8 +1024,13 @@
     }
 
     lba = ldl_be_p(buf + 2);
-    if (lba >= total_sectors || lba + nb_sectors - 1 >= total_sectors) {
+    if (lba >= total_sectors || (uint64_t)lba + nb_sectors - 1 >= total_sectors) {
         ide_atapi_cmd_error(s, ILLEGAL_REQUEST, ASC_LOGICAL_BLOCK_OOR);
+        return;
+    }
+
+    if (lba > INT32_MAX || nb_sectors > INT32_MAX / 2352) {
+        ide_atapi_cmd_error(s, ILLEGAL_REQUEST, ASC_INV_FIELD_IN_CMD_PACKET);
         return;
     }
 
@@ -1053,7 +1065,7 @@
 static void cmd_seek(IDEState *s, uint8_t* buf)
 {
     unsigned int lba;
-    uint64_t total_sectors = s->nb_sectors >> 2;
+    uint64_t total_sectors = xemu_file_replace_disc_sectors((uintptr_t)blk_bs(s->blk), s->nb_sectors >> 2);
 
     lba = ldl_be_p(buf + 2);
     if (lba >= total_sectors) {
@@ -1112,7 +1124,7 @@
 {
     int format, msf, start_track, len;
     int max_len;
-    uint64_t total_sectors = s->nb_sectors >> 2;
+    uint64_t total_sectors = xemu_file_replace_disc_sectors((uintptr_t)blk_bs(s->blk), s->nb_sectors >> 2);
 
     max_len = lduw_be_p(buf + 7);
     format = buf[9] >> 6;
@@ -1149,7 +1161,7 @@
 
 static void cmd_read_cdvd_capacity(IDEState *s, uint8_t* buf)
 {
-    uint64_t total_sectors = s->nb_sectors >> 2;
+    uint64_t total_sectors = xemu_file_replace_disc_sectors((uintptr_t)blk_bs(s->blk), s->nb_sectors >> 2);
 
     /* NOTE: it is really the number of sectors minus 1 */
     stl_be_p(buf, total_sectors - 1);
diff --git a/hw/usb/hcd-ohci.c b/hw/usb/hcd-ohci.c
index 7b3f120..90a72a6 100644
--- a/hw/usb/hcd-ohci.c
+++ b/hw/usb/hcd-ohci.c
@@ -36,6 +36,10 @@
 #include "hw/qdev-dma.h"
 #include "hw/qdev-properties.h"
 #include "trace.h"
+#include "../../ui/xui/debug-tools/addons/diagnostics/controller-xid-trace-hook.h"
+#include "../../ui/xui/debug-tools/addons/diagnostics/guest-input-consumer-trace.h"
+#include "../../ui/xui/debug-tools/addons/diagnostics/ohci-slow-frame-profile-hook.h"
+#include "../../ui/xui/debug-tools/addons/diagnostics/scheduler-trace-hook.h"
 #include "hcd-ohci.h"
 
 /* This causes frames to occur 1000x slower */
@@ -273,7 +277,21 @@ static inline void ohci_intr_update(OHCIState *ohci)
 /* Set an interrupt */
 static inline void ohci_set_interrupt(OHCIState *ohci, uint32_t intr)
 {
+    uint32_t old_status = ohci->intr_status;
+
     ohci->intr_status |= intr;
+    if (intr & OHCI_INTR_RHSC) {
+        xemu_debug_tools_controller_xid_trace_usb_event(
+            XEMU_DEBUG_TOOLS_CONTROLLER_XID_OHCI_RHSC, -1, NULL,
+            old_status, ohci->intr_status, intr, ohci->intr);
+    }
+    if (intr & OHCI_INTR_WD) {
+        xemu_debug_tools_controller_xid_trace_usb_event5(
+            XEMU_DEBUG_TOOLS_CONTROLLER_XID_OHCI_WDH_IRQ, -1, NULL,
+            old_status, ohci->intr_status, ohci->intr, ohci->hcca, 1);
+    }
+    xemu_debug_tools_scheduler_trace_note_irq(
+        XEMU_DEBUG_TOOLS_SCHED_WAKE_OHCI, ohci->intr_status, ohci->intr);
     ohci_intr_update(ohci);
 }
 
@@ -574,8 +592,8 @@ static int ohci_service_iso_td(OHCIState *ohci, struct ohci_ed *ed)
     int pid;
     int ret;
     int i;
-    USBDevice *dev;
-    USBEndpoint *ep;
+    USBDevice *dev = NULL;
+    USBEndpoint *ep = NULL;
     USBPacket *pkt;
     QEMU_UNINITIALIZED uint8_t buf[8192];
     bool int_req;
@@ -871,7 +889,10 @@ static void ohci_td_pkt(const char *msg, const uint8_t *buf, size_t len)
  * Service a transport descriptor.
  * Returns nonzero to terminate processing of this endpoint.
  */
-static int ohci_service_td(OHCIState *ohci, struct ohci_ed *ed)
+static int ohci_service_td(OHCIState *ohci, struct ohci_ed *ed,
+                           uint32_t ed_address,
+                           XemuDebugToolsOhciListKind list_kind,
+                           XemuDebugToolsOhciSlowFrameEvent *profile)
 {
     int dir;
     size_t len = 0, pktlen = 0;
@@ -879,12 +900,13 @@ static int ohci_service_td(OHCIState *ohci, struct ohci_ed *ed)
     int pid;
     int ret;
     int i;
-    USBDevice *dev;
-    USBEndpoint *ep;
+    USBDevice *dev = NULL;
+    USBEndpoint *ep = NULL;
     struct ohci_td td;
     uint32_t addr;
     int flag_r;
     int completion;
+    uint64_t profile_start_ns;
 
     addr = ed->head & OHCI_DPTR_MASK;
     if (addr == 0) {
@@ -898,11 +920,26 @@ static int ohci_service_td(OHCIState *ohci, struct ohci_ed *ed)
         trace_usb_ohci_td_skip_async();
         return 1;
     }
+    profile_start_ns = profile ? ohci_profile_now_ns() : 0;
     if (ohci_read_td(ohci, addr, &td)) {
+        if (profile) {
+            uint64_t duration_ns = ohci_profile_now_ns() - profile_start_ns;
+            profile->td_read_ns += duration_ns;
+            ohci_profile_add_leaf(profile, XEMU_DEBUG_TOOLS_OHCI_LEAF_TD_READ,
+                                  list_kind, duration_ns, ed_address, addr,
+                                  -1, -1, 0, 0, 0, 0);
+        }
         trace_usb_ohci_td_read_error(addr);
         ohci_die(ohci);
         return 1;
     }
+    if (profile) {
+        uint64_t duration_ns = ohci_profile_now_ns() - profile_start_ns;
+        profile->td_read_ns += duration_ns;
+        ohci_profile_add_leaf(profile, XEMU_DEBUG_TOOLS_OHCI_LEAF_TD_READ,
+                              list_kind, duration_ns, ed_address, addr,
+                              -1, -1, 0, 0, 0, 0);
+    }
 
     dir = OHCI_BM(ed->flags, ED_D);
     switch (dir) {
@@ -960,10 +997,21 @@ static int ohci_service_td(OHCIState *ohci, struct ohci_ed *ed)
                 pktlen = len;
             }
             if (!completion) {
+                profile_start_ns = profile ? ohci_profile_now_ns() : 0;
                 if (ohci_copy_td(ohci, &td, ohci->usb_buf, pktlen,
                                  DMA_DIRECTION_TO_DEVICE)) {
                     ohci_die(ohci);
                 }
+                if (profile) {
+                    uint64_t duration_ns = ohci_profile_now_ns() - profile_start_ns;
+                    profile->dma_to_device_ns += duration_ns;
+                    ++profile->dma_to_device_count;
+                    ohci_profile_add_leaf(
+                        profile, XEMU_DEBUG_TOOLS_OHCI_LEAF_DMA_TO_DEVICE,
+                        list_kind, duration_ns, ed_address, addr, dir, pid, 0,
+                        OHCI_BM(ed->flags, ED_FA), OHCI_BM(ed->flags, ED_EN),
+                        (uint32_t)pktlen);
+                }
             }
         }
     }
@@ -974,15 +1022,48 @@ static int ohci_service_td(OHCIState *ohci, struct ohci_ed *ed)
     ohci_td_pkt("OUT", ohci->usb_buf, pktlen);
 
     if (completion) {
+        if (ohci->usb_packet.ep != NULL) {
+            dev = ohci->usb_packet.ep->dev;
+        }
         ohci->async_td = 0;
         ohci->async_complete = false;
     } else {
+        if (dir == OHCI_TD_DIR_SETUP) {
+            xemu_debug_tools_controller_xid_trace_usb_event(
+                XEMU_DEBUG_TOOLS_CONTROLLER_XID_OHCI_SETUP_TD, -1, NULL,
+                OHCI_BM(ed->flags, ED_FA), OHCI_BM(ed->flags, ED_EN),
+                (uint32_t)pktlen, addr);
+        }
+        profile_start_ns = profile ? ohci_profile_now_ns() : 0;
         dev = ohci_find_device(ohci, OHCI_BM(ed->flags, ED_FA));
+        if (profile) {
+            uint64_t duration_ns = ohci_profile_now_ns() - profile_start_ns;
+            profile->device_lookup_ns += duration_ns;
+            ohci_profile_add_leaf(
+                profile, XEMU_DEBUG_TOOLS_OHCI_LEAF_DEVICE_LOOKUP,
+                list_kind, duration_ns, ed_address, addr, dir, pid, 0,
+                OHCI_BM(ed->flags, ED_FA), OHCI_BM(ed->flags, ED_EN),
+                (uint32_t)pktlen);
+        }
         if (dev == NULL) {
+            xemu_debug_tools_controller_xid_trace_usb_event(
+                XEMU_DEBUG_TOOLS_CONTROLLER_XID_OHCI_TD_DEVICE_MISSING, -1,
+                NULL, OHCI_BM(ed->flags, ED_FA), OHCI_BM(ed->flags, ED_EN),
+                (uint32_t)dir, addr);
             trace_usb_ohci_td_dev_error();
             return 1;
         }
+        profile_start_ns = profile ? ohci_profile_now_ns() : 0;
         ep = usb_ep_get(dev, pid, OHCI_BM(ed->flags, ED_EN));
+        if (profile) {
+            uint64_t duration_ns = ohci_profile_now_ns() - profile_start_ns;
+            profile->device_lookup_ns += duration_ns;
+            ohci_profile_add_leaf(
+                profile, XEMU_DEBUG_TOOLS_OHCI_LEAF_DEVICE_LOOKUP,
+                list_kind, duration_ns, ed_address, addr, dir, pid, 0,
+                OHCI_BM(ed->flags, ED_FA), OHCI_BM(ed->flags, ED_EN),
+                (uint32_t)pktlen);
+        }
         if (ohci->async_td) {
             /*
              * ??? The hardware should allow one active packet per
@@ -996,11 +1077,32 @@ static int ohci_service_td(OHCIState *ohci, struct ohci_ed *ed)
         usb_packet_setup(&ohci->usb_packet, pid, ep, 0, addr, !flag_r,
                          OHCI_BM(td.flags, TD_DI) == 0);
         usb_packet_addbuf(&ohci->usb_packet, ohci->usb_buf, pktlen);
+        profile_start_ns = profile ? ohci_profile_now_ns() : 0;
         usb_handle_packet(dev, &ohci->usb_packet);
+        if (profile) {
+            uint64_t duration_ns = ohci_profile_now_ns() - profile_start_ns;
+            profile->usb_handle_packet_ns += duration_ns;
+            ++profile->usb_packet_count;
+            ohci_profile_add_leaf(
+                profile, XEMU_DEBUG_TOOLS_OHCI_LEAF_USB_HANDLE_PACKET,
+                list_kind, duration_ns, ed_address, addr, dir, pid,
+                ohci->usb_packet.status, OHCI_BM(ed->flags, ED_FA),
+                OHCI_BM(ed->flags, ED_EN), (uint32_t)pktlen);
+        }
         trace_usb_ohci_td_packet_status(ohci->usb_packet.status);
 
         if (ohci->usb_packet.status == USB_RET_ASYNC) {
+            profile_start_ns = profile ? ohci_profile_now_ns() : 0;
             usb_device_flush_ep_queue(dev, ep);
+            if (profile) {
+                uint64_t duration_ns = ohci_profile_now_ns() - profile_start_ns;
+                profile->async_flush_ns += duration_ns;
+                ohci_profile_add_leaf(
+                    profile, XEMU_DEBUG_TOOLS_OHCI_LEAF_ASYNC_FLUSH,
+                    list_kind, duration_ns, ed_address, addr, dir, pid,
+                    ohci->usb_packet.status, OHCI_BM(ed->flags, ED_FA),
+                    OHCI_BM(ed->flags, ED_EN), (uint32_t)pktlen);
+            }
             ohci->async_td = addr;
             return 1;
         }
@@ -1013,9 +1115,35 @@ static int ohci_service_td(OHCIState *ohci, struct ohci_ed *ed)
 
     if (ret >= 0) {
         if (dir == OHCI_TD_DIR_IN) {
+            profile_start_ns = profile ? ohci_profile_now_ns() : 0;
             if (ohci_copy_td(ohci, &td, ohci->usb_buf, ret,
                              DMA_DIRECTION_FROM_DEVICE)) {
                 ohci_die(ohci);
+            } else if (OHCI_BM(ed->flags, ED_EN) == 2 && ret > 0) {
+                const uint32_t guest_length0 = MIN(
+                    (uint32_t)ret, 0x1000u - (td.cbp & 0xfffu));
+                const uint32_t guest_length1 = (uint32_t)ret - guest_length0;
+                const uint32_t guest_address0 =
+                    (uint32_t)(td.cbp + ohci->localmem_base);
+                const uint32_t guest_address1 = guest_length1 != 0
+                    ? (uint32_t)((td.be & ~0xfffu) + ohci->localmem_base)
+                    : 0;
+                xemu_debug_tools_controller_xid_trace_guest_input_write(
+                    dev, addr, guest_address0, guest_length0, guest_address1,
+                    guest_length1, ohci->usb_buf, (uint32_t)ret);
+                xemu_debug_tools_guest_input_consumer_trace_note_report(
+                    guest_address0, guest_length0, guest_address1,
+                    guest_length1, ohci->usb_buf, (uint32_t)ret);
+            }
+            if (profile) {
+                uint64_t duration_ns = ohci_profile_now_ns() - profile_start_ns;
+                profile->dma_from_device_ns += duration_ns;
+                ++profile->dma_from_device_count;
+                ohci_profile_add_leaf(
+                    profile, XEMU_DEBUG_TOOLS_OHCI_LEAF_DMA_FROM_DEVICE,
+                    list_kind, duration_ns, ed_address, addr, dir, pid,
+                    ohci->usb_packet.status, OHCI_BM(ed->flags, ED_FA),
+                    OHCI_BM(ed->flags, ED_EN), (uint32_t)ret);
             }
             ohci_td_pkt("IN", ohci->usb_buf, pktlen);
         } else {
@@ -1089,11 +1217,27 @@ static int ohci_service_td(OHCIState *ohci, struct ohci_ed *ed)
         ed->head |= OHCI_ED_H;
     }
 
+    if (dir == OHCI_TD_DIR_IN && OHCI_BM(ed->flags, ED_EN) == 2) {
+        xemu_debug_tools_controller_xid_trace_usb_event5(
+            XEMU_DEBUG_TOOLS_CONTROLLER_XID_OHCI_TD_COMPLETE, -1, dev,
+            OHCI_BM(ed->flags, ED_FA), OHCI_BM(ed->flags, ED_EN), addr,
+            (uint32_t)ret, OHCI_BM(td.flags, TD_CC));
+    }
+
     /* Retire this TD */
     ed->head &= ~OHCI_DPTR_MASK;
     ed->head |= td.next & OHCI_DPTR_MASK;
-    td.next = ohci->done;
-    ohci->done = addr;
+    {
+        uint32_t previous_done = ohci->done;
+        td.next = previous_done;
+        ohci->done = addr;
+        if (dir == OHCI_TD_DIR_IN && OHCI_BM(ed->flags, ED_EN) == 2) {
+            xemu_debug_tools_controller_xid_trace_usb_event5(
+                XEMU_DEBUG_TOOLS_CONTROLLER_XID_OHCI_DONE_HEAD, -1, dev,
+                previous_done, addr, ohci->done, ohci->done_count,
+                OHCI_BM(td.flags, TD_CC));
+        }
+    }
     i = OHCI_BM(td.flags, TD_DI);
     if (i < ohci->done_count) {
         ohci->done_count = i;
@@ -1102,32 +1246,72 @@ static int ohci_service_td(OHCIState *ohci, struct ohci_ed *ed)
         ohci->done_count = 0;
     }
 exit_no_retire:
+    profile_start_ns = profile ? ohci_profile_now_ns() : 0;
     if (ohci_put_td(ohci, addr, &td)) {
+        if (profile) {
+            uint64_t duration_ns = ohci_profile_now_ns() - profile_start_ns;
+            profile->td_write_ns += duration_ns;
+            ohci_profile_add_leaf(profile, XEMU_DEBUG_TOOLS_OHCI_LEAF_TD_WRITE,
+                                  list_kind, duration_ns, ed_address, addr,
+                                  dir, pid, ret,
+                                  OHCI_BM(ed->flags, ED_FA),
+                                  OHCI_BM(ed->flags, ED_EN),
+                                  (uint32_t)pktlen);
+        }
         ohci_die(ohci);
         return 1;
     }
+    if (profile) {
+        uint64_t duration_ns = ohci_profile_now_ns() - profile_start_ns;
+        profile->td_write_ns += duration_ns;
+        ohci_profile_add_leaf(profile, XEMU_DEBUG_TOOLS_OHCI_LEAF_TD_WRITE,
+                              list_kind, duration_ns, ed_address, addr,
+                              dir, pid, ret, OHCI_BM(ed->flags, ED_FA),
+                              OHCI_BM(ed->flags, ED_EN), (uint32_t)pktlen);
+    }
     return OHCI_BM(td.flags, TD_CC) != OHCI_CC_NOERROR;
 }
 
 /* Service an endpoint list.  Returns nonzero if active TD were found. */
-static int ohci_service_ed_list(OHCIState *ohci, uint32_t head)
+static int ohci_service_ed_list(OHCIState *ohci, uint32_t head,
+                                XemuDebugToolsOhciListKind list_kind,
+                                XemuDebugToolsOhciSlowFrameEvent *profile)
 {
     struct ohci_ed ed;
     uint32_t next_ed;
     uint32_t cur;
     int active;
     uint32_t link_cnt = 0;
+    uint64_t profile_start_ns;
     active = 0;
 
     if (head == 0) {
         return 0;
     }
     for (cur = head; cur && link_cnt++ < ED_LINK_LIMIT; cur = next_ed) {
+        if (profile) {
+            ++profile->ed_count;
+        }
+        profile_start_ns = profile ? ohci_profile_now_ns() : 0;
         if (ohci_read_ed(ohci, cur, &ed)) {
+            if (profile) {
+                uint64_t duration_ns = ohci_profile_now_ns() - profile_start_ns;
+                profile->ed_read_ns += duration_ns;
+                ohci_profile_add_leaf(profile, XEMU_DEBUG_TOOLS_OHCI_LEAF_ED_READ,
+                                      list_kind, duration_ns, cur, 0,
+                                      -1, -1, 0, 0, 0, 0);
+            }
             trace_usb_ohci_ed_read_error(cur);
             ohci_die(ohci);
             return 0;
         }
+        if (profile) {
+            uint64_t duration_ns = ohci_profile_now_ns() - profile_start_ns;
+            profile->ed_read_ns += duration_ns;
+            ohci_profile_add_leaf(profile, XEMU_DEBUG_TOOLS_OHCI_LEAF_ED_READ,
+                                  list_kind, duration_ns, cur, 0,
+                                  -1, -1, 0, 0, 0, 0);
+        }
 
         next_ed = ed.next & OHCI_DPTR_MASK;
 
@@ -1136,10 +1320,20 @@ static int ohci_service_ed_list(OHCIState *ohci, uint32_t head)
             /* Cancel pending packets for ED that have been paused. */
             addr = ed.head & OHCI_DPTR_MASK;
             if (ohci->async_td && addr == ohci->async_td) {
+                profile_start_ns = profile ? ohci_profile_now_ns() : 0;
                 usb_cancel_packet(&ohci->usb_packet);
                 ohci->async_td = 0;
                 usb_device_ep_stopped(ohci->usb_packet.ep->dev,
                                       ohci->usb_packet.ep);
+                if (profile) {
+                    uint64_t duration_ns = ohci_profile_now_ns() - profile_start_ns;
+                    profile->endpoint_stop_ns += duration_ns;
+                    ohci_profile_add_leaf(
+                        profile, XEMU_DEBUG_TOOLS_OHCI_LEAF_ENDPOINT_STOP,
+                        list_kind, duration_ns, cur, addr,
+                        -1, -1, 0, OHCI_BM(ed.flags, ED_FA),
+                        OHCI_BM(ed.flags, ED_EN), 0);
+                }
             }
             continue;
         }
@@ -1157,21 +1351,61 @@ static int ohci_service_ed_list(OHCIState *ohci, uint32_t head)
             active = 1;
 
             if ((ed.flags & OHCI_ED_F) == 0) {
-                if (ohci_service_td(ohci, &ed)) {
+                if (profile) {
+                    ++profile->td_count;
+                }
+                if (ohci_service_td(ohci, &ed, cur, list_kind, profile)) {
                     break;
                 }
             } else {
                 /* Handle isochronous endpoints */
+                profile_start_ns = profile ? ohci_profile_now_ns() : 0;
+                if (profile) {
+                    ++profile->iso_td_count;
+                }
                 if (ohci_service_iso_td(ohci, &ed)) {
+                    if (profile) {
+                        uint64_t duration_ns = ohci_profile_now_ns() - profile_start_ns;
+                        profile->iso_td_ns += duration_ns;
+                        ohci_profile_add_leaf(
+                            profile, XEMU_DEBUG_TOOLS_OHCI_LEAF_ISO_TD,
+                            list_kind, duration_ns, cur,
+                            ed.head & OHCI_DPTR_MASK, -1, -1, 0,
+                            OHCI_BM(ed.flags, ED_FA), OHCI_BM(ed.flags, ED_EN), 0);
+                    }
                     break;
                 }
+                if (profile) {
+                    uint64_t duration_ns = ohci_profile_now_ns() - profile_start_ns;
+                    profile->iso_td_ns += duration_ns;
+                    ohci_profile_add_leaf(
+                        profile, XEMU_DEBUG_TOOLS_OHCI_LEAF_ISO_TD,
+                        list_kind, duration_ns, cur,
+                        ed.head & OHCI_DPTR_MASK, -1, -1, 0,
+                        OHCI_BM(ed.flags, ED_FA), OHCI_BM(ed.flags, ED_EN), 0);
+                }
             }
         }
 
+        profile_start_ns = profile ? ohci_profile_now_ns() : 0;
         if (ohci_put_ed(ohci, cur, &ed)) {
+            if (profile) {
+                uint64_t duration_ns = ohci_profile_now_ns() - profile_start_ns;
+                profile->ed_write_ns += duration_ns;
+                ohci_profile_add_leaf(profile, XEMU_DEBUG_TOOLS_OHCI_LEAF_ED_WRITE,
+                                      list_kind, duration_ns, cur, 0,
+                                      -1, -1, 0, 0, 0, 0);
+            }
             ohci_die(ohci);
             return 0;
         }
+        if (profile) {
+            uint64_t duration_ns = ohci_profile_now_ns() - profile_start_ns;
+            profile->ed_write_ns += duration_ns;
+            ohci_profile_add_leaf(profile, XEMU_DEBUG_TOOLS_OHCI_LEAF_ED_WRITE,
+                                  list_kind, duration_ns, cur, 0,
+                                  -1, -1, 0, 0, 0, 0);
+        }
     }
 
     return active;
@@ -1191,23 +1425,38 @@ static void ohci_sof(OHCIState *ohci)
 }
 
 /* Process Control and Bulk lists. */
-static void ohci_process_lists(OHCIState *ohci)
+static void ohci_process_lists(OHCIState *ohci,
+                               XemuDebugToolsOhciSlowFrameEvent *profile)
 {
+    uint64_t profile_start_ns;
+
     if ((ohci->ctl & OHCI_CTL_CLE) && (ohci->status & OHCI_STATUS_CLF)) {
         if (ohci->ctrl_cur && ohci->ctrl_cur != ohci->ctrl_head) {
             trace_usb_ohci_process_lists(ohci->ctrl_head, ohci->ctrl_cur);
         }
-        if (!ohci_service_ed_list(ohci, ohci->ctrl_head)) {
+        profile_start_ns = profile ? ohci_profile_now_ns() : 0;
+        if (!ohci_service_ed_list(ohci, ohci->ctrl_head,
+                                  XEMU_DEBUG_TOOLS_OHCI_LIST_CONTROL,
+                                  profile)) {
             ohci->ctrl_cur = 0;
             ohci->status &= ~OHCI_STATUS_CLF;
         }
+        if (profile) {
+            profile->control_list_ns += ohci_profile_now_ns() - profile_start_ns;
+        }
     }
 
     if ((ohci->ctl & OHCI_CTL_BLE) && (ohci->status & OHCI_STATUS_BLF)) {
-        if (!ohci_service_ed_list(ohci, ohci->bulk_head)) {
+        profile_start_ns = profile ? ohci_profile_now_ns() : 0;
+        if (!ohci_service_ed_list(ohci, ohci->bulk_head,
+                                  XEMU_DEBUG_TOOLS_OHCI_LIST_BULK,
+                                  profile)) {
             ohci->bulk_cur = 0;
             ohci->status &= ~OHCI_STATUS_BLF;
         }
+        if (profile) {
+            profile->bulk_list_ns += ohci_profile_now_ns() - profile_start_ns;
+        }
     }
 }
 
@@ -1216,33 +1465,72 @@ static void ohci_frame_boundary(void *opaque)
 {
     OHCIState *ohci = opaque;
     struct ohci_hcca hcca;
-
+    XemuDebugToolsOhciSlowFrameEvent profile_storage = { 0 };
+    XemuDebugToolsOhciSlowFrameEvent *profile = NULL;
+    uint64_t profile_start_ns;
+    const uint64_t threshold_ns =
+        xemu_debug_tools_ohci_slow_frame_trace_threshold_ns();
+
+    if (threshold_ns != 0) {
+        profile = &profile_storage;
+        profile->threshold_ns = threshold_ns;
+        profile->frame_number = ohci->frame_number;
+        profile->ctl = ohci->ctl;
+        profile->status = ohci->status;
+        profile->hcca = ohci->hcca;
+        profile->virtual_start_ns =
+            (uint64_t)qemu_clock_get_ns(QEMU_CLOCK_VIRTUAL);
+        profile->host_start_ns = ohci_profile_now_ns();
+    }
+
+    profile_start_ns = profile ? ohci_profile_now_ns() : 0;
     if (ohci_read_hcca(ohci, ohci->hcca, &hcca)) {
+        if (profile) {
+            profile->hcca_read_ns += ohci_profile_now_ns() - profile_start_ns;
+        }
         trace_usb_ohci_hcca_read_error(ohci->hcca);
         ohci_die(ohci);
+        ohci_profile_finish(profile);
         return;
     }
+    if (profile) {
+        profile->hcca_read_ns += ohci_profile_now_ns() - profile_start_ns;
+    }
 
     /* Process all the lists at the end of the frame */
     if (ohci->ctl & OHCI_CTL_PLE) {
         int n;
 
         n = ohci->frame_number & 0x1f;
-        ohci_service_ed_list(ohci, le32_to_cpu(hcca.intr[n]));
+        profile_start_ns = profile ? ohci_profile_now_ns() : 0;
+        ohci_service_ed_list(ohci, le32_to_cpu(hcca.intr[n]),
+                             XEMU_DEBUG_TOOLS_OHCI_LIST_PERIODIC, profile);
+        if (profile) {
+            profile->periodic_list_ns +=
+                ohci_profile_now_ns() - profile_start_ns;
+        }
     }
 
     /* Cancel all pending packets if either of the lists has been disabled. */
     if (ohci->old_ctl & (~ohci->ctl) & (OHCI_CTL_BLE | OHCI_CTL_CLE)) {
+        profile_start_ns = profile ? ohci_profile_now_ns() : 0;
         ohci_stop_endpoints(ohci);
+        if (profile) {
+            profile->stop_endpoints_ns +=
+                ohci_profile_now_ns() - profile_start_ns;
+        }
     }
     ohci->old_ctl = ohci->ctl;
-    ohci_process_lists(ohci);
+    ohci_process_lists(ohci, profile);
 
     /* Stop if UnrecoverableError happened or ohci_sof will crash */
     if (ohci->intr_status & OHCI_INTR_UE) {
+        ohci_profile_finish(profile);
         return;
     }
 
+    profile_start_ns = profile ? ohci_profile_now_ns() : 0;
+
     /* Frame boundary, so do EOF stuf here */
     ohci->frt = ohci->fit;
 
@@ -1270,11 +1558,24 @@ static void ohci_frame_boundary(void *opaque)
     }
     /* Do SOF stuff here */
     ohci_sof(ohci);
+    if (profile) {
+        profile->done_sof_ns += ohci_profile_now_ns() - profile_start_ns;
+    }
 
     /* Writeback HCCA */
+    profile_start_ns = profile ? ohci_profile_now_ns() : 0;
     if (ohci_put_hcca(ohci, ohci->hcca, &hcca)) {
+        if (profile) {
+            profile->hcca_write_ns += ohci_profile_now_ns() - profile_start_ns;
+        }
         ohci_die(ohci);
+        ohci_profile_finish(profile);
+        return;
+    }
+    if (profile) {
+        profile->hcca_write_ns += ohci_profile_now_ns() - profile_start_ns;
     }
+    ohci_profile_finish(profile);
 }
 
 /*
@@ -1490,11 +1791,15 @@ static void ohci_port_set_status(OHCIState *ohci, int portnum, uint32_t val)
     }
 
     if (ohci_port_set_if_connected(ohci, portnum, val & OHCI_PORT_PRS)) {
+        uint32_t reset_state = port->ctrl;
         trace_usb_ohci_port_reset(portnum);
         usb_device_reset(port->port.dev);
         port->ctrl &= ~OHCI_PORT_PRS;
         /* ??? Should this also set OHCI_PORT_PESC. */
         port->ctrl |= OHCI_PORT_PES | OHCI_PORT_PRSC;
+        xemu_debug_tools_controller_xid_trace_usb_event(
+            XEMU_DEBUG_TOOLS_CONTROLLER_XID_OHCI_PORT_RESET, portnum,
+            port->port.dev, reset_state, port->ctrl, val, 0);
     }
 
     /* Invert order here to ensure in ambiguous case, device is powered up. */
@@ -1504,6 +1809,9 @@ static void ohci_port_set_status(OHCIState *ohci, int portnum, uint32_t val)
     if (val & OHCI_PORT_PPS) {
         ohci_port_power(ohci, portnum, 1);
     }
+    xemu_debug_tools_controller_xid_trace_usb_event(
+        XEMU_DEBUG_TOOLS_CONTROLLER_XID_OHCI_PORT_STATUS_WRITE, portnum,
+        port->port.dev, val, old_state, port->ctrl, 0);
     if (old_state != port->ctrl) {
         ohci_set_interrupt(ohci, OHCI_INTR_RHSC);
     }
@@ -1678,7 +1986,15 @@ static void ohci_mem_write(void *opaque,
         break;
 
     case 3: /* HcInterruptStatus */
-        ohci->intr_status &= ~val;
+        {
+            uint32_t old_status = ohci->intr_status;
+            ohci->intr_status &= ~val;
+            if (val & OHCI_INTR_WD) {
+                xemu_debug_tools_controller_xid_trace_usb_event5(
+                    XEMU_DEBUG_TOOLS_CONTROLLER_XID_OHCI_WDH_IRQ, -1, NULL,
+                    old_status, ohci->intr_status, ohci->intr, (uint32_t)val, 0);
+            }
+        }
         ohci_intr_update(ohci);
         break;
 
@@ -1799,6 +2115,9 @@ static void ohci_attach(USBPort *port1)
         ohci_set_interrupt(s, OHCI_INTR_RD);
     }
 
+    xemu_debug_tools_controller_xid_trace_usb_event(
+        XEMU_DEBUG_TOOLS_CONTROLLER_XID_OHCI_PORT_ATTACH, port1->index,
+        port1->dev, old_state, port->ctrl, port1->dev->speed, 0);
     trace_usb_ohci_port_attach(port1->index);
 
     if (old_state != port->ctrl) {
@@ -1836,6 +2155,9 @@ static void ohci_detach(USBPort *port1)
         port->ctrl &= ~OHCI_PORT_PES;
         port->ctrl |= OHCI_PORT_PESC;
     }
+    xemu_debug_tools_controller_xid_trace_usb_event(
+        XEMU_DEBUG_TOOLS_CONTROLLER_XID_OHCI_PORT_DETACH, port1->index,
+        port1->dev, old_state, port->ctrl, 0, 0);
     trace_usb_ohci_port_detach(port1->index);
 
     if (old_state != port->ctrl) {
@@ -1871,7 +2193,7 @@ static void ohci_async_complete_packet(USBPort *port, USBPacket *packet)
 
     trace_usb_ohci_async_complete();
     ohci->async_complete = true;
-    ohci_process_lists(ohci);
+    ohci_process_lists(ohci, NULL);
 }
 
 static USBPortOps ohci_port_ops = {
diff --git a/hw/xbox/mcpx/apu/monitor.c b/hw/xbox/mcpx/apu/monitor.c
index 730fcc9..a00682b 100644
--- a/hw/xbox/mcpx/apu/monitor.c
+++ b/hw/xbox/mcpx/apu/monitor.c
@@ -18,6 +18,8 @@
  */
 
 #include "apu_int.h"
+#include "ui/xui/debug-tools/addons/diagnostics/av-sync-trace-hook.h"
+#include "ui/xui/debug-tools/addons/Video-Recorder/video-recorder-c.h"
 
 void mcpx_apu_monitor_init(MCPXAPUState *d, Error **errp)
 {
@@ -73,8 +75,15 @@ void mcpx_apu_monitor_frame(MCPXAPUState *d)
         return;
     }
 
+    xemu_debug_tools_av_sync_trace_note_audio_batch(
+        d, ARRAY_SIZE(d->monitor.frame_buf));
+
+    float vu = pow(fmax(0.0, fmin(g_config.audio.volume_limit, 1.0)), M_E);
+    float record_gain = fmax(0.0, fmin(g_config.general.video_recording.audio_gain, 2.0));
+    xemu_video_recorder_submit_audio(&d->monitor.frame_buf[0][0],
+                                     ARRAY_SIZE(d->monitor.frame_buf), record_gain);
+
     if (d->monitor.stream) {
-        float vu = pow(fmax(0.0, fmin(g_config.audio.volume_limit, 1.0)), M_E);
         SDL_SetAudioStreamGain(d->monitor.stream, vu);
         SDL_PutAudioStreamData(d->monitor.stream, d->monitor.frame_buf,
                             sizeof(d->monitor.frame_buf));
diff --git a/hw/xbox/nv2a/nv2a.c b/hw/xbox/nv2a/nv2a.c
index 1565c05..3595d8b 100644
--- a/hw/xbox/nv2a/nv2a.c
+++ b/hw/xbox/nv2a/nv2a.c
@@ -20,10 +20,14 @@
  */
 
 #include "hw/xbox/nv2a/nv2a_int.h"
+#include "ui/xui/debug-tools/addons/diagnostics/flip-stall-trace-hook.h"
+#include "ui/xui/debug-tools/addons/diagnostics/scheduler-trace-hook.h"
+#include "ui/xui/debug-tools/addons/diagnostics/ptimer-irq-delivery-trace.h"
 #include "qemu/main-loop.h"
 
 void nv2a_update_irq(NV2AState *d)
 {
+    const uint32_t previous_pmc_pending = d->pmc.pending_interrupts;
     /* PFIFO */
     if (d->pfifo.pending_interrupts & d->pfifo.enabled_interrupts) {
         d->pmc.pending_interrupts |= NV_PMC_INTR_0_PFIFO;
@@ -44,7 +48,6 @@ void nv2a_update_irq(NV2AState *d)
     } else {
         d->pmc.pending_interrupts &= ~NV_PMC_INTR_0_PGRAPH;
     }
-
     /* PTIMER */
     if (d->ptimer.pending_interrupts & d->ptimer.enabled_interrupts) {
         d->pmc.pending_interrupts |= NV_PMC_INTR_0_PTIMER;
@@ -52,10 +55,32 @@ void nv2a_update_irq(NV2AState *d)
         d->pmc.pending_interrupts &= ~NV_PMC_INTR_0_PTIMER;
     }
 
+    {
+        const uint32_t newly_active =
+            d->pmc.pending_interrupts & ~previous_pmc_pending;
+        if (newly_active & NV_PMC_INTR_0_PGRAPH) {
+            xemu_debug_tools_scheduler_trace_note_irq(
+                XEMU_DEBUG_TOOLS_SCHED_WAKE_PGRAPH,
+                qatomic_read(&d->pgraph.pending_interrupts),
+                qatomic_read(&d->pgraph.enabled_interrupts));
+        }
+        if (newly_active & NV_PMC_INTR_0_PFIFO) {
+            xemu_debug_tools_scheduler_trace_note_irq(
+                XEMU_DEBUG_TOOLS_SCHED_WAKE_PFIFO,
+                d->pfifo.pending_interrupts, d->pfifo.enabled_interrupts);
+        }
+    }
+
     if (d->pmc.pending_interrupts && d->pmc.enabled_interrupts) {
+        xemu_debug_tools_ptimer_irq_delivery_note_nv2a_line(
+            1, d->ptimer.pending_interrupts, d->ptimer.enabled_interrupts,
+            d->pmc.pending_interrupts, d->pmc.enabled_interrupts);
         trace_nv2a_irq(d->pmc.pending_interrupts);
         pci_irq_assert(PCI_DEVICE(d));
     } else {
+        xemu_debug_tools_ptimer_irq_delivery_note_nv2a_line(
+            0, d->ptimer.pending_interrupts, d->ptimer.enabled_interrupts,
+            d->pmc.pending_interrupts, d->pmc.enabled_interrupts);
         pci_irq_deassert(PCI_DEVICE(d));
     }
 }
@@ -208,6 +233,12 @@ static void nv2a_vga_gfx_update(void *opaque)
     NV2AState *d = container_of(vga, NV2AState, vga);
     d->pcrtc.pending_interrupts |= NV_PCRTC_INTR_0_VBLANK;
     d->pcrtc.raster = 0;
+    xemu_debug_tools_flip_stall_trace_record(
+        XEMU_DEBUG_TOOLS_FLIP_VBLANK_IRQ, &d->pgraph, 0,
+        d->pcrtc.pending_interrupts, 0, 0, 0);
+    xemu_debug_tools_scheduler_trace_note_irq(
+        XEMU_DEBUG_TOOLS_SCHED_WAKE_PCRTC_VBLANK,
+        d->pcrtc.pending_interrupts, d->pcrtc.enabled_interrupts);
 
     nv2a_update_irq(d);
 }
@@ -276,13 +307,13 @@ static void nv2a_lock_fifo(NV2AState *d)
     bql_unlock();
     qemu_cond_wait(&d->pfifo.fifo_idle_cond, &d->pfifo.lock);
     bql_lock();
-    qemu_mutex_lock(&d->pgraph.lock);
+    PGRAPH_LOCK(&d->pgraph);
 }
 
 static void nv2a_unlock_fifo(NV2AState *d)
 {
     pfifo_kick(d);
-    qemu_mutex_unlock(&d->pgraph.lock);
+    PGRAPH_UNLOCK(&d->pgraph);
     qemu_mutex_unlock(&d->pfifo.lock);
 }
 
diff --git a/hw/xbox/nv2a/pfifo.c b/hw/xbox/nv2a/pfifo.c
index ff4625c..206cd78 100644
--- a/hw/xbox/nv2a/pfifo.c
+++ b/hw/xbox/nv2a/pfifo.c
@@ -20,6 +20,9 @@
  */
 
 #include "nv2a_int.h"
+#include "ui/xui/debug-tools/addons/diagnostics/flip-stall-trace-hook.h"
+#include "ui/xui/debug-tools/addons/diagnostics/pgraph-flip-delay-trace-hook.h"
+#include "ui/xui/debug-tools/addons/diagnostics/pgraph-pusher-progress-trace-hook.h"
 
 typedef struct RAMHTEntry {
     uint32_t handle;
@@ -127,13 +130,17 @@ static bool pfifo_stall_for_flip(NV2AState *d)
     bool should_stall = false;
 
     if (qatomic_read(&d->pgraph.waiting_for_flip)) {
-        qemu_mutex_lock(&d->pgraph.lock);
+        PGRAPH_LOCK(&d->pgraph);
         if (!is_flip_stall_complete(d)) {
             should_stall = true;
+            xemu_debug_tools_flip_stall_trace_record(
+                XEMU_DEBUG_TOOLS_FLIP_PFIFO_CHECK_STALL, &d->pgraph,
+                NV097_FLIP_STALL, 0, 1, 0, 0);
         } else {
             d->pgraph.waiting_for_flip = false;
+            xemu_debug_tools_flip_stall_trace_end(&d->pgraph);
         }
-        qemu_mutex_unlock(&d->pgraph.lock);
+        PGRAPH_UNLOCK(&d->pgraph);
     }
 
     return should_stall;
@@ -183,10 +190,12 @@ static ssize_t pfifo_run_puller(NV2AState *d, uint32_t method_entry,
         assert(subchannel < 8);
         SET_MASK(*engine_reg, 3 << (4*subchannel), entry.engine);
         SET_MASK(*pull1, NV_PFIFO_CACHE1_PULL1_ENGINE, entry.engine);
+        xemu_debug_tools_pgraph_flip_delay_pfifo_gate_begin(
+            &d->pgraph, 0, parameter);
 
         // TODO: this is fucked
         qemu_mutex_unlock(&d->pfifo.lock);
-        qemu_mutex_lock(&d->pgraph.lock);
+        PGRAPH_LOCK(&d->pgraph);
 
         // Switch contexts if necessary
         if (can_fifo_access(d)) {
@@ -198,7 +207,7 @@ static ssize_t pfifo_run_puller(NV2AState *d, uint32_t method_entry,
             }
         }
 
-        qemu_mutex_unlock(&d->pgraph.lock);
+        PGRAPH_UNLOCK(&d->pgraph);
         qemu_mutex_lock(&d->pfifo.lock);
 
     } else if (method >= 0x100) {
@@ -218,10 +227,12 @@ static ssize_t pfifo_run_puller(NV2AState *d, uint32_t method_entry,
         enum FIFOEngine engine = GET_MASK(*engine_reg, 3 << (4*subchannel));
         assert(engine == ENGINE_GRAPHICS);
         SET_MASK(*pull1, NV_PFIFO_CACHE1_PULL1_ENGINE, engine);
+        xemu_debug_tools_pgraph_flip_delay_pfifo_gate_begin(
+            &d->pgraph, method, parameter);
 
         // TODO: this is fucked
         qemu_mutex_unlock(&d->pfifo.lock);
-        qemu_mutex_lock(&d->pgraph.lock);
+        PGRAPH_LOCK(&d->pgraph);
 
         if (can_fifo_access(d)) {
             num_proc =
@@ -229,7 +240,7 @@ static ssize_t pfifo_run_puller(NV2AState *d, uint32_t method_entry,
                               num_words_available, max_lookahead_words, inc);
         }
 
-        qemu_mutex_unlock(&d->pgraph.lock);
+        PGRAPH_UNLOCK(&d->pgraph);
         qemu_mutex_lock(&d->pfifo.lock);
     } else {
         assert(!"Unrecognized pfifo puller method");
@@ -290,6 +301,10 @@ static void pfifo_run_pusher(NV2AState *d)
 
     hwaddr dma_len;
     uint8_t *dma = nv_dma_map(d, dma_instance, &dma_len);
+    XemuDebugToolsPgraphPusherPfifoScope pusher_progress_scope;
+    uint32_t pusher_progress_method_words = 0;
+    xemu_debug_tools_pgraph_pusher_progress_pfifo_begin(
+        &pusher_progress_scope, channel_id, *dma_get, *dma_put);
 
     while (!pfifo_pusher_should_stall(d)) {
         uint32_t dma_get_v = *dma_get;
@@ -335,13 +350,20 @@ static void pfifo_run_pusher(NV2AState *d)
 
             *status &= ~NV_PFIFO_CACHE1_STATUS_LOW_MARK;
 
+            XemuDebugToolsPgraphPusherPullerScope method_breakdown_scope;
+            xemu_debug_tools_pgraph_pusher_method_breakdown_puller_begin(
+                &method_breakdown_scope);
             ssize_t num_words_processed =
                 pfifo_run_puller(d, method_entry, word, word_ptr,
                                  MIN(method_count, num_words_available),
                                  num_words_available);
+            xemu_debug_tools_pgraph_pusher_method_breakdown_puller_end(
+                &method_breakdown_scope,
+                num_words_processed > 0 ? (uint32_t)num_words_processed : 0);
             if (num_words_processed < 0) {
                 break;
             }
+            pusher_progress_method_words += (uint32_t)num_words_processed;
 
             dma_get_v += (num_words_processed-1)*4;
 
@@ -437,6 +459,13 @@ static void pfifo_run_pusher(NV2AState *d)
     // NV2A_DPRINTF("DMA pusher done: max 0x%" HWADDR_PRIx ", 0x%" HWADDR_PRIx " - 0x%" HWADDR_PRIx "\n",
     //      dma_len, control->dma_get, control->dma_put);
 
+    xemu_debug_tools_pgraph_pusher_progress_pfifo_end(
+        &pusher_progress_scope, *dma_get, *dma_put,
+        pusher_progress_method_words,
+        qatomic_read(&d->pgraph.waiting_for_nop) ? 1 : 0,
+        qatomic_read(&d->pgraph.waiting_for_flip) ? 1 : 0,
+        can_fifo_access(d) ? 1 : 0);
+
     uint32_t error = GET_MASK(*dma_state, NV_PFIFO_CACHE1_DMA_STATE_ERROR);
     if (error) {
         NV2A_DPRINTF("pb error: %d\n", error);
diff --git a/hw/xbox/nv2a/pgraph/gl/draw.c b/hw/xbox/nv2a/pgraph/gl/draw.c
index c569341..bfd1332 100644
--- a/hw/xbox/nv2a/pgraph/gl/draw.c
+++ b/hw/xbox/nv2a/pgraph/gl/draw.c
@@ -23,6 +23,7 @@
 #include "hw/xbox/nv2a/nv2a_int.h"
 #include "debug.h"
 #include "renderer.h"
+#include "ui/xui/debug-tools/addons/diagnostics/pgraph-draw-trace-hook.h"
 
 void pgraph_gl_clear_surface(NV2AState *d, uint32_t parameter)
 {
@@ -335,6 +336,9 @@ void pgraph_gl_draw_end(NV2AState *d)
     PGRAPHState *pg = &d->pgraph;
     PGRAPHGLState *r = pg->gl_renderer_state;
 
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_GL_DRAW_END_TOTAL, pg, 0, 0);
+
     uint32_t control_0 = pgraph_reg_r(pg, NV_PGRAPH_CONTROL_0);
     bool mask_alpha = control_0 & NV_PGRAPH_CONTROL_0_ALPHA_WRITE_ENABLE;
     bool mask_red = control_0 & NV_PGRAPH_CONTROL_0_RED_WRITE_ENABLE;
@@ -355,15 +359,25 @@ void pgraph_gl_draw_end(NV2AState *d)
         // processing, it is legal to attempt a draw that is entirely
         // clipped regardless of 0x880. See xemu#635 for context.
         NV2A_GL_DGROUP_END();
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_GL_DRAW_END_TOTAL);
         return;
     }
 
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_GL_FLUSH_DRAW, pg, 0, 0);
     pgraph_gl_flush_draw(d);
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_GL_FLUSH_DRAW);
 
     /* End of visibility testing */
     if (pg->zpass_pixel_count_enable) {
         nv2a_profile_inc_counter(NV2A_PROF_QUERY);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_GL_END_QUERY, pg, 0, 0);
         glEndQuery(GL_SAMPLES_PASSED);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_GL_END_QUERY);
     }
 
     pg->draw_time++;
@@ -374,8 +388,15 @@ void pgraph_gl_draw_end(NV2AState *d)
         r->zeta_binding->draw_time = pg->draw_time;
     }
 
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_SURFACE_DIRTY, pg, color_write ? 1 : 0,
+        (depth_test || stencil_test) ? 1 : 0);
     pgraph_gl_set_surface_dirty(pg, color_write, depth_test || stencil_test);
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_SURFACE_DIRTY);
     NV2A_GL_DGROUP_END();
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_GL_DRAW_END_TOTAL);
 }
 
 void pgraph_gl_flush_draw(NV2AState *d)
@@ -395,14 +416,24 @@ void pgraph_gl_flush_draw(NV2AState *d)
         assert(pg->inline_buffer_length == 0);
         assert(pg->inline_array_length == 0);
 
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_BIND_VERTEX_ATTRIBUTES, pg,
+            pg->draw_arrays_min_start, pg->draw_arrays_max_count);
         pgraph_gl_bind_vertex_attributes(d, pg->draw_arrays_min_start,
                                       pg->draw_arrays_max_count - 1,
                                       false, 0,
                                       pg->draw_arrays_max_count - 1);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_BIND_VERTEX_ATTRIBUTES);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_GL_MULTI_DRAW_ARRAYS, pg,
+            pg->draw_arrays_length, pg->draw_arrays_max_count);
         glMultiDrawArrays(r->shader_binding->gl_primitive_mode,
                           pg->draw_arrays_start,
                           pg->draw_arrays_count,
                           pg->draw_arrays_length);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_GL_MULTI_DRAW_ARRAYS);
     } else if (pg->inline_elements_length) {
         NV2A_GL_DPRINTF(false, "Inline Elements");
         nv2a_profile_inc_counter(NV2A_PROF_INLINE_ELEMENTS);
@@ -411,15 +442,28 @@ void pgraph_gl_flush_draw(NV2AState *d)
 
         uint32_t min_element = (uint32_t)-1;
         uint32_t max_element = 0;
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_INLINE_ELEMENT_SCAN, pg,
+            pg->inline_elements_length, 0);
         for (int i=0; i < pg->inline_elements_length; i++) {
             max_element = MAX(pg->inline_elements[i], max_element);
             min_element = MIN(pg->inline_elements[i], min_element);
         }
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_INLINE_ELEMENT_SCAN);
 
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_BIND_VERTEX_ATTRIBUTES, pg,
+            min_element, max_element);
         pgraph_gl_bind_vertex_attributes(
                 d, min_element, max_element, false, 0,
                 pg->inline_elements[pg->inline_elements_length - 1]);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_BIND_VERTEX_ATTRIBUTES);
 
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_ELEMENT_CACHE_LOOKUP, pg,
+            pg->inline_elements_length, 0);
         VertexKey k;
         memset(&k, 0, sizeof(VertexKey));
         k.count = pg->inline_elements_length;
@@ -432,18 +476,30 @@ void pgraph_gl_flush_draw(NV2AState *d)
         LruNode *node = lru_lookup(&r->element_cache, h, &k);
         VertexLruNode *found = container_of(node, VertexLruNode, node);
         glBindBuffer(GL_ELEMENT_ARRAY_BUFFER, found->gl_buffer);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_ELEMENT_CACHE_LOOKUP);
         if (!found->initialized) {
             nv2a_profile_inc_counter(NV2A_PROF_GEOM_BUFFER_UPDATE_4);
+            xemu_debug_tools_pgraph_draw_trace_stage_begin(
+                XEMU_DEBUG_TOOLS_PGRAPH_DRAW_GL_BUFFER_DATA, pg,
+                pg->inline_elements_length * 4ULL, GL_ELEMENT_ARRAY_BUFFER);
             glBufferData(GL_ELEMENT_ARRAY_BUFFER,
                          pg->inline_elements_length * 4,
                          pg->inline_elements, GL_STATIC_DRAW);
+            xemu_debug_tools_pgraph_draw_trace_stage_end(
+                XEMU_DEBUG_TOOLS_PGRAPH_DRAW_GL_BUFFER_DATA);
             found->initialized = true;
         } else {
             nv2a_profile_inc_counter(NV2A_PROF_GEOM_BUFFER_UPDATE_4_NOTDIRTY);
         }
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_GL_DRAW_ELEMENTS, pg,
+            pg->inline_elements_length, GL_UNSIGNED_INT);
         glDrawElements(r->shader_binding->gl_primitive_mode,
                        pg->inline_elements_length, GL_UNSIGNED_INT,
                        (void *)0);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_GL_DRAW_ELEMENTS);
     } else if (pg->inline_buffer_length) {
         NV2A_GL_DPRINTF(false, "Inline Buffer");
         nv2a_profile_inc_counter(NV2A_PROF_INLINE_BUFFERS);
@@ -451,9 +507,16 @@ void pgraph_gl_flush_draw(NV2AState *d)
 
         if (pg->compressed_attrs) {
             pg->compressed_attrs = 0;
+            xemu_debug_tools_pgraph_draw_trace_stage_begin(
+                XEMU_DEBUG_TOOLS_PGRAPH_DRAW_BIND_SHADERS, pg, 0, 0);
             pgraph_gl_bind_shaders(pg);
+            xemu_debug_tools_pgraph_draw_trace_stage_end(
+                XEMU_DEBUG_TOOLS_PGRAPH_DRAW_BIND_SHADERS);
         }
 
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_INLINE_BUFFER_UPLOAD, pg,
+            pg->inline_buffer_length, NV2A_VERTEXSHADER_ATTRIBUTES);
         for (int i = 0; i < NV2A_VERTEXSHADER_ATTRIBUTES; i++) {
             VertexAttribute *attr = &pg->vertex_attributes[i];
             if (attr->inline_buffer_populated) {
@@ -473,16 +536,32 @@ void pgraph_gl_flush_draw(NV2AState *d)
                 glVertexAttrib4fv(i, attr->inline_value);
             }
         }
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_INLINE_BUFFER_UPLOAD);
 
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_GL_DRAW_ARRAYS, pg,
+            pg->inline_buffer_length, 0);
         glDrawArrays(r->shader_binding->gl_primitive_mode,
                      0, pg->inline_buffer_length);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_GL_DRAW_ARRAYS);
     } else if (pg->inline_array_length) {
         NV2A_GL_DPRINTF(false, "Inline Array");
         nv2a_profile_inc_counter(NV2A_PROF_INLINE_ARRAYS);
 
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_INLINE_ARRAY_BIND, pg,
+            pg->inline_array_length, 0);
         unsigned int index_count = pgraph_gl_bind_inline_array(d);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_INLINE_ARRAY_BIND);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_GL_DRAW_ARRAYS, pg, index_count, 0);
         glDrawArrays(r->shader_binding->gl_primitive_mode,
                      0, index_count);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_GL_DRAW_ARRAYS);
     } else {
         NV2A_GL_DPRINTF(true, "EMPTY NV097_SET_BEGIN_END");
         NV2A_UNCONFIRMED("EMPTY NV097_SET_BEGIN_END");
diff --git a/hw/xbox/nv2a/pgraph/gl/renderer.c b/hw/xbox/nv2a/pgraph/gl/renderer.c
index e9e2109..2a954f1 100644
--- a/hw/xbox/nv2a/pgraph/gl/renderer.c
+++ b/hw/xbox/nv2a/pgraph/gl/renderer.c
@@ -23,6 +23,7 @@
 #include "hw/xbox/nv2a/pgraph/pgraph.h"
 #include "debug.h"
 #include "renderer.h"
+#include "ui/xui/debug-tools/addons/diagnostics/pgraph-flip-delay-trace-hook.h"
 
 GloContext *g_nv2a_context_render;
 GloContext *g_nv2a_context_display;
@@ -121,13 +122,16 @@ static void pgraph_gl_process_pending(NV2AState *d)
     PGRAPHState *pg = &d->pgraph;
     PGRAPHGLState *r = pg->gl_renderer_state;
 
+    xemu_debug_tools_pgraph_flip_delay_renderer_gate_begin(
+        &d->pgraph, XEMU_PGRAPH_FLIP_DELAY_RENDERER_GL);
+
     if (qatomic_read(&r->downloads_pending) ||
         qatomic_read(&r->download_dirty_surfaces_pending) ||
         qatomic_read(&d->pgraph.sync_pending) ||
         qatomic_read(&d->pgraph.flush_pending) ||
         qatomic_read(&r->shader_cache_writeback_pending)) {
         qemu_mutex_unlock(&d->pfifo.lock);
-        qemu_mutex_lock(&d->pgraph.lock);
+        PGRAPH_LOCK(&d->pgraph);
         if (qatomic_read(&r->downloads_pending)) {
             pgraph_gl_process_pending_downloads(d);
         }
@@ -143,9 +147,10 @@ static void pgraph_gl_process_pending(NV2AState *d)
         if (qatomic_read(&r->shader_cache_writeback_pending)) {
             pgraph_gl_shader_write_cache_reload_list(&d->pgraph);
         }
-        qemu_mutex_unlock(&d->pgraph.lock);
+        PGRAPH_UNLOCK(&d->pgraph);
         qemu_mutex_lock(&d->pfifo.lock);
     }
+    xemu_debug_tools_pgraph_flip_delay_renderer_gate_cancel();
 }
 
 static void pgraph_gl_pre_savevm_trigger(NV2AState *d)
diff --git a/hw/xbox/nv2a/pgraph/gl/surface.c b/hw/xbox/nv2a/pgraph/gl/surface.c
index 714121e..018452e 100644
--- a/hw/xbox/nv2a/pgraph/gl/surface.c
+++ b/hw/xbox/nv2a/pgraph/gl/surface.c
@@ -43,19 +43,19 @@ void pgraph_gl_set_surface_scale_factor(NV2AState *d, unsigned int scale)
     qatomic_set(&d->pfifo.halt, true);
     qemu_mutex_unlock(&d->pfifo.lock);
 
-    qemu_mutex_lock(&d->pgraph.lock);
+    PGRAPH_LOCK(&d->pgraph);
     qemu_event_reset(&r->dirty_surfaces_download_complete);
     qatomic_set(&r->download_dirty_surfaces_pending, true);
-    qemu_mutex_unlock(&d->pgraph.lock);
+    PGRAPH_UNLOCK(&d->pgraph);
     qemu_mutex_lock(&d->pfifo.lock);
     pfifo_kick(d);
     qemu_mutex_unlock(&d->pfifo.lock);
     qemu_event_wait(&r->dirty_surfaces_download_complete);
 
-    qemu_mutex_lock(&d->pgraph.lock);
+    PGRAPH_LOCK(&d->pgraph);
     qemu_event_reset(&d->pgraph.flush_complete);
     qatomic_set(&d->pgraph.flush_pending, true);
-    qemu_mutex_unlock(&d->pgraph.lock);
+    PGRAPH_UNLOCK(&d->pgraph);
     qemu_mutex_lock(&d->pfifo.lock);
     pfifo_kick(d);
     qemu_mutex_unlock(&d->pfifo.lock);
@@ -425,7 +425,7 @@ static void surface_access_callback(void *opaque, MemoryRegion *mr, hwaddr addr,
                                     hwaddr len, bool write)
 {
     NV2AState *d = (NV2AState *)opaque;
-    qemu_mutex_lock(&d->pgraph.lock);
+    PGRAPH_LOCK(&d->pgraph);
 
     PGRAPHGLState *r = d->pgraph.gl_renderer_state;
     bool wait_for_downloads = false;
@@ -454,7 +454,7 @@ static void surface_access_callback(void *opaque, MemoryRegion *mr, hwaddr addr,
         }
     }
 
-    qemu_mutex_unlock(&d->pgraph.lock);
+    PGRAPH_UNLOCK(&d->pgraph);
 
     if (wait_for_downloads) {
         qemu_mutex_lock(&d->pfifo.lock);
diff --git a/hw/xbox/nv2a/pgraph/null/renderer.c b/hw/xbox/nv2a/pgraph/null/renderer.c
index 8b34efc..ee671e2 100644
--- a/hw/xbox/nv2a/pgraph/null/renderer.c
+++ b/hw/xbox/nv2a/pgraph/null/renderer.c
@@ -21,6 +21,7 @@
 #include "qemu/thread.h"
 #include "hw/hw.h"
 #include "hw/xbox/nv2a/nv2a_int.h"
+#include "ui/xui/debug-tools/addons/diagnostics/pgraph-flip-delay-trace-hook.h"
 
 static void pgraph_null_sync(NV2AState *d)
 {
@@ -36,21 +37,25 @@ static void pgraph_null_flush(NV2AState *d)
 
 static void pgraph_null_process_pending(NV2AState *d)
 {
+    xemu_debug_tools_pgraph_flip_delay_renderer_gate_begin(
+        &d->pgraph, XEMU_PGRAPH_FLIP_DELAY_RENDERER_NULL);
+
     if (
         qatomic_read(&d->pgraph.sync_pending) ||
         qatomic_read(&d->pgraph.flush_pending)
         ) {
         qemu_mutex_unlock(&d->pfifo.lock);
-        qemu_mutex_lock(&d->pgraph.lock);
+        PGRAPH_LOCK(&d->pgraph);
         if (qatomic_read(&d->pgraph.sync_pending)) {
             pgraph_null_sync(d);
         }
         if (qatomic_read(&d->pgraph.flush_pending)) {
             pgraph_null_flush(d);
         }
-        qemu_mutex_unlock(&d->pgraph.lock);
+        PGRAPH_UNLOCK(&d->pgraph);
         qemu_mutex_lock(&d->pfifo.lock);
     }
+    xemu_debug_tools_pgraph_flip_delay_renderer_gate_cancel();
 }
 
 static void pgraph_null_clear_report_value(NV2AState *d)
diff --git a/hw/xbox/nv2a/pgraph/pgraph.c b/hw/xbox/nv2a/pgraph/pgraph.c
index 9d8118f..34bdbea 100644
--- a/hw/xbox/nv2a/pgraph/pgraph.c
+++ b/hw/xbox/nv2a/pgraph/pgraph.c
@@ -1,3 +1,5 @@
+#include "ui/xui/debug-tools/addons/diagnostics/pgraph-ptimer-latency-trace-hook.h"
+#include "ui/xui/debug-tools/addons/diagnostics/flip-stall-trace-hook.h"
 /*
  * QEMU Geforce NV2A implementation
  *
@@ -20,6 +22,7 @@
  */
 
 #include <math.h>
+#include "qemu/timer.h"
 
 #include "hw/xbox/nv2a/nv2a_int.h"
 #include "ui/xemu-notifications.h"
@@ -27,6 +30,9 @@
 #include "util.h"
 #include "swizzle.h"
 #include "nv2a_vsh_emulator.h"
+#include "ui/xui/debug-tools/addons/diagnostics/pgraph-draw-trace-hook.h"
+#include "ui/xui/debug-tools/addons/diagnostics/pgraph-pusher-progress-trace-hook.h"
+#include "ui/xui/debug-tools/addons/diagnostics/av-sync-trace-hook.h"
 
 #define PG_GET_MASK(reg, mask) GET_MASK(pgraph_reg_r(pg, reg), mask)
 #define PG_SET_MASK(reg, mask, value)        \
@@ -44,7 +50,7 @@ uint64_t pgraph_read(void *opaque, hwaddr addr, unsigned int size)
     NV2AState *d = (NV2AState *)opaque;
     PGRAPHState *pg = &d->pgraph;
 
-    qemu_mutex_lock(&pg->lock);
+    PGRAPH_LOCK(pg);
 
     uint64_t r = 0;
     switch (addr) {
@@ -74,7 +80,7 @@ uint64_t pgraph_read(void *opaque, hwaddr addr, unsigned int size)
         break;
     }
 
-    qemu_mutex_unlock(&pg->lock);
+    PGRAPH_UNLOCK(pg);
 
     nv2a_reg_log_read(NV_PGRAPH, addr, size, r);
     return r;
@@ -85,10 +91,36 @@ void pgraph_write(void *opaque, hwaddr addr, uint64_t val, unsigned int size)
     NV2AState *d = (NV2AState *)opaque;
     PGRAPHState *pg = &d->pgraph;
 
+    /* Diagnostics fallback for the stock PGRAPH path. Patch 20 replaces this
+     * with its lock-decoupled increment path, but the same trace ABI remains
+     * available when Patch 20 is intentionally omitted for isolation tests. */
+    const uint64_t trace_threshold =
+        (addr == NV_PGRAPH_INCREMENT)
+            ? xemu_debug_tools_pgraph_increment_trace_threshold_ns() : 0;
+    XemuDebugToolsPgraphIncrementEvent trace = { 0 };
+    uint64_t trace_stage_start = 0;
+    if (trace_threshold != 0) {
+        trace.threshold_ns = trace_threshold;
+        trace.host_start_ns = qemu_clock_get_ns(QEMU_CLOCK_HOST);
+        trace.virtual_start_ns = qemu_clock_get_ns(QEMU_CLOCK_VIRTUAL);
+        trace.value = (uint32_t)val;
+        trace.had_bql = bql_locked() ? 1 : 0;
+    }
+
     nv2a_reg_log_write(NV_PGRAPH, addr, size, val);
 
     qemu_mutex_lock(&d->pfifo.lock); // FIXME: Factor out fifo lock here
-    qemu_mutex_lock(&pg->lock);
+    if (trace_threshold != 0) {
+        trace_stage_start = qemu_clock_get_ns(QEMU_CLOCK_HOST);
+    }
+    PGRAPH_LOCK(pg);
+    if (trace_threshold != 0) {
+        /* Stock has one renderer lock rather than Patch 20's surface_lock.
+         * Preserve the stage field by measuring the equivalent serialization
+         * wait here; Patch 20 restores the dedicated surface-lock timing. */
+        trace.surface_lock_wait_ns =
+            qemu_clock_get_ns(QEMU_CLOCK_HOST) - trace_stage_start;
+    }
 
     switch (addr) {
     case NV_PGRAPH_INTR:
@@ -107,14 +139,46 @@ void pgraph_write(void *opaque, hwaddr addr, uint64_t val, unsigned int size)
         break;
     case NV_PGRAPH_INCREMENT:
         if (val & NV_PGRAPH_INCREMENT_READ_3D) {
+            const uint32_t surface_before = pgraph_reg_r(pg, NV_PGRAPH_SURFACE);
+            const uint32_t read_before =
+                GET_MASK(surface_before, NV_PGRAPH_SURFACE_READ_3D);
+            const uint32_t modulo =
+                GET_MASK(surface_before, NV_PGRAPH_SURFACE_MODULO_3D);
+            if (trace_threshold != 0) {
+                trace.surface_before = surface_before;
+                trace.read_before = read_before;
+                trace.modulo = modulo;
+                trace_stage_start = qemu_clock_get_ns(QEMU_CLOCK_HOST);
+            }
+
             PG_SET_MASK(NV_PGRAPH_SURFACE,
                      NV_PGRAPH_SURFACE_READ_3D,
-                     (PG_GET_MASK(NV_PGRAPH_SURFACE,
-                              NV_PGRAPH_SURFACE_READ_3D)+1)
-                        % PG_GET_MASK(NV_PGRAPH_SURFACE,
-                                   NV_PGRAPH_SURFACE_MODULO_3D) );
+                     (read_before + 1) % modulo);
+
+            const uint32_t surface_after = pgraph_reg_r(pg, NV_PGRAPH_SURFACE);
+            const uint32_t read_after =
+                GET_MASK(surface_after, NV_PGRAPH_SURFACE_READ_3D);
+            if (trace_threshold != 0) {
+                trace.surface_after = surface_after;
+                trace.read_after = read_after;
+                trace.surface_update_ns =
+                    qemu_clock_get_ns(QEMU_CLOCK_HOST) - trace_stage_start;
+            }
+
+            xemu_debug_tools_flip_stall_trace_record(
+                XEMU_DEBUG_TOOLS_FLIP_VBLANK_READ_INCREMENT, pg,
+                NV097_SET_FLIP_READ, (uint32_t)val, read_before,
+                read_after, 0);
+
             nv2a_profile_increment();
+            if (trace_threshold != 0) {
+                trace_stage_start = qemu_clock_get_ns(QEMU_CLOCK_HOST);
+            }
             pfifo_kick(d);
+            if (trace_threshold != 0) {
+                trace.pfifo_kick_ns =
+                    qemu_clock_get_ns(QEMU_CLOCK_HOST) - trace_stage_start;
+            }
         }
         break;
     case NV_PGRAPH_RDI_DATA: {
@@ -173,8 +237,21 @@ void pgraph_write(void *opaque, hwaddr addr, uint64_t val, unsigned int size)
         break;
     }
 
-    qemu_mutex_unlock(&pg->lock);
+    PGRAPH_UNLOCK(pg);
     qemu_mutex_unlock(&d->pfifo.lock);
+
+    if (trace_threshold != 0) {
+        /* Stock never drops BQL in this path, so there is no reacquire wait.
+         * Keep the field explicit so the profiler output remains comparable. */
+        trace.bql_reacquire_wait_ns = 0;
+        trace.bql_held_after = bql_locked() ? 1 : 0;
+        trace.host_end_ns = qemu_clock_get_ns(QEMU_CLOCK_HOST);
+        trace.virtual_end_ns = qemu_clock_get_ns(QEMU_CLOCK_VIRTUAL);
+        trace.host_duration_ns = trace.host_end_ns - trace.host_start_ns;
+        trace.virtual_duration_ns =
+            (int64_t)trace.virtual_end_ns - (int64_t)trace.virtual_start_ns;
+        xemu_debug_tools_pgraph_increment_trace_record(&trace);
+    }
 }
 
 void pgraph_context_switch(NV2AState *d, unsigned int channel_id)
@@ -198,12 +275,12 @@ void pgraph_context_switch(NV2AState *d, unsigned int channel_id)
                             NV_PGRAPH_DEBUG_3_HW_CONTEXT_SWITCH));
 
         pg->waiting_for_context_switch = true;
-        qemu_mutex_unlock(&pg->lock);
+        PGRAPH_UNLOCK(pg);
         bql_lock();
         pg->pending_interrupts |= NV_PGRAPH_INTR_CONTEXT_SWITCH;
         nv2a_update_irq(d);
         bql_unlock();
-        qemu_mutex_lock(&pg->lock);
+        PGRAPH_LOCK(pg);
     }
 }
 
@@ -669,8 +746,27 @@ int pgraph_method(NV2AState *d, unsigned int subchannel,
     uint32_t graphics_class = PG_GET_MASK(NV_PGRAPH_CTX_SWITCH1,
                                        NV_PGRAPH_CTX_SWITCH1_GRCLASS);
 
+    xemu_debug_tools_pgraph_lock_trace_method_enter(
+        subchannel, graphics_class, method, parameter);
     pgraph_method_log(subchannel, graphics_class, method, parameter);
 
+    /* Diagnostics-only identity handoff for the PFIFO puller timer. The timer
+     * itself lives at the existing pfifo_run_puller() call boundary so this
+     * path adds no clock read inside PGRAPH. */
+    uint32_t pusher_method_base = method;
+    const char *pusher_method_name = NULL;
+    if (graphics_class == NV_KELVIN_PRIMITIVE) {
+        size_t pusher_method_index = METHOD_ADDR_TO_INDEX(method);
+        if (pusher_method_index < ARRAY_SIZE(pgraph_kelvin_methods) &&
+            pgraph_kelvin_methods[pusher_method_index].handler != NULL) {
+            pusher_method_base = pgraph_kelvin_methods[pusher_method_index].base;
+            pusher_method_name = pgraph_kelvin_methods[pusher_method_index].name;
+        }
+    }
+    xemu_debug_tools_pgraph_pusher_method_breakdown_note_method(
+        subchannel, graphics_class, method, pusher_method_base, parameter,
+        pusher_method_name);
+
     if (subchannel != 0) {
         // catches context switching issues on xbox d3d
         assert(graphics_class != 0x97);
@@ -807,11 +903,13 @@ int pgraph_method(NV2AState *d, unsigned int subchannel,
         goto unhandled;
     }
 
+    xemu_debug_tools_pgraph_lock_trace_method_exit();
     return num_processed;
 
 unhandled:
     trace_nv2a_pgraph_method_unhandled(subchannel, graphics_class,
                                            method, parameter);
+    xemu_debug_tools_pgraph_lock_trace_method_exit();
     return num_processed;
 }
 
@@ -849,11 +947,11 @@ DEF_METHOD(NV097, NO_OPERATION)
     pg->pending_interrupts |= NV_PGRAPH_INTR_ERROR;
     pg->waiting_for_nop = true;
 
-    qemu_mutex_unlock(&pg->lock);
+    PGRAPH_UNLOCK(pg);
     bql_lock();
     nv2a_update_irq(d);
     bql_unlock();
-    qemu_mutex_lock(&pg->lock);
+    PGRAPH_LOCK(pg);
 }
 
 DEF_METHOD(NV097, WAIT_FOR_IDLE)
@@ -863,18 +961,24 @@ DEF_METHOD(NV097, WAIT_FOR_IDLE)
 
 DEF_METHOD(NV097, SET_FLIP_READ)
 {
+    xemu_debug_tools_flip_stall_trace_record(
+        XEMU_DEBUG_TOOLS_FLIP_SET_READ, pg, NV097_SET_FLIP_READ, parameter, 0, 0, 0);
     PG_SET_MASK(NV_PGRAPH_SURFACE, NV_PGRAPH_SURFACE_READ_3D,
              parameter);
 }
 
 DEF_METHOD(NV097, SET_FLIP_WRITE)
 {
+    xemu_debug_tools_flip_stall_trace_record(
+        XEMU_DEBUG_TOOLS_FLIP_SET_WRITE, pg, NV097_SET_FLIP_WRITE, parameter, 0, 0, 0);
     PG_SET_MASK(NV_PGRAPH_SURFACE, NV_PGRAPH_SURFACE_WRITE_3D,
              parameter);
 }
 
 DEF_METHOD(NV097, SET_FLIP_MODULO)
 {
+    xemu_debug_tools_flip_stall_trace_record(
+        XEMU_DEBUG_TOOLS_FLIP_SET_MODULO, pg, NV097_SET_FLIP_MODULO, parameter, 0, 0, 0);
     PG_SET_MASK(NV_PGRAPH_SURFACE, NV_PGRAPH_SURFACE_MODULO_3D,
              parameter);
 }
@@ -894,17 +998,25 @@ DEF_METHOD(NV097, FLIP_INCREMENT_WRITE)
     uint32_t new =
         PG_GET_MASK(NV_PGRAPH_SURFACE, NV_PGRAPH_SURFACE_WRITE_3D);
 
+    xemu_debug_tools_flip_stall_trace_record(
+        XEMU_DEBUG_TOOLS_FLIP_INCREMENT_WRITE, pg,
+        NV097_FLIP_INCREMENT_WRITE, parameter, old, new, 0);
     trace_nv2a_pgraph_flip_increment_write(old, new);
     pg->frame_time++;
 }
 
 DEF_METHOD(NV097, FLIP_STALL)
 {
+    xemu_debug_tools_flip_stall_trace_begin(pg);
     trace_nv2a_pgraph_flip_stall();
     d->pgraph.renderer->ops.surface_update(d, false, true, true);
     d->pgraph.renderer->ops.flip_stall(d);
     nv2a_profile_flip_stall();
+    xemu_debug_tools_av_sync_trace_note_pgraph_flip(pg->frame_time);
     pg->waiting_for_flip = true;
+    xemu_debug_tools_flip_stall_trace_record(
+        XEMU_DEBUG_TOOLS_FLIP_WAITING_SET, pg, NV097_FLIP_STALL,
+        parameter, 0, 0, 0);
 }
 
 // TODO: these should be loading the dma objects from ramin here?
@@ -2505,9 +2617,21 @@ DEF_METHOD(NV097, SET_BEGIN_END)
             return;
         }
         nv2a_profile_inc_counter(NV2A_PROF_BEGIN_ENDS);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_SET_BEGIN_END_TOTAL, pg, 0, 0);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_RENDERER_DRAW_END, pg, 0, 0);
         d->pgraph.renderer->ops.draw_end(d);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_RENDERER_DRAW_END);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_RESET_INLINE_BUFFERS, pg, 0, 0);
         pgraph_reset_inline_buffers(pg);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_RESET_INLINE_BUFFERS);
         pg->primitive_mode = PRIM_TYPE_INVALID;
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_SET_BEGIN_END_TOTAL);
     } else {
         if (pg->primitive_mode != PRIM_TYPE_INVALID) {
             NV2A_DPRINTF("Begin without End!\n");
@@ -2847,10 +2971,13 @@ DEF_METHOD(NV097, BACK_END_WRITE_SEMAPHORE_RELEASE)
 {
     d->pgraph.renderer->ops.surface_update(d, false, true, true);
 
-    //qemu_mutex_unlock(&d->pgraph.lock);
+    //PGRAPH_UNLOCK(&d->pgraph);
     //bql_lock();
 
     uint32_t semaphore_offset = pgraph_reg_r(pg, NV_PGRAPH_SEMAPHOREOFFSET);
+    XemuDebugToolsPgraphPusherSemaphoreScope pusher_progress_scope;
+    xemu_debug_tools_pgraph_pusher_progress_semaphore_begin(
+        &pusher_progress_scope, pg->dma_semaphore, semaphore_offset, parameter);
 
     hwaddr semaphore_dma_len;
     uint8_t *semaphore_data = (uint8_t*)nv_dma_map(d, pg->dma_semaphore,
@@ -2858,9 +2985,25 @@ DEF_METHOD(NV097, BACK_END_WRITE_SEMAPHORE_RELEASE)
     assert(semaphore_offset < semaphore_dma_len);
     semaphore_data += semaphore_offset;
 
+    if (pusher_progress_scope.active) {
+        const hwaddr semaphore_phys = semaphore_data - d->vram_ptr;
+        const uint32_t semaphore_guest_va =
+            semaphore_phys < memory_region_size(d->vram) &&
+                    semaphore_phys <= 0x7FFFFFFFu
+                ? 0x80000000u + (uint32_t)semaphore_phys
+                : 0;
+        xemu_debug_tools_pgraph_pusher_progress_semaphore_target(
+            &pusher_progress_scope, (uint32_t)semaphore_phys,
+            semaphore_guest_va, ldl_le_p((uint32_t*)semaphore_data));
+    }
+    xemu_debug_tools_pgraph_pusher_progress_semaphore_prewrite(
+        &pusher_progress_scope,
+        qatomic_read(&d->pgraph.waiting_for_nop) ? 1 : 0,
+        qatomic_read(&d->pgraph.waiting_for_flip) ? 1 : 0);
+
     stl_le_p((uint32_t*)semaphore_data, parameter);
 
-    //qemu_mutex_lock(&d->pgraph.lock);
+    //PGRAPH_LOCK(&d->pgraph);
     //bql_unlock();
 }
 
@@ -3188,19 +3331,19 @@ void pgraph_process_pending(NV2AState *d)
     if (pg->renderer_switch_phase == PGRAPH_RENDERER_SWITCH_PHASE_CPU_WAITING) {
         qemu_mutex_lock(&d->pgraph.renderer_lock);
         qemu_mutex_unlock(&d->pfifo.lock);
-        qemu_mutex_lock(&d->pgraph.lock);
+        PGRAPH_LOCK(&d->pgraph);
 
         if (pg->renderer) {
             qemu_event_reset(&pg->flush_complete);
             pg->flush_pending = true;
 
             qemu_mutex_lock(&d->pfifo.lock);
-            qemu_mutex_unlock(&d->pgraph.lock);
+            PGRAPH_UNLOCK(&d->pgraph);
 
             pg->renderer->ops.process_pending(d);
 
             qemu_mutex_unlock(&d->pfifo.lock);
-            qemu_mutex_lock(&d->pgraph.lock);
+            PGRAPH_LOCK(&d->pgraph);
             while (pg->framebuffer_in_use) {
                 qemu_cond_wait(&d->pgraph.framebuffer_released,
                                &d->pgraph.renderer_lock);
@@ -3214,7 +3357,7 @@ void pgraph_process_pending(NV2AState *d)
         init_renderer(pg);
 
         qemu_mutex_unlock(&d->pgraph.renderer_lock);
-        qemu_mutex_unlock(&d->pgraph.lock);
+        PGRAPH_UNLOCK(&d->pgraph);
         qemu_mutex_lock(&d->pfifo.lock);
 
         pg->renderer_switch_phase = PGRAPH_RENDERER_SWITCH_PHASE_IDLE;
diff --git a/hw/xbox/nv2a/pgraph/pgraph.h b/hw/xbox/nv2a/pgraph/pgraph.h
index b4db914..0660a3d 100644
--- a/hw/xbox/nv2a/pgraph/pgraph.h
+++ b/hw/xbox/nv2a/pgraph/pgraph.h
@@ -28,6 +28,7 @@
 #include "qemu/units.h"
 #include "qemu/thread.h"
 #include "cpu.h"
+#include "ui/xui/debug-tools/addons/diagnostics/pgraph-lock-trace-hook.h"
 
 #include "surface.h"
 #include "texture.h"
@@ -272,6 +273,25 @@ typedef struct PGRAPHState {
     };
 } PGRAPHState;
 
+static inline void pgraph_lock_observed(PGRAPHState *pg, const char *file,
+                                        int line)
+{
+    xemu_debug_tools_pgraph_lock_trace_lock_attempt(pg, file, line);
+    qemu_mutex_lock(&pg->lock);
+    xemu_debug_tools_pgraph_lock_trace_lock_acquired(pg, file, line);
+}
+
+static inline void pgraph_unlock_observed(PGRAPHState *pg, const char *file,
+                                          int line)
+{
+    xemu_debug_tools_pgraph_lock_trace_unlock_begin(pg, file, line);
+    qemu_mutex_unlock(&pg->lock);
+    xemu_debug_tools_pgraph_lock_trace_unlock_end(pg);
+}
+
+#define PGRAPH_LOCK(pg) pgraph_lock_observed((pg), __FILE__, __LINE__)
+#define PGRAPH_UNLOCK(pg) pgraph_unlock_observed((pg), __FILE__, __LINE__)
+
 void pgraph_init(NV2AState *d);
 void pgraph_init_thread(NV2AState *d);
 void pgraph_destroy(PGRAPHState *pg);
diff --git a/hw/xbox/nv2a/pgraph/vk/draw.c b/hw/xbox/nv2a/pgraph/vk/draw.c
index d78087e..fbcfd2f 100644
--- a/hw/xbox/nv2a/pgraph/vk/draw.c
+++ b/hw/xbox/nv2a/pgraph/vk/draw.c
@@ -19,6 +19,9 @@
 
 #include "qemu/osdep.h"
 #include "qemu/fast-hash.h"
+#include "qemu/timer.h"
+#include "ui/xui/debug-tools/addons/diagnostics/flip-stall-trace-hook.h"
+#include "ui/xui/debug-tools/addons/diagnostics/pgraph-draw-trace-hook.h"
 #include "renderer.h"
 #include <math.h>
 
@@ -427,6 +430,8 @@ static void create_clear_pipeline(PGRAPHState *pg)
 
     NV2A_VK_DGROUP_BEGIN("Creating clear pipeline");
 
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_STATE_KEY, pg, 0, 0);
     PipelineKey key;
     memset(&key, 0, sizeof(key));
     key.clear = true;
@@ -435,10 +440,18 @@ static void create_clear_pipeline(PGRAPHState *pg)
     key.regs[0] = r->clear_parameter;
 
     uint64_t hash = fast_hash((void *)&key, sizeof(key));
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_STATE_KEY);
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_CACHE_LOOKUP, pg, 0, 0);
     LruNode *node = lru_lookup(&r->pipeline_cache, hash, &key);
     PipelineBinding *snode = container_of(node, PipelineBinding, node);
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_CACHE_LOOKUP);
 
     if (snode->pipeline != VK_NULL_HANDLE) {
+        xemu_debug_tools_pgraph_draw_trace_pipeline_cache(
+            XEMU_DEBUG_TOOLS_PGRAPH_PIPELINE_CACHE_HIT);
         NV2A_VK_DPRINTF("Cache hit");
         r->pipeline_binding_changed = r->pipeline_binding != snode;
         r->pipeline_binding = snode;
@@ -446,6 +459,10 @@ static void create_clear_pipeline(PGRAPHState *pg)
         return;
     }
 
+    xemu_debug_tools_pgraph_draw_trace_pipeline_cache(
+        XEMU_DEBUG_TOOLS_PGRAPH_PIPELINE_CACHE_MISS);
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_BUILD_STATE, pg, 0, 0);
     NV2A_VK_DPRINTF("Cache miss");
     nv2a_profile_inc_counter(NV2A_PROF_PIPELINE_GEN);
     memcpy(&snode->key, &key, sizeof(key));
@@ -597,9 +614,17 @@ static void create_clear_pipeline(PGRAPHState *pg)
     };
 
     VkPipeline pipeline;
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_BUILD_STATE);
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_CREATE, pg, 0, 0);
     VK_CHECK(vkCreateGraphicsPipelines(r->device, r->vk_pipeline_cache, 1,
                                        &pipeline_info, NULL, &pipeline));
 
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_CREATE);
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_INSTALL, pg, 0, 0);
     snode->pipeline = pipeline;
     snode->layout = layout;
     snode->render_pass = pipeline_info.renderPass;
@@ -608,6 +633,8 @@ static void create_clear_pipeline(PGRAPHState *pg)
     r->pipeline_binding = snode;
     r->pipeline_binding_changed = true;
 
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_INSTALL);
     NV2A_VK_DGROUP_END();
 }
 
@@ -696,8 +723,18 @@ static void create_pipeline(PGRAPHState *pg)
     NV2AState *d = container_of(pg, NV2AState, pgraph);
     PGRAPHVkState *r = pg->vk_renderer_state;
 
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_TEXTURE_BIND, pg, 0, 0);
     pgraph_vk_bind_textures(d);
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_TEXTURE_BIND);
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_SHADER_BIND, pg, 0, 0);
     pgraph_vk_bind_shaders(pg);
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_SHADER_BIND);
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_STATE_KEY, pg, 0, 0);
 
     // FIXME: If nothing was dirty, don't even try creating the key or hashing.
     //        Just use the same pipeline.
@@ -707,6 +744,10 @@ static void create_pipeline(PGRAPHState *pg)
     // FIXME: We could clear less
 
     if (r->pipeline_binding && !pipeline_dirty) {
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_STATE_KEY);
+        xemu_debug_tools_pgraph_draw_trace_pipeline_cache(
+            XEMU_DEBUG_TOOLS_PGRAPH_PIPELINE_FAST_REUSE);
         NV2A_VK_DPRINTF("Cache hit");
         NV2A_VK_DGROUP_END();
         return;
@@ -716,9 +757,17 @@ static void create_pipeline(PGRAPHState *pg)
     init_pipeline_key(pg, &key);
     uint64_t hash = fast_hash((void *)&key, sizeof(key));
 
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_STATE_KEY);
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_CACHE_LOOKUP, pg, 0, 0);
     LruNode *node = lru_lookup(&r->pipeline_cache, hash, &key);
     PipelineBinding *snode = container_of(node, PipelineBinding, node);
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_CACHE_LOOKUP);
     if (snode->pipeline != VK_NULL_HANDLE) {
+        xemu_debug_tools_pgraph_draw_trace_pipeline_cache(
+            XEMU_DEBUG_TOOLS_PGRAPH_PIPELINE_CACHE_HIT);
         NV2A_VK_DPRINTF("Cache hit");
         r->pipeline_binding_changed = r->pipeline_binding != snode;
         r->pipeline_binding = snode;
@@ -726,6 +775,10 @@ static void create_pipeline(PGRAPHState *pg)
         return;
     }
 
+    xemu_debug_tools_pgraph_draw_trace_pipeline_cache(
+        XEMU_DEBUG_TOOLS_PGRAPH_PIPELINE_CACHE_MISS);
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_BUILD_STATE, pg, 0, 0);
     NV2A_VK_DPRINTF("Cache miss");
     nv2a_profile_inc_counter(NV2A_PROF_PIPELINE_GEN);
 
@@ -1005,9 +1058,17 @@ static void create_pipeline(PGRAPHState *pg)
         .basePipelineHandle = VK_NULL_HANDLE,
     };
     VkPipeline pipeline;
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_BUILD_STATE);
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_CREATE, pg, 0, 0);
     VK_CHECK(vkCreateGraphicsPipelines(r->device, r->vk_pipeline_cache, 1,
                                        &pipeline_create_info, NULL, &pipeline));
 
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_CREATE);
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_INSTALL, pg, 0, 0);
     snode->pipeline = pipeline;
     snode->layout = layout;
     snode->render_pass = pipeline_create_info.renderPass;
@@ -1016,6 +1077,8 @@ static void create_pipeline(PGRAPHState *pg)
     r->pipeline_binding = snode;
     r->pipeline_binding_changed = true;
 
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_INSTALL);
     NV2A_VK_DGROUP_END();
 }
 
@@ -1213,6 +1276,17 @@ const enum NV2A_PROF_COUNTERS_ENUM finish_reason_to_counter_enum[] = {
 void pgraph_vk_finish(PGRAPHState *pg, FinishReason finish_reason)
 {
     PGRAPHVkState *r = pg->vk_renderer_state;
+    uint64_t flip_trace_start_ns = 0;
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_FINISH_TOTAL, pg, finish_reason, r->in_command_buffer ? 1 : 0);
+
+
+    if (finish_reason == VK_FINISH_REASON_FLIP_STALL) {
+        flip_trace_start_ns = qemu_clock_get_ns(QEMU_CLOCK_HOST);
+        xemu_debug_tools_flip_stall_trace_record(
+            XEMU_DEBUG_TOOLS_FLIP_VK_FINISH_BEGIN, pg,
+            NV097_FLIP_STALL, (uint32_t)finish_reason, 0, 0, 0);
+    }
 
     assert(!r->in_draw);
     assert(r->debug_depth == 0);
@@ -1259,8 +1333,13 @@ void pgraph_vk_finish(PGRAPHState *pg, FinishReason finish_reason)
         };
         nv2a_profile_inc_counter(NV2A_PROF_QUEUE_SUBMIT);
         vkResetFences(r->device, 1, &r->command_buffer_fence);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_FINISH_QUEUE_SUBMIT, pg, finish_reason, 0);
         VK_CHECK(vkQueueSubmit(r->queue, ARRAY_SIZE(submit_infos), submit_infos,
                                r->command_buffer_fence));
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_FINISH_QUEUE_SUBMIT);
+
         r->submit_count += 1;
 
         bool check_budget = false;
@@ -1277,8 +1356,13 @@ void pgraph_vk_finish(PGRAPHState *pg, FinishReason finish_reason)
             check_budget = true;
         }
 
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_FINISH_FENCE_WAIT, pg, finish_reason, 0);
         VK_CHECK(vkWaitForFences(r->device, 1, &r->command_buffer_fence,
                                  VK_TRUE, UINT64_MAX));
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_FINISH_FENCE_WAIT);
+
 
         r->descriptor_set_index = 0;
         r->in_command_buffer = false;
@@ -1293,6 +1377,16 @@ void pgraph_vk_finish(PGRAPHState *pg, FinishReason finish_reason)
     pgraph_vk_process_pending_reports_internal(d);
 
     pgraph_vk_compute_finish_complete(r);
+
+    if (finish_reason == VK_FINISH_REASON_FLIP_STALL) {
+        uint64_t end_ns = qemu_clock_get_ns(QEMU_CLOCK_HOST);
+        xemu_debug_tools_flip_stall_trace_record(
+            XEMU_DEBUG_TOOLS_FLIP_VK_FINISH_END, pg,
+            NV097_FLIP_STALL, (uint32_t)finish_reason, 0, 0,
+            end_ns >= flip_trace_start_ns ? end_ns - flip_trace_start_ns : 0);
+    }
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_FINISH_TOTAL);
 }
 
 void pgraph_vk_begin_command_buffer(PGRAPHState *pg)
@@ -1356,18 +1450,29 @@ static void begin_pre_draw(PGRAPHState *pg)
 {
     PGRAPHVkState *r = pg->vk_renderer_state;
 
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PRE_DRAW, pg, 0, 0);
+
     assert(r->color_binding || r->zeta_binding);
     assert(!r->color_binding || r->color_binding->initialized);
     assert(!r->zeta_binding || r->zeta_binding->initialized);
 
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_PREP, pg,
+        pg->clearing ? 1 : 0, 0);
     if (pg->clearing) {
         create_clear_pipeline(pg);
     } else {
         create_pipeline(pg);
     }
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_PREP);
 
     bool render_pass_dirty = r->pipeline_binding->render_pass != r->render_pass;
 
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_FRAMEBUFFER_PREP, pg,
+        r->framebuffer_dirty ? 1 : 0, render_pass_dirty ? 1 : 0);
     if (r->framebuffer_dirty || render_pass_dirty) {
         pgraph_vk_ensure_not_in_render_pass(pg);
     }
@@ -1378,14 +1483,35 @@ static void begin_pre_draw(PGRAPHState *pg)
         create_frame_buffer(pg);
         r->framebuffer_dirty = false;
     }
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_FRAMEBUFFER_PREP);
     if (!pg->clearing) {
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_DESCRIPTOR_UPDATE, pg, 0, 0);
         pgraph_vk_update_descriptor_sets(pg);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_DESCRIPTOR_UPDATE);
     }
+    /* Descriptor updates may finish the command buffer and destroy framebuffers.
+     * Keep this recovery after them; timing hooks must not reorder renderer work.
+     */
     if (r->framebuffer_index == 0) {
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_FRAMEBUFFER_PREP, pg, 0, 0);
         create_frame_buffer(pg);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_FRAMEBUFFER_PREP);
     }
 
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_COMMAND_BUFFER, pg,
+        r->in_command_buffer ? 1 : 0, 0);
     pgraph_vk_ensure_command_buffer(pg);
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_COMMAND_BUFFER);
+
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PRE_DRAW);
 }
 
 static float clamp_line_width_to_device_limits(PGRAPHState *pg, float width)
@@ -1407,8 +1533,14 @@ static void begin_draw(PGRAPHState *pg)
 {
     PGRAPHVkState *r = pg->vk_renderer_state;
 
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_BEGIN_DRAW, pg, 0, 0);
+
     assert(r->in_command_buffer);
 
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_RENDER_PASS, pg,
+        r->in_render_pass ? 1 : 0, r->query_in_flight ? 1 : 0);
     // Visibility testing
     if (!pg->clearing && pg->zpass_pixel_count_enable) {
         if (r->new_query_needed && r->query_in_flight) {
@@ -1434,8 +1566,12 @@ static void begin_draw(PGRAPHState *pg)
         begin_render_pass(pg);
         must_bind_pipeline = true;
     }
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_RENDER_PASS);
 
     if (must_bind_pipeline) {
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_BIND, pg, 0, 0);
         nv2a_profile_inc_counter(NV2A_PROF_PIPELINE_BIND);
         vkCmdBindPipeline(r->command_buffer, VK_PIPELINE_BIND_POINT_GRAPHICS,
                           r->pipeline_binding->pipeline);
@@ -1480,14 +1616,22 @@ static void begin_draw(PGRAPHState *pg)
                 clamp_line_width_to_device_limits(pg, pg->surface_scale_factor);
             vkCmdSetLineWidth(r->command_buffer, line_width);
         }
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_PIPELINE_BIND);
     }
 
     if (!pg->clearing) {
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_DESCRIPTOR_BIND, pg, 0, 0);
         bind_descriptor_sets(pg);
         push_vertex_attr_values(pg);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_DESCRIPTOR_BIND);
     }
 
     r->in_draw = true;
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_BEGIN_DRAW);
 }
 
 static void end_draw(PGRAPHState *pg)
@@ -1509,6 +1653,9 @@ void pgraph_vk_draw_end(NV2AState *d)
     PGRAPHState *pg = &d->pgraph;
     PGRAPHVkState *r = pg->vk_renderer_state;
 
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_DRAW_END_TOTAL, pg, 0, 0);
+
     uint32_t control_0 = pgraph_reg_r(pg, NV_PGRAPH_CONTROL_0);
     bool mask_alpha = control_0 & NV_PGRAPH_CONTROL_0_ALPHA_WRITE_ENABLE;
     bool mask_red = control_0 & NV_PGRAPH_CONTROL_0_RED_WRITE_ENABLE;
@@ -1529,10 +1676,16 @@ void pgraph_vk_draw_end(NV2AState *d)
         // processing, it is legal to attempt a draw that is entirely
         // clipped regardless of 0x880. See xemu#635 for context.
         NV2A_VK_DPRINTF("nop draw!\n");
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_DRAW_END_TOTAL);
         return;
     }
 
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_FLUSH_DRAW, pg, 0, 0);
     pgraph_vk_flush_draw(d);
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_FLUSH_DRAW);
 
     pg->draw_time++;
     if (r->color_binding && pgraph_color_write_enabled(pg)) {
@@ -1542,7 +1695,14 @@ void pgraph_vk_draw_end(NV2AState *d)
         r->zeta_binding->draw_time = pg->draw_time;
     }
 
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_SURFACE_DIRTY, pg,
+        color_write ? 1 : 0, (depth_test || stencil_test) ? 1 : 0);
     pgraph_vk_set_surface_dirty(pg, color_write, depth_test || stencil_test);
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_SURFACE_DIRTY);
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_DRAW_END_TOTAL);
 }
 
 static int compare_memory_sync_requirement_by_addr(const void *p1,
@@ -1565,6 +1725,10 @@ static void sync_vertex_ram_buffer(PGRAPHState *pg)
         return;
     }
 
+    xemu_debug_tools_pgraph_draw_trace_metric(
+        XEMU_DEBUG_TOOLS_PGRAPH_METRIC_VERTEX_REQUESTED_RANGES, r->num_vertex_ram_buffer_syncs);
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_RANGE_PREP, pg, 0, 0);
     // Align sync requirements to page boundaries
     NV2A_VK_DGROUP_BEGIN("Sync vertex RAM buffer");
 
@@ -1620,17 +1784,39 @@ static void sync_vertex_ram_buffer(PGRAPHState *pg)
         NV2A_VK_DPRINTF("Reduced to %d sync checks", num_syncs);
     }
 
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_RANGE_PREP);
+    xemu_debug_tools_pgraph_draw_trace_metric(
+        XEMU_DEBUG_TOOLS_PGRAPH_METRIC_VERTEX_MERGED_RANGES, num_syncs);
     for (int i = 0; i < num_syncs; i++) {
         hwaddr addr = merged[i].addr;
         VkDeviceSize size = merged[i].size;
 
         NV2A_VK_DPRINTF("- %d: %08"HWADDR_PRIx" %zd bytes", i, addr, size);
 
+        xemu_debug_tools_pgraph_draw_trace_metric(
+            XEMU_DEBUG_TOOLS_PGRAPH_METRIC_VERTEX_TESTED_BYTES, size);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_DIRTY_CHECK, pg, 0, 0);
         if (memory_region_test_and_clear_dirty(d->vram, addr, size,
                                                DIRTY_MEMORY_NV2A)) {
+            xemu_debug_tools_pgraph_draw_trace_stage_end(
+                XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_DIRTY_CHECK);
+            xemu_debug_tools_pgraph_draw_trace_metric(
+                XEMU_DEBUG_TOOLS_PGRAPH_METRIC_VERTEX_DIRTY_RANGES, 1);
+            xemu_debug_tools_pgraph_draw_trace_metric(
+                XEMU_DEBUG_TOOLS_PGRAPH_METRIC_VERTEX_DIRTY_BYTES, size);
             NV2A_VK_DPRINTF("Memory dirty. Synchronizing...");
+            xemu_debug_tools_pgraph_draw_trace_stage_begin(
+                XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_RANGE_UPDATE, pg, 0, 0);
+
             pgraph_vk_update_vertex_ram_buffer(pg, addr, d->vram_ptr + addr,
                                                size);
+            xemu_debug_tools_pgraph_draw_trace_stage_end(
+                XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_RANGE_UPDATE);
+        } else {
+            xemu_debug_tools_pgraph_draw_trace_stage_end(
+                XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_DIRTY_CHECK);
         }
     }
 
@@ -2038,31 +2224,62 @@ void pgraph_vk_flush_draw(NV2AState *d)
         assert(pg->inline_buffer_length == 0);
         assert(pg->inline_array_length == 0);
 
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_BIND, pg,
+            pg->draw_arrays_min_start, pg->draw_arrays_max_count);
         pgraph_vk_bind_vertex_attributes(d, pg->draw_arrays_min_start,
                                          pg->draw_arrays_max_count - 1, false,
                                          0, pg->draw_arrays_max_count - 1);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_BIND);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_ELEMENT_SCAN, pg,
+            pg->draw_arrays_length, 0);
         uint32_t min_element = INT_MAX;
         uint32_t max_element = 0;
         for (int i = 0; i < pg->draw_arrays_length; i++) {
             min_element = MIN(pg->draw_arrays_start[i], min_element);
             max_element = MAX(max_element, pg->draw_arrays_start[i] + pg->draw_arrays_count[i]);
         }
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_ELEMENT_SCAN);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_SYNC_VERTEX_RAM, pg, 0, 0);
         sync_vertex_ram_buffer(pg);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_SYNC_VERTEX_RAM);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_REMAP_ATTRIBUTES, pg, max_element, 0);
         VertexBufferRemap remap = remap_unaligned_attributes(pg, max_element);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_REMAP_ATTRIBUTES);
 
         begin_pre_draw(pg);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_UPLOAD, pg, max_element, 0);
         copy_remapped_attributes_to_inline_buffer(pg, remap, 0, max_element);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_UPLOAD);
         pgraph_vk_begin_debug_marker(r, r->command_buffer, RGBA_BLUE,
                                      "Draw Arrays");
         begin_draw(pg);
         bind_vertex_buffer(pg, remap.attributes, 0);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_ISSUE_DRAW, pg,
+            pg->draw_arrays_length, pg->draw_arrays_max_count);
         for (int i = 0; i < pg->draw_arrays_length; i++) {
             uint32_t start = pg->draw_arrays_start[i],
                      count = pg->draw_arrays_count[i];
             NV2A_VK_DPRINTF("- [%d] Start:%d Count:%d", i, start, count);
             vkCmdDraw(r->command_buffer, count, 1, start, 0);
         }
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_ISSUE_DRAW);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_END_DRAW, pg, 0, 0);
         end_draw(pg);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_END_DRAW);
         pgraph_vk_end_debug_marker(r, r->command_buffer);
 
         NV2A_VK_DGROUP_END();
@@ -2078,22 +2295,44 @@ void pgraph_vk_flush_draw(NV2AState *d)
 
         ensure_buffer_space(pg, BUFFER_INDEX_STAGING, index_data_size);
 
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_ELEMENT_SCAN, pg,
+            pg->inline_elements_length, 0);
         uint32_t min_element = (uint32_t)-1;
         uint32_t max_element = 0;
         for (int i = 0; i < pg->inline_elements_length; i++) {
             max_element = MAX(pg->inline_elements[i], max_element);
             min_element = MIN(pg->inline_elements[i], min_element);
         }
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_ELEMENT_SCAN);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_BIND, pg, min_element, max_element);
         pgraph_vk_bind_vertex_attributes(
             d, min_element, max_element, false, 0,
             pg->inline_elements[pg->inline_elements_length - 1]);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_BIND);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_SYNC_VERTEX_RAM, pg, 0, 0);
         sync_vertex_ram_buffer(pg);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_SYNC_VERTEX_RAM);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_REMAP_ATTRIBUTES, pg, max_element + 1, 0);
         VertexBufferRemap remap = remap_unaligned_attributes(pg, max_element + 1);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_REMAP_ATTRIBUTES);
 
         begin_pre_draw(pg);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_UPLOAD, pg,
+            pg->inline_elements_length, index_data_size);
         copy_remapped_attributes_to_inline_buffer(pg, remap, 0, max_element + 1);
         VkDeviceSize buffer_offset = pgraph_vk_update_index_buffer(
             pg, pg->inline_elements, index_data_size);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_UPLOAD);
         pgraph_vk_begin_debug_marker(r, r->command_buffer, RGBA_BLUE,
                                      "Inline Elements");
         begin_draw(pg);
@@ -2101,9 +2340,18 @@ void pgraph_vk_flush_draw(NV2AState *d)
         vkCmdBindIndexBuffer(r->command_buffer,
                              r->storage_buffers[BUFFER_INDEX].buffer,
                              buffer_offset, VK_INDEX_TYPE_UINT32);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_ISSUE_DRAW, pg,
+            pg->inline_elements_length, 0);
         vkCmdDrawIndexed(r->command_buffer, pg->inline_elements_length, 1, 0, 0,
                          0);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_ISSUE_DRAW);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_END_DRAW, pg, 0, 0);
         end_draw(pg);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_END_DRAW);
         pgraph_vk_end_debug_marker(r, r->command_buffer);
 
         NV2A_VK_DGROUP_END();
@@ -2117,7 +2365,15 @@ void pgraph_vk_flush_draw(NV2AState *d)
         size_t sizes[NV2A_VERTEXSHADER_ATTRIBUTES];
         size_t offset = 0;
 
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_BIND, pg,
+            pg->inline_buffer_length, 0);
         pgraph_vk_bind_vertex_attributes_inline(d);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_BIND);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_ELEMENT_SCAN, pg,
+            r->num_active_vertex_attribute_descriptions, 0);
         for (int i = 0; i < r->num_active_vertex_attribute_descriptions; i++) {
             int attr_index = r->vertex_attribute_descriptions[i].location;
 
@@ -2130,17 +2386,32 @@ void pgraph_vk_flush_draw(NV2AState *d)
             attr->inline_buffer_populated = false;
             offset += vertex_data_size;
         }
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_ELEMENT_SCAN);
         ensure_buffer_space(pg, BUFFER_VERTEX_INLINE_STAGING, offset);
 
         begin_pre_draw(pg);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_UPLOAD, pg, offset, 0);
         VkDeviceSize buffer_offset = pgraph_vk_update_vertex_inline_buffer(
             pg, data, sizes, r->num_active_vertex_attribute_descriptions);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_UPLOAD);
         pgraph_vk_begin_debug_marker(r, r->command_buffer, RGBA_BLUE,
                                      "Inline Buffer");
         begin_draw(pg);
         bind_inline_vertex_buffer(pg, buffer_offset);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_ISSUE_DRAW, pg,
+            pg->inline_buffer_length, 0);
         vkCmdDraw(r->command_buffer, pg->inline_buffer_length, 1, 0, 0);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_ISSUE_DRAW);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_END_DRAW, pg, 0, 0);
         end_draw(pg);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_END_DRAW);
         pgraph_vk_end_debug_marker(r, r->command_buffer);
 
         NV2A_VK_DGROUP_END();
@@ -2152,6 +2423,9 @@ void pgraph_vk_flush_draw(NV2AState *d)
         ensure_buffer_space(pg, BUFFER_VERTEX_INLINE_STAGING,
                                inline_array_data_size);
 
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_ELEMENT_SCAN, pg,
+            pg->inline_array_length, 0);
         unsigned int offset = 0;
         for (int i = 0; i < NV2A_VERTEXSHADER_ATTRIBUTES; i++) {
             VertexAttribute *attr = &pg->vertex_attributes[i];
@@ -2167,24 +2441,44 @@ void pgraph_vk_flush_draw(NV2AState *d)
             offset += attr->size * attr->count;
             offset = ROUND_UP(offset, attr->size);
         }
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_ELEMENT_SCAN);
 
         unsigned int vertex_size = offset;
         unsigned int index_count = pg->inline_array_length * 4 / vertex_size;
 
         NV2A_DPRINTF("draw inline array %d, %d\n", vertex_size, index_count);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_BIND, pg,
+            vertex_size, index_count);
         pgraph_vk_bind_vertex_attributes(d, 0, index_count - 1, true,
                                          vertex_size, index_count - 1);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_BIND);
 
         begin_pre_draw(pg);
         void *inline_array_data = pg->inline_array;
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_UPLOAD, pg,
+            inline_array_data_size, index_count);
         VkDeviceSize buffer_offset = pgraph_vk_update_vertex_inline_buffer(
             pg, &inline_array_data, &inline_array_data_size, 1);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_UPLOAD);
         pgraph_vk_begin_debug_marker(r, r->command_buffer, RGBA_BLUE,
                                      "Inline Array");
         begin_draw(pg);
         bind_inline_vertex_buffer(pg, buffer_offset);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_ISSUE_DRAW, pg, index_count, 0);
         vkCmdDraw(r->command_buffer, index_count, 1, 0, 0);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_ISSUE_DRAW);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_END_DRAW, pg, 0, 0);
         end_draw(pg);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_END_DRAW);
         pgraph_vk_end_debug_marker(r, r->command_buffer);
         NV2A_VK_DGROUP_END();
     } else {
diff --git a/hw/xbox/nv2a/pgraph/vk/renderer.c b/hw/xbox/nv2a/pgraph/vk/renderer.c
index 4272bbc..fd8f9c2 100644
--- a/hw/xbox/nv2a/pgraph/vk/renderer.c
+++ b/hw/xbox/nv2a/pgraph/vk/renderer.c
@@ -19,6 +19,7 @@
 
 #include "hw/xbox/nv2a/nv2a_int.h"
 #include "renderer.h"
+#include "ui/xui/debug-tools/addons/diagnostics/pgraph-flip-delay-trace-hook.h"
 
 #include "gloffscreen.h"
 
@@ -117,13 +118,16 @@ static void pgraph_vk_process_pending(NV2AState *d)
 {
     PGRAPHVkState *r = d->pgraph.vk_renderer_state;
 
+    xemu_debug_tools_pgraph_flip_delay_renderer_gate_begin(
+        &d->pgraph, XEMU_PGRAPH_FLIP_DELAY_RENDERER_VK);
+
     if (qatomic_read(&r->downloads_pending) ||
         qatomic_read(&r->download_dirty_surfaces_pending) ||
         qatomic_read(&d->pgraph.sync_pending) ||
         qatomic_read(&d->pgraph.flush_pending)
     ) {
         qemu_mutex_unlock(&d->pfifo.lock);
-        qemu_mutex_lock(&d->pgraph.lock);
+        PGRAPH_LOCK(&d->pgraph);
         if (qatomic_read(&r->downloads_pending)) {
             pgraph_vk_process_pending_downloads(d);
         }
@@ -136,9 +140,10 @@ static void pgraph_vk_process_pending(NV2AState *d)
         if (qatomic_read(&d->pgraph.flush_pending)) {
             pgraph_vk_flush(d);
         }
-        qemu_mutex_unlock(&d->pgraph.lock);
+        PGRAPH_UNLOCK(&d->pgraph);
         qemu_mutex_lock(&d->pfifo.lock);
     }
+    xemu_debug_tools_pgraph_flip_delay_renderer_gate_cancel();
 }
 
 static void pgraph_vk_flip_stall(NV2AState *d)
diff --git a/hw/xbox/nv2a/pgraph/vk/surface.c b/hw/xbox/nv2a/pgraph/vk/surface.c
index 9d438f2..8fb0f9f 100644
--- a/hw/xbox/nv2a/pgraph/vk/surface.c
+++ b/hw/xbox/nv2a/pgraph/vk/surface.c
@@ -41,19 +41,19 @@ void pgraph_vk_set_surface_scale_factor(NV2AState *d, unsigned int scale)
     qemu_mutex_unlock(&d->pfifo.lock);
 
     // FIXME: It's just flush
-    qemu_mutex_lock(&d->pgraph.lock);
+    PGRAPH_LOCK(&d->pgraph);
     qemu_event_reset(&d->pgraph.vk_renderer_state->dirty_surfaces_download_complete);
     qatomic_set(&d->pgraph.vk_renderer_state->download_dirty_surfaces_pending, true);
-    qemu_mutex_unlock(&d->pgraph.lock);
+    PGRAPH_UNLOCK(&d->pgraph);
     qemu_mutex_lock(&d->pfifo.lock);
     pfifo_kick(d);
     qemu_mutex_unlock(&d->pfifo.lock);
     qemu_event_wait(&d->pgraph.vk_renderer_state->dirty_surfaces_download_complete);
 
-    qemu_mutex_lock(&d->pgraph.lock);
+    PGRAPH_LOCK(&d->pgraph);
     qemu_event_reset(&d->pgraph.flush_complete);
     qatomic_set(&d->pgraph.flush_pending, true);
-    qemu_mutex_unlock(&d->pgraph.lock);
+    PGRAPH_UNLOCK(&d->pgraph);
     qemu_mutex_lock(&d->pfifo.lock);
     pfifo_kick(d);
     qemu_mutex_unlock(&d->pfifo.lock);
@@ -538,7 +538,7 @@ static void surface_access_callback(void *opaque, MemoryRegion *mr, hwaddr addr,
                                     hwaddr len, bool write)
 {
     NV2AState *d = (NV2AState *)opaque;
-    qemu_mutex_lock(&d->pgraph.lock);
+    PGRAPH_LOCK(&d->pgraph);
 
     PGRAPHVkState *r = d->pgraph.vk_renderer_state;
     bool wait_for_downloads = false;
@@ -567,7 +567,7 @@ static void surface_access_callback(void *opaque, MemoryRegion *mr, hwaddr addr,
         }
     }
 
-    qemu_mutex_unlock(&d->pgraph.lock);
+    PGRAPH_UNLOCK(&d->pgraph);
 
     if (wait_for_downloads) {
         qemu_mutex_lock(&d->pfifo.lock);
diff --git a/hw/xbox/nv2a/pgraph/vk/vertex.c b/hw/xbox/nv2a/pgraph/vk/vertex.c
index 1e84101..e94062b 100644
--- a/hw/xbox/nv2a/pgraph/vk/vertex.c
+++ b/hw/xbox/nv2a/pgraph/vk/vertex.c
@@ -24,6 +24,7 @@
  */
 
 #include "renderer.h"
+#include "ui/xui/debug-tools/addons/diagnostics/pgraph-draw-trace-hook.h"
 
 VkDeviceSize pgraph_vk_update_index_buffer(PGRAPHState *pg, void *data,
                                            VkDeviceSize size)
@@ -47,23 +48,55 @@ void pgraph_vk_update_vertex_ram_buffer(PGRAPHState *pg, hwaddr offset,
 {
     PGRAPHVkState *r = pg->vk_renderer_state;
 
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_SURFACE_DOWNLOAD, pg, 0, 0);
     pgraph_vk_download_surfaces_in_range_if_dirty(pg, offset, size);
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_SURFACE_DOWNLOAD);
 
+
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_OVERLAP_CHECK, pg, 0, 0);
     size_t start_bit = offset / TARGET_PAGE_SIZE;
     size_t end_bit = TARGET_PAGE_ALIGN(offset + size) / TARGET_PAGE_SIZE;
     size_t nbits = end_bit - start_bit;
 
     if (find_next_bit(r->uploaded_bitmap, start_bit + nbits, start_bit) <
         end_bit) {
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_OVERLAP_CHECK);
+        xemu_debug_tools_pgraph_draw_trace_metric(
+            XEMU_DEBUG_TOOLS_PGRAPH_METRIC_VERTEX_OVERLAP_FINISHES, 1);
+        xemu_debug_tools_pgraph_draw_trace_stage_begin(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_DIRTY_FINISH, pg, 0, 0);
         // Vertex data changed while building the draw list. Finish drawing
         // before updating RAM buffer.
         pgraph_vk_finish(pg, VK_FINISH_REASON_VERTEX_BUFFER_DIRTY);
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_DIRTY_FINISH);
+    } else {
+        xemu_debug_tools_pgraph_draw_trace_stage_end(
+            XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_OVERLAP_CHECK);
     }
 
     nv2a_profile_inc_counter(NV2A_PROF_GEOM_BUFFER_UPDATE_1);
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_COPY, pg, 0, 0);
     memcpy(r->storage_buffers[BUFFER_VERTEX_RAM].mapped + offset, data, size);
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_COPY);
+    xemu_debug_tools_pgraph_draw_trace_metric(
+        XEMU_DEBUG_TOOLS_PGRAPH_METRIC_VERTEX_COPY_CALLS, 1);
+    xemu_debug_tools_pgraph_draw_trace_metric(
+        XEMU_DEBUG_TOOLS_PGRAPH_METRIC_VERTEX_COPY_BYTES, size);
 
+
+    xemu_debug_tools_pgraph_draw_trace_stage_begin(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_BITMAP, pg, 0, 0);
     bitmap_set(r->uploaded_bitmap, start_bit, nbits);
+    xemu_debug_tools_pgraph_draw_trace_stage_end(
+        XEMU_DEBUG_TOOLS_PGRAPH_DRAW_VK_VERTEX_BITMAP);
+
 }
 
 static void update_memory_buffer(NV2AState *d, hwaddr addr, hwaddr size)
diff --git a/hw/xbox/nv2a/ptimer.c b/hw/xbox/nv2a/ptimer.c
index 87b5484..0645216 100644
--- a/hw/xbox/nv2a/ptimer.c
+++ b/hw/xbox/nv2a/ptimer.c
@@ -21,6 +21,11 @@
  */
 
 #include "nv2a_int.h"
+#include "../../../ui/xui/debug-tools/addons/diagnostics/ptimer-trace-hook.h"
+#include "../../../ui/xui/debug-tools/addons/diagnostics/qemu-timer-trace-hook.h"
+#include "../../../ui/xui/debug-tools/addons/diagnostics/pgraph-ptimer-latency-trace-hook.h"
+#include "../../../ui/xui/debug-tools/addons/diagnostics/scheduler-trace-hook.h"
+#include "../../../ui/xui/debug-tools/addons/diagnostics/ptimer-irq-delivery-trace.h"
 
 #define CLOCK_HIGH_MASK 0x1fffffffULL
 #define ALARM_MASK 0xffffffe0ULL
@@ -62,11 +67,18 @@ void ptimer_reset(NV2AState *d)
     d->ptimer.numerator = 1;
     d->ptimer.denominator = 1;
     timer_del(&d->ptimer.timer);
+    xemu_debug_tools_ptimer_trace_record(
+        d, XEMU_DEBUG_TOOLS_PTIMER_TRACE_TIMER_DELETE, 0, 0);
+    xemu_debug_tools_ptimer_trace_record(
+        d, XEMU_DEBUG_TOOLS_PTIMER_TRACE_RESET, 0, 0);
 }
 
 void ptimer_init(NV2AState *d)
 {
     timer_init_ns(&d->ptimer.timer, QEMU_CLOCK_VIRTUAL, ptimer_alarm_fired, d);
+    xemu_debug_tools_ptimer_trace_init();
+    xemu_debug_tools_qemu_timer_trace_init();
+    xemu_debug_tools_qemu_timer_trace_watch(&d->ptimer.timer);
     ptimer_reset(d);
 }
 
@@ -129,6 +141,10 @@ static void schedule_qemu_timer(NV2AState *d)
 {
     if (!(d->ptimer.enabled_interrupts & NV_PTIMER_INTR_0_ALARM)) {
         timer_del(&d->ptimer.timer);
+        xemu_debug_tools_ptimer_trace_record(
+            d, XEMU_DEBUG_TOOLS_PTIMER_TRACE_SCHEDULE_DISABLED, 0, 0);
+        xemu_debug_tools_ptimer_trace_record(
+            d, XEMU_DEBUG_TOOLS_PTIMER_TRACE_TIMER_DELETE, 0, 0);
         return;
     }
 
@@ -137,10 +153,21 @@ static void schedule_qemu_timer(NV2AState *d)
         ptimer_alarm_distance(reg_now, d->ptimer.alarm_time);
 
     if (!diff_reg_time) {
+        const uint32_t previous = d->ptimer.pending_interrupts;
         d->ptimer.pending_interrupts |= NV_PTIMER_INTR_0_ALARM;
         d->ptimer.alarm_time =
             get_next_alarm_time(reg_now, d->ptimer.alarm_time);
+        xemu_debug_tools_ptimer_irq_delivery_note_set(
+            d->ptimer.pending_interrupts, d->ptimer.enabled_interrupts,
+            d->pmc.pending_interrupts, d->pmc.enabled_interrupts);
         nv2a_update_irq(d);
+        xemu_debug_tools_ptimer_trace_record(
+            d, XEMU_DEBUG_TOOLS_PTIMER_TRACE_IRQ_SET,
+            d->ptimer.pending_interrupts, previous);
+        xemu_debug_tools_scheduler_trace_note_irq(
+            XEMU_DEBUG_TOOLS_SCHED_WAKE_PTIMER,
+            d->ptimer.pending_interrupts, d->ptimer.enabled_interrupts);
+        xemu_debug_tools_ptimer_irq_latency_note_set(d);
         diff_reg_time = ptimer_alarm_distance(reg_now, d->ptimer.alarm_time);
     }
 
@@ -150,6 +177,8 @@ static void schedule_qemu_timer(NV2AState *d)
 
     timer_mod(&d->ptimer.timer,
               qemu_clock_get_ns(QEMU_CLOCK_VIRTUAL) + diff_ns);
+    xemu_debug_tools_ptimer_trace_record(
+        d, XEMU_DEBUG_TOOLS_PTIMER_TRACE_SCHEDULE, 0, 0);
 }
 
 static void ptimer_alarm_fired(void *opaque)
@@ -157,11 +186,28 @@ static void ptimer_alarm_fired(void *opaque)
     NV2AState *d = (NV2AState *)opaque;
     uint64_t reg_now = get_reg_time(d);
 
+    xemu_debug_tools_ptimer_trace_record(
+        d, (d->ptimer.enabled_interrupts & NV_PTIMER_INTR_0_ALARM)
+               ? XEMU_DEBUG_TOOLS_PTIMER_TRACE_CALLBACK
+               : XEMU_DEBUG_TOOLS_PTIMER_TRACE_CALLBACK_DISABLED,
+        0, 0);
+
     if (is_alarm_reached(reg_now, d->ptimer.alarm_time)) {
+        const uint32_t previous = d->ptimer.pending_interrupts;
         d->ptimer.pending_interrupts |= NV_PTIMER_INTR_0_ALARM;
         d->ptimer.alarm_time =
             get_next_alarm_time(reg_now, d->ptimer.alarm_time);
+        xemu_debug_tools_ptimer_irq_delivery_note_set(
+            d->ptimer.pending_interrupts, d->ptimer.enabled_interrupts,
+            d->pmc.pending_interrupts, d->pmc.enabled_interrupts);
         nv2a_update_irq(d);
+        xemu_debug_tools_ptimer_trace_record(
+            d, XEMU_DEBUG_TOOLS_PTIMER_TRACE_IRQ_SET,
+            d->ptimer.pending_interrupts, previous);
+        xemu_debug_tools_scheduler_trace_note_irq(
+            XEMU_DEBUG_TOOLS_SCHED_WAKE_PTIMER,
+            d->ptimer.pending_interrupts, d->ptimer.enabled_interrupts);
+        xemu_debug_tools_ptimer_irq_latency_note_set(d);
     }
 
     schedule_qemu_timer(d);
@@ -176,10 +222,21 @@ uint64_t ptimer_read(void *opaque, hwaddr addr, unsigned int size)
     case NV_PTIMER_INTR_0: {
         uint64_t reg_now = get_reg_time(d);
         if (is_alarm_reached(reg_now, d->ptimer.alarm_time)) {
+            const uint32_t previous = d->ptimer.pending_interrupts;
             d->ptimer.pending_interrupts |= NV_PTIMER_INTR_0_ALARM;
             d->ptimer.alarm_time =
                 get_next_alarm_time(reg_now, d->ptimer.alarm_time);
+            xemu_debug_tools_ptimer_irq_delivery_note_set(
+                d->ptimer.pending_interrupts, d->ptimer.enabled_interrupts,
+                d->pmc.pending_interrupts, d->pmc.enabled_interrupts);
             nv2a_update_irq(d);
+            xemu_debug_tools_ptimer_trace_record(
+                d, XEMU_DEBUG_TOOLS_PTIMER_TRACE_IRQ_SET,
+                d->ptimer.pending_interrupts, previous);
+            xemu_debug_tools_scheduler_trace_note_irq(
+                XEMU_DEBUG_TOOLS_SCHED_WAKE_PTIMER,
+                d->ptimer.pending_interrupts, d->ptimer.enabled_interrupts);
+            xemu_debug_tools_ptimer_irq_latency_note_set(d);
             schedule_qemu_timer(d);
         }
         r = d->ptimer.pending_interrupts;
@@ -219,41 +276,80 @@ void ptimer_write(void *opaque, hwaddr addr, uint64_t val, unsigned int size)
     nv2a_reg_log_write(NV_PTIMER, addr, size, val);
 
     switch (addr) {
-    case NV_PTIMER_INTR_0:
+    case NV_PTIMER_INTR_0: {
+        const uint32_t previous = d->ptimer.pending_interrupts;
         d->ptimer.pending_interrupts &= ~val;
         nv2a_update_irq(d);
-        break;
-    case NV_PTIMER_INTR_EN_0:
+        xemu_debug_tools_ptimer_trace_record(
+            d, XEMU_DEBUG_TOOLS_PTIMER_TRACE_IRQ_CLEAR,
+            (uint32_t)val, previous);
+        xemu_debug_tools_ptimer_irq_latency_note_clear(d, (uint32_t)val);
+        xemu_debug_tools_ptimer_irq_delivery_note_clear(
+            (uint32_t)val, d->ptimer.pending_interrupts,
+            d->ptimer.enabled_interrupts, d->pmc.pending_interrupts,
+            d->pmc.enabled_interrupts);
+    } break;
+    case NV_PTIMER_INTR_EN_0: {
+        const uint32_t previous = d->ptimer.enabled_interrupts;
         d->ptimer.enabled_interrupts = val;
         if (val & NV_PTIMER_INTR_0_ALARM) {
             uint64_t reg_now = get_reg_time(d);
             if (is_alarm_reached(reg_now, d->ptimer.alarm_time)) {
+                const uint32_t previous_pending = d->ptimer.pending_interrupts;
                 d->ptimer.pending_interrupts |= NV_PTIMER_INTR_0_ALARM;
                 d->ptimer.alarm_time =
                     get_next_alarm_time(reg_now, d->ptimer.alarm_time);
+                xemu_debug_tools_ptimer_irq_delivery_note_set(
+                    d->ptimer.pending_interrupts, d->ptimer.enabled_interrupts,
+                    d->pmc.pending_interrupts, d->pmc.enabled_interrupts);
+                xemu_debug_tools_ptimer_trace_record(
+                    d, XEMU_DEBUG_TOOLS_PTIMER_TRACE_IRQ_SET,
+                    d->ptimer.pending_interrupts, previous_pending);
+                xemu_debug_tools_scheduler_trace_note_irq(
+                    XEMU_DEBUG_TOOLS_SCHED_WAKE_PTIMER,
+                    d->ptimer.pending_interrupts, d->ptimer.enabled_interrupts);
+                xemu_debug_tools_ptimer_irq_latency_note_set(d);
             }
             schedule_qemu_timer(d);
         } else {
             timer_del(&d->ptimer.timer);
+            xemu_debug_tools_ptimer_trace_record(
+                d, XEMU_DEBUG_TOOLS_PTIMER_TRACE_TIMER_DELETE, 0, 0);
         }
         nv2a_update_irq(d);
-        break;
-    case NV_PTIMER_DENOMINATOR:
+        xemu_debug_tools_ptimer_trace_record(
+            d, XEMU_DEBUG_TOOLS_PTIMER_TRACE_INTR_EN_WRITE,
+            (uint32_t)val, previous);
+    } break;
+    case NV_PTIMER_DENOMINATOR: {
+        const uint32_t previous = d->ptimer.denominator;
         d->ptimer.denominator = val;
         if (timer_pending(&d->ptimer.timer)) {
             schedule_qemu_timer(d);
         }
-        break;
-    case NV_PTIMER_NUMERATOR:
+        xemu_debug_tools_ptimer_trace_record(
+            d, XEMU_DEBUG_TOOLS_PTIMER_TRACE_DENOMINATOR_WRITE,
+            (uint32_t)val, previous);
+    } break;
+    case NV_PTIMER_NUMERATOR: {
+        const uint32_t previous = d->ptimer.numerator;
         d->ptimer.numerator = val;
         if (timer_pending(&d->ptimer.timer)) {
             schedule_qemu_timer(d);
         }
-        break;
+        xemu_debug_tools_ptimer_trace_record(
+            d, XEMU_DEBUG_TOOLS_PTIMER_TRACE_NUMERATOR_WRITE,
+            (uint32_t)val, previous);
+    } break;
     case NV_PTIMER_ALARM_0: {
+        const uint32_t previous =
+            PTIMER_REG_TIME_GET_TIME_0(d->ptimer.alarm_time);
         uint64_t reg_now = get_reg_time(d);
         d->ptimer.alarm_time = get_next_alarm_time(reg_now, val);
         schedule_qemu_timer(d);
+        xemu_debug_tools_ptimer_trace_record(
+            d, XEMU_DEBUG_TOOLS_PTIMER_TRACE_ALARM_WRITE,
+            (uint32_t)val, previous);
     } break;
     case NV_PTIMER_TIME_0: {
         uint64_t current_reg_time = get_reg_time(d);
@@ -264,6 +360,9 @@ void ptimer_write(void *opaque, hwaddr addr, uint64_t val, unsigned int size)
         if (timer_pending(&d->ptimer.timer)) {
             schedule_qemu_timer(d);
         }
+        xemu_debug_tools_ptimer_trace_record(
+            d, XEMU_DEBUG_TOOLS_PTIMER_TRACE_TIME0_WRITE,
+            (uint32_t)val, 0);
     } break;
     case NV_PTIMER_TIME_1: {
         uint64_t current_reg_time = get_reg_time(d);
@@ -274,6 +373,9 @@ void ptimer_write(void *opaque, hwaddr addr, uint64_t val, unsigned int size)
         if (timer_pending(&d->ptimer.timer)) {
             schedule_qemu_timer(d);
         }
+        xemu_debug_tools_ptimer_trace_record(
+            d, XEMU_DEBUG_TOOLS_PTIMER_TRACE_TIME1_WRITE,
+            (uint32_t)val, 0);
     } break;
     default:
         break;
diff --git a/hw/xbox/nv2a/user.c b/hw/xbox/nv2a/user.c
index 60437eb..5cebacc 100644
--- a/hw/xbox/nv2a/user.c
+++ b/hw/xbox/nv2a/user.c
@@ -20,6 +20,7 @@
  */
 
 #include "nv2a_int.h"
+#include "ui/xui/debug-tools/addons/diagnostics/pgraph-pusher-progress-trace-hook.h"
 
 /* USER - PFIFO MMIO and DMA submission area */
 uint64_t user_read(void *opaque, hwaddr addr, unsigned int size)
@@ -90,9 +91,16 @@ void user_write(void *opaque, hwaddr addr, uint64_t val, unsigned int size)
 
         if (channel_id == cur_channel_id) {
             switch (addr & 0xFFFF) {
-            case NV_USER_DMA_PUT:
+            case NV_USER_DMA_PUT: {
+                const uint32_t old_dma_put =
+                    d->pfifo.regs[NV_PFIFO_CACHE1_DMA_PUT];
+                const uint32_t dma_get =
+                    d->pfifo.regs[NV_PFIFO_CACHE1_DMA_GET];
                 d->pfifo.regs[NV_PFIFO_CACHE1_DMA_PUT] = val;
+                xemu_debug_tools_pgraph_pusher_progress_note_dma_put(
+                    channel_id, dma_get, old_dma_put, (uint32_t)val);
                 break;
+            }
             case NV_USER_DMA_GET:
                 d->pfifo.regs[NV_PFIFO_CACHE1_DMA_GET] = val;
                 break;
diff --git a/hw/xbox/xbox_pci.c b/hw/xbox/xbox_pci.c
index 2a01e13..6c2209b 100644
--- a/hw/xbox/xbox_pci.c
+++ b/hw/xbox/xbox_pci.c
@@ -43,6 +43,7 @@
 #include "hw/xbox/xbox_pci.h"
 #include "hw/irq.h"
 #include "migration/vmstate.h"
+#include "ui/xui/debug-tools/addons/diagnostics/ptimer-irq-delivery-trace.h"
 
  /*
   * xbox chipset based on nForce 420, which was based on AMD-760
@@ -99,6 +100,8 @@ static void xbox_lpc_set_irq(void *opaque, int pirq, int level)
     if (pic_irq >= XBOX_NUM_PIC_IRQS) {
         return;
     }
+    xemu_debug_tools_ptimer_irq_delivery_note_pci_route(
+        pirq, pic_irq, level);
     qemu_set_irq(lpc->pic[pic_irq], level);
 }
 
diff --git a/hw/xbox/xid-gamepad.c b/hw/xbox/xid-gamepad.c
index 43d0d0e..0c49d89 100644
--- a/hw/xbox/xid-gamepad.c
+++ b/hw/xbox/xid-gamepad.c
@@ -20,6 +20,8 @@
  */
 
 #include "xid.h"
+#include "../../ui/xui/debug-tools/addons/diagnostics/controller-xid-trace-hook.h"
+#include "../../ui/xui/debug-tools/addons/diagnostics/controller-input-poll-trace-hook.h"
 
 // #define DEBUG_XID
 #ifdef DEBUG_XID
@@ -136,8 +138,48 @@ static void usb_xid_gamepad_handle_data(USBDevice *dev, USBPacket *p)
     switch (p->pid) {
     case USB_TOKEN_IN:
         if (p->ep->nr == GAMEPAD_IN_ENDPOINT_ID) {
+            const int input_poll_trace =
+                xemu_debug_tools_controller_input_poll_trace_begin(
+                    s->device_index, p->pid, p->ep->nr, (uint32_t)p->iov.size);
+            uint64_t input_poll_stage = input_poll_trace
+                ? xemu_debug_tools_controller_input_poll_trace_stage_begin() : 0;
+            xemu_debug_tools_controller_xid_trace_xid_event(
+                XEMU_DEBUG_TOOLS_CONTROLLER_XID_XID_INTERRUPT_IN,
+                s->device_index, s, p->ep->nr, p->iov.size);
+            if (input_poll_trace) {
+                xemu_debug_tools_controller_input_poll_trace_stage_end(
+                    XEMU_DEBUG_TOOLS_INPUT_POLL_STAGE_INTERRUPT_TRACE,
+                    input_poll_stage);
+                input_poll_stage =
+                    xemu_debug_tools_controller_input_poll_trace_stage_begin();
+            }
             update_input(s);
+            if (input_poll_trace) {
+                xemu_debug_tools_controller_input_poll_trace_stage_end(
+                    XEMU_DEBUG_TOOLS_INPUT_POLL_STAGE_UPDATE_INPUT,
+                    input_poll_stage);
+                input_poll_stage =
+                    xemu_debug_tools_controller_input_poll_trace_stage_begin();
+            }
             usb_packet_copy(p, &s->in_state, s->in_state.bLength);
+            if (input_poll_trace) {
+                xemu_debug_tools_controller_input_poll_trace_stage_end(
+                    XEMU_DEBUG_TOOLS_INPUT_POLL_STAGE_USB_PACKET_COPY,
+                    input_poll_stage);
+                input_poll_stage =
+                    xemu_debug_tools_controller_input_poll_trace_stage_begin();
+            }
+            xemu_debug_tools_controller_xid_trace_xid_packet_complete(
+                s->device_index, s, p->status, p->actual_length, p->iov.size,
+                p->ep->nr, (const uint8_t *)&s->in_state,
+                s->in_state.bLength);
+            if (input_poll_trace) {
+                xemu_debug_tools_controller_input_poll_trace_stage_end(
+                    XEMU_DEBUG_TOOLS_INPUT_POLL_STAGE_PACKET_COMPLETE_TRACE,
+                    input_poll_stage);
+                xemu_debug_tools_controller_input_poll_trace_end(
+                    p->status, (uint32_t)p->actual_length);
+            }
         } else {
             assert(!"Invalid GAMEPAD_IN_ENDPOINT_ID");
         }
@@ -190,6 +232,10 @@ static void usb_xbox_gamepad_realize(USBDevice *dev, Error **errp)
     memset(&s->out_state_capabilities, 0xFF, sizeof(s->out_state_capabilities));
     s->out_state_capabilities.length = sizeof(s->out_state_capabilities);
     s->out_state_capabilities.report_id = 0;
+
+    xemu_debug_tools_controller_xid_trace_xid_event(
+        XEMU_DEBUG_TOOLS_CONTROLLER_XID_XID_REALIZE,
+        s->device_index, s, XID_DEVICESUBTYPE_GAMEPAD, 0);
 }
 
 static void usb_xbox_gamepad_s_realize(USBDevice *dev, Error **errp)
@@ -214,6 +260,10 @@ static void usb_xbox_gamepad_s_realize(USBDevice *dev, Error **errp)
     memset(&s->out_state_capabilities, 0xFF, sizeof(s->out_state_capabilities));
     s->out_state_capabilities.length = sizeof(s->out_state_capabilities);
     s->out_state_capabilities.report_id = 0;
+
+    xemu_debug_tools_controller_xid_trace_xid_event(
+        XEMU_DEBUG_TOOLS_CONTROLLER_XID_XID_REALIZE,
+        s->device_index, s, XID_DEVICESUBTYPE_GAMEPAD_S, 0);
 }
 
 static const Property xid_properties[] = {
diff --git a/hw/xbox/xid.c b/hw/xbox/xid.c
index 396d811..a621f10 100644
--- a/hw/xbox/xid.c
+++ b/hw/xbox/xid.c
@@ -20,6 +20,8 @@
  */
 
 #include "xid.h"
+#include "../../ui/xui/debug-tools/addons/diagnostics/controller-xid-trace-hook.h"
+#include "../../ui/xui/debug-tools/addons/diagnostics/controller-input-poll-trace-hook.h"
 
 /*
  * http://xbox-linux.cvs.sourceforge.net/viewvc/xbox-linux/kernel-2.6/drivers/usb/input/xpad.c
@@ -46,6 +48,11 @@ void update_output(USBXIDGamepadState *s)
     }
 
     ControllerState *state = xemu_input_get_bound(s->device_index);
+    if (state == NULL) {
+        xemu_debug_tools_controller_xid_trace_xid_event(
+            XEMU_DEBUG_TOOLS_CONTROLLER_XID_XID_BOUND_MISSING,
+            s->device_index, s, 2, 0);
+    }
     assert(state);
     state->rumble_l = s->out_state.left_actuator_strength;
     state->rumble_r = s->out_state.right_actuator_strength;
@@ -59,8 +66,30 @@ void update_input(USBXIDGamepadState *s)
         return;
     }
 
+    const int input_poll_trace =
+        xemu_debug_tools_controller_input_poll_trace_active();
+    uint64_t input_poll_stage = input_poll_trace
+        ? xemu_debug_tools_controller_input_poll_trace_stage_begin() : 0;
     ControllerState *state = xemu_input_get_bound(s->device_index);
+    if (input_poll_trace) {
+        xemu_debug_tools_controller_input_poll_trace_stage_end(
+            XEMU_DEBUG_TOOLS_INPUT_POLL_STAGE_GET_BOUND, input_poll_stage);
+    }
+    if (state == NULL) {
+        xemu_debug_tools_controller_xid_trace_xid_event(
+            XEMU_DEBUG_TOOLS_CONTROLLER_XID_XID_BOUND_MISSING,
+            s->device_index, s, 1, 0);
+    }
     assert(state);
+    if (input_poll_trace) {
+        const uint32_t host_connected =
+            (state->type != INPUT_DEVICE_SDL_GAMEPAD ||
+             state->sdl_gamepad != NULL) ? 1u : 0u;
+        xemu_debug_tools_controller_input_poll_trace_set_controller(
+            state->sdl_joystick_id, state->type, host_connected);
+        input_poll_stage =
+            xemu_debug_tools_controller_input_poll_trace_stage_begin();
+    }
     xemu_input_update_controller(state);
 
     const int button_map_analog[6][2] = {
@@ -101,11 +130,30 @@ void update_input(USBXIDGamepadState *s)
     s->in_state.sThumbLY = state->axis[CONTROLLER_AXIS_LSTICK_Y];
     s->in_state.sThumbRX = state->axis[CONTROLLER_AXIS_RSTICK_X];
     s->in_state.sThumbRY = state->axis[CONTROLLER_AXIS_RSTICK_Y];
+
+    if (input_poll_trace) {
+        xemu_debug_tools_controller_input_poll_trace_stage_end(
+            XEMU_DEBUG_TOOLS_INPUT_POLL_STAGE_INPUT_MAPPING, input_poll_stage);
+        input_poll_stage =
+            xemu_debug_tools_controller_input_poll_trace_stage_begin();
+    }
+    xemu_debug_tools_controller_xid_trace_xid_state(
+        s->device_index, s, state->buttons, state->axis, s->in_state.wButtons,
+        s->in_state.bAnalogButtons, s->in_state.sThumbLX, s->in_state.sThumbLY,
+        s->in_state.sThumbRX, s->in_state.sThumbRY);
+    if (input_poll_trace) {
+        xemu_debug_tools_controller_input_poll_trace_stage_end(
+            XEMU_DEBUG_TOOLS_INPUT_POLL_STAGE_XID_STATE_TRACE,
+            input_poll_stage);
+    }
 }
 
 void usb_xid_handle_reset(USBDevice *dev)
 {
+    USBXIDGamepadState *s = (USBXIDGamepadState *)dev;
     DPRINTF("xid reset\n");
+    xemu_debug_tools_controller_xid_trace_xid_event(
+        XEMU_DEBUG_TOOLS_CONTROLLER_XID_XID_RESET, s->device_index, s, 0, 0);
 }
 
 void usb_xid_handle_control(USBDevice *dev, USBPacket *p,
@@ -114,6 +162,9 @@ void usb_xid_handle_control(USBDevice *dev, USBPacket *p,
     USBXIDGamepadState *s = (USBXIDGamepadState *)dev;
 
     DPRINTF("xid handle_control 0x%x 0x%x\n", request, value);
+    xemu_debug_tools_controller_xid_trace_usb_event(
+        XEMU_DEBUG_TOOLS_CONTROLLER_XID_USB_CONTROL_REQUEST,
+        s->device_index, s, request, value, index, length);
 
     int ret = usb_desc_handle_control(dev, p, request, value, index, length, data);
     if (ret >= 0) {
@@ -125,6 +176,9 @@ void usb_xid_handle_control(USBDevice *dev, USBPacket *p,
     /* HID requests */
     case ClassInterfaceRequest | HID_GET_REPORT:
         DPRINTF("xid GET_REPORT 0x%x\n", value);
+        xemu_debug_tools_controller_xid_trace_xid_event(
+            XEMU_DEBUG_TOOLS_CONTROLLER_XID_XID_GET_REPORT,
+            s->device_index, s, value, length);
         update_input(s);
         if (value == 0x0100) { /* input */
             if (length <= s->in_state.bLength) {
@@ -225,4 +279,8 @@ static void usb_xid_handle_destroy(USBDevice *dev)
 
 void usb_xbox_gamepad_unrealize(USBDevice *dev)
 {
+    USBXIDGamepadState *s = (USBXIDGamepadState *)dev;
+    xemu_debug_tools_controller_xid_trace_xid_event(
+        XEMU_DEBUG_TOOLS_CONTROLLER_XID_XID_UNREALIZE,
+        s->device_index, s, 0, 0);
 }
diff --git a/include/qemu/main-loop.h b/include/qemu/main-loop.h
index 80ab109..2729bd2 100644
--- a/include/qemu/main-loop.h
+++ b/include/qemu/main-loop.h
@@ -451,8 +451,10 @@ void main_loop_poll_remove_notifier(Notifier *notify);
 
 #ifdef XBOX
 void qemu_init_main_loop_lock(void);
-void qemu_mutex_lock_main_loop(void);
+void qemu_mutex_lock_main_loop_impl(const char *file, int line);
 void qemu_mutex_unlock_main_loop(void);
+#define qemu_mutex_lock_main_loop() \
+    qemu_mutex_lock_main_loop_impl(__FILE__, __LINE__)
 #endif
 
 #endif
diff --git a/include/qemu/timer.h b/include/qemu/timer.h
index 83fba24..4dc064b 100644
--- a/include/qemu/timer.h
+++ b/include/qemu/timer.h
@@ -89,6 +89,11 @@ struct QEMUTimer {
     QEMUTimer *next;
     int attributes;
     int scale;
+
+    /* Info: Diagnostics-only callback source metadata. */
+    const char *xemu_debug_cb_name;
+    const char *xemu_debug_cb_file;
+    int xemu_debug_cb_line;
 };
 
 extern QEMUTimerListGroup main_loop_tlg;
@@ -602,6 +607,8 @@ static inline QEMUTimer *timer_new_ms(QEMUClockType type, QEMUTimerCB *cb,
     return timer_new(type, SCALE_MS, cb, opaque);
 }
 
+#include "../../ui/xui/debug-tools/addons/diagnostics/qemu-timer-source-hook.h"
+
 /**
  * timer_deinit:
  * @ts: the timer to be de-initialised
diff --git a/system/cpus.c b/system/cpus.c
index ef2d2f2..d1ebbeb 100644
--- a/system/cpus.c
+++ b/system/cpus.c
@@ -47,6 +47,8 @@
 #include "hw/boards.h"
 #include "hw/hw.h"
 #include "trace.h"
+#include "../ui/xui/debug-tools/addons/diagnostics/bql-trace-hook.h"
+#include "../ui/xui/debug-tools/addons/memory-tools/exact-execution-trace.h"
 
 #ifdef CONFIG_LINUX
 
@@ -341,6 +343,14 @@ bool cpu_can_run(CPUState *cpu)
 
 void cpu_handle_guest_debug(CPUState *cpu)
 {
+    /* Memory Tools exact tracing owns continuous single-step debug exits on
+     * generic accelerators. User breakpoints/watchpoints are deliberately not
+     * consumed by the addon and continue through the normal QEMU debugger
+     * stop path below. */
+    if (xemu_memory_tools_exact_trace_handle_guest_debug(cpu)) {
+        return;
+    }
+
     if (replay_running_debug()) {
         if (!cpu->singlestep_enabled) {
             /*
@@ -575,14 +585,18 @@ void bql_lock_impl(const char *file, int line)
     QemuMutexLockFunc bql_lock_fn = qatomic_read(&bql_mutex_lock_func);
 
     g_assert(!bql_locked());
+    xemu_debug_tools_bql_trace_lock_attempt(file, line);
     bql_lock_fn(&bql, file, line);
+    xemu_debug_tools_bql_trace_lock_acquired(file, line);
 }
 
 void bql_unlock(void)
 {
     g_assert(bql_locked());
     g_assert(!bql_unlock_blocked);
+    xemu_debug_tools_bql_trace_unlock_begin();
     qemu_mutex_unlock(&bql);
+    xemu_debug_tools_bql_trace_unlock_end();
 }
 
 void qemu_cond_wait_bql(QemuCond *cond)
diff --git a/system/vl.c b/system/vl.c
index f45bdc3..f757204 100644
--- a/system/vl.c
+++ b/system/vl.c
@@ -38,6 +38,7 @@
 #include "qobject/qjson.h"
 #include "qemu-version.h"
 #include "qemu/cutils.h"
+#include "ui/xui/debug-tools/addons/Folder-HDD/folder-hdd.h"
 #include "qemu/help_option.h"
 #include "qemu/hw-version.h"
 #include "qemu/uuid.h"
@@ -3057,19 +3058,46 @@ void qemu_init(int argc, char **argv)
     fake_argv[fake_argc++] = strdup("-m");
     fake_argv[fake_argc++] = g_strdup_printf("%d", mem);
 
-    const char *hdd_path = g_config.sys.files.hdd_path;
-    if (strlen(hdd_path) > 0) {
-        if (xemu_check_file(hdd_path)) {
-            char *msg = g_strdup_printf("Failed to open hard disk image file '%s'. Please check machine settings.", hdd_path);
-            xemu_queue_error_message(msg);
-            g_free(msg);
+    if ((int)g_config.sys.files.hdd_type == CONFIG_SYS_FILES_HDD_TYPE_FOLDER) {
+        const char *folder_root = g_config.sys.files.hdd_folder_path;
+        char *folder_error = NULL;
+        if (!cmp_folder_hdd_root_is_usable(folder_root, &folder_error)) {
+            const char *message = folder_error ? folder_error :
+                "Folder-HDD root is not usable. Please check machine settings.";
+            if (folder_root && folder_root[0] &&
+                !g_file_test(folder_root, G_FILE_TEST_EXISTS)) {
+                xemu_queue_error_message_with_action(
+                    message, "Make Folders", cmp_folder_hdd_make_folders_action,
+                    (void *)folder_root);
+            } else {
+                xemu_queue_error_message(message);
+            }
+            g_free(folder_error);
+            autostart = 0;
         } else {
+            char *folder_uri = cmp_folder_hdd_make_uri(folder_root);
+            char *escaped_folder_uri = strdup_double_commas(folder_uri);
             fake_argv[fake_argc++] = strdup("-drive");
-            char *escaped_hdd_path = strdup_double_commas(hdd_path);
-            fake_argv[fake_argc++] = g_strdup_printf("index=0,media=disk,file=%s%s",
-                escaped_hdd_path,
-                strlen(escaped_hdd_path) > 0 ? ",locked=on" : "");
-            free(escaped_hdd_path);
+            fake_argv[fake_argc++] = g_strdup_printf(
+                "index=0,media=disk,file=%s,locked=on", escaped_folder_uri);
+            free(escaped_folder_uri);
+            g_free(folder_uri);
+        }
+    } else {
+        const char *hdd_path = g_config.sys.files.hdd_path;
+        if (strlen(hdd_path) > 0) {
+            if (xemu_check_file(hdd_path)) {
+                char *msg = g_strdup_printf("Failed to open hard disk image file '%s'. Please check machine settings.", hdd_path);
+                xemu_queue_error_message(msg);
+                g_free(msg);
+            } else {
+                fake_argv[fake_argc++] = strdup("-drive");
+                char *escaped_hdd_path = strdup_double_commas(hdd_path);
+                fake_argv[fake_argc++] = g_strdup_printf("index=0,media=disk,file=%s%s",
+                    escaped_hdd_path,
+                    strlen(escaped_hdd_path) > 0 ? ",locked=on" : "");
+                free(escaped_hdd_path);
+            }
         }
     }
 
diff --git a/target/i386/tcg/system/seg_helper.c b/target/i386/tcg/system/seg_helper.c
index 8c7856b..3fc05b8 100644
--- a/target/i386/tcg/system/seg_helper.c
+++ b/target/i386/tcg/system/seg_helper.c
@@ -26,6 +26,7 @@
 #include "accel/tcg/cpu-ldst.h"
 #include "tcg/helper-tcg.h"
 #include "../seg_helper.h"
+#include "ui/xui/debug-tools/addons/diagnostics/ptimer-irq-delivery-trace.h"
 
 void helper_syscall(CPUX86State *env, int next_eip_addend)
 {
@@ -204,6 +205,8 @@ bool x86_cpu_exec_interrupt(CPUState *cs, int interrupt_request)
         cpu_svm_check_intercept_param(env, SVM_EXIT_INTR, 0, 0);
         cpu_reset_interrupt(cs, CPU_INTERRUPT_HARD | CPU_INTERRUPT_VIRQ);
         intno = cpu_get_pic_interrupt(env);
+        xemu_debug_tools_ptimer_irq_delivery_note_tcg_take(
+            cs, CPU_INTERRUPT_HARD, intno);
         qemu_log_mask(CPU_LOG_INT,
                       "Servicing hardware INT=0x%02x\n", intno);
         do_interrupt_x86_hardirq(env, intno, 1);
diff --git a/target/i386/whpx/whpx-accel-ops.c b/target/i386/whpx/whpx-accel-ops.c
index f758861..794e72e 100644
--- a/target/i386/whpx/whpx-accel-ops.c
+++ b/target/i386/whpx/whpx-accel-ops.c
@@ -14,10 +14,10 @@
 #include "accel/accel-cpu-ops.h"
 #include "system/cpus.h"
 #include "qemu/guest-random.h"
-
 #include "system/whpx.h"
 #include "whpx-internal.h"
 #include "whpx-accel-ops.h"
+#include "../../../ui/xui/debug-tools/backend/whpx-debug.h"
 
 static void *whpx_cpu_thread_fn(void *arg)
 {
@@ -94,6 +94,9 @@ static void whpx_accel_ops_class_init(ObjectClass *oc, const void *data)
     ops->synchronize_post_init = whpx_cpu_synchronize_post_init;
     ops->synchronize_state = whpx_cpu_synchronize_state;
     ops->synchronize_pre_loadvm = whpx_cpu_synchronize_pre_loadvm;
+
+    /* Info: Register WHPX guest-debug hooks. */
+    xemu_debug_register_whpx_ops(ops);
 }
 
 static const TypeInfo whpx_accel_ops_type = {
diff --git a/target/i386/whpx/whpx-all.c b/target/i386/whpx/whpx-all.c
index 6bf8d5f..573d91b 100644
--- a/target/i386/whpx/whpx-all.c
+++ b/target/i386/whpx/whpx-all.c
@@ -32,6 +32,7 @@
 #include <winerror.h>
 
 #include "whpx-internal.h"
+#include "../../../ui/xui/debug-tools/backend/whpx-debug.h"
 #include "whpx-accel-ops.h"
 
 #include <winhvplatform.h>
@@ -1358,7 +1359,7 @@ static int whpx_first_vcpu_starting(CPUState *cpu)
     }
 
     uint64_t exception_mask;
-    if (whpx->step_pending ||
+    if (whpx->step_pending || xemu_whpx_watchpoints_active(cpu) ||
         (whpx->breakpoints.breakpoints &&
          whpx->breakpoints.breakpoints->used)) {
         /*
@@ -1724,7 +1725,9 @@ static int whpx_vcpu_run(CPUState *cpu)
             }
         }
 
-        if (exclusive_step_mode != WHPX_STEP_NONE || cpu->singlestep_enabled) {
+        if (exclusive_step_mode != WHPX_STEP_NONE || cpu->singlestep_enabled ||
+            xemu_whpx_user_step_pending()) {
+            /* Info: Keep explicit Step Into independent from WHPX breakpoint step-over. */
             whpx_vcpu_configure_single_stepping(cpu, true, NULL);
         }
 
@@ -1739,7 +1742,8 @@ static int whpx_vcpu_run(CPUState *cpu)
             break;
         }
 
-        if (exclusive_step_mode != WHPX_STEP_NONE || cpu->singlestep_enabled) {
+        if (exclusive_step_mode != WHPX_STEP_NONE || cpu->singlestep_enabled ||
+            xemu_whpx_user_step_pending()) {
             whpx_vcpu_configure_single_stepping(cpu,
                 false,
                 &vcpu->exit_ctx.VpContext.Rflags);
@@ -1989,22 +1993,15 @@ static int whpx_vcpu_run(CPUState *cpu)
         case WHvRunVpExitReasonException:
             whpx_get_registers(cpu);
 
-            if ((vcpu->exit_ctx.VpException.ExceptionType ==
-                 WHvX64ExceptionTypeDebugTrapOrFault) &&
-                (vcpu->exit_ctx.VpException.InstructionByteCount >= 1) &&
-                (vcpu->exit_ctx.VpException.InstructionBytes[0] ==
-                 whpx_breakpoint_instruction)) {
-                /* Stopped at a software breakpoint. */
-                cpu->exception_index = EXCP_DEBUG;
-            } else if ((vcpu->exit_ctx.VpException.ExceptionType ==
-                        WHvX64ExceptionTypeDebugTrapOrFault) &&
-                       !cpu->singlestep_enabled) {
-                /*
-                 * Just finished stepping over a breakpoint, but the
-                 * gdb does not expect us to do single-stepping.
-                 * Don't do anything special.
-                 */
-                cpu->exception_index = EXCP_INTERRUPT;
+            if (vcpu->exit_ctx.VpException.ExceptionType ==
+                WHvX64ExceptionTypeDebugTrapOrFault) {
+                int is_execute_breakpoint =
+                    vcpu->exit_ctx.VpException.InstructionByteCount >= 1 &&
+                    vcpu->exit_ctx.VpException.InstructionBytes[0] ==
+                        whpx_breakpoint_instruction;
+
+                cpu->exception_index = xemu_whpx_debug_exception_index(
+                    cpu, is_execute_breakpoint);
             } else {
                 /* Another exception or debug event. Report it to GDB. */
                 cpu->exception_index = EXCP_DEBUG;
@@ -2114,6 +2111,9 @@ void whpx_cpu_synchronize_pre_loadvm(CPUState *cpu)
 static void whpx_pre_resume_vm(AccelState *as, bool step_pending)
 {
     whpx_global.step_pending = step_pending;
+
+    /* Info: Mirror explicit user-step state into the WHPX Debug Tools bridge. */
+    xemu_whpx_set_user_step_pending(step_pending);
 }
 
 /*
diff --git a/tests/xbox/ptimer/meson.build b/tests/xbox/ptimer/meson.build
index 94af2c7..d6bae4c 100644
--- a/tests/xbox/ptimer/meson.build
+++ b/tests/xbox/ptimer/meson.build
@@ -3,7 +3,12 @@ test_nv2a_ptimer = executable(
   files(
     'test-nv2a-ptimer.c',
     'mock-nv2a-ptimer.c',
-    meson.project_source_root() / 'hw/xbox/nv2a/ptimer.c'
+    meson.project_source_root() / 'hw/xbox/nv2a/ptimer.c',
+    meson.project_source_root() / 'ui/xui/debug-tools/addons/stubs/ptimer-trace-stub.c',
+    meson.project_source_root() / 'ui/xui/debug-tools/addons/stubs/qemu-timer-trace-stub.c',
+    meson.project_source_root() / 'ui/xui/debug-tools/addons/stubs/ptimer-irq-delivery-trace-stub.c',
+    meson.project_source_root() / 'ui/xui/debug-tools/addons/stubs/scheduler-trace-stub.c',
+    meson.project_source_root() / 'ui/xui/debug-tools/addons/stubs/pgraph-ptimer-latency-trace-stub.c'
   ),
   c_args: [
     '-DCOMPILING_PER_TARGET',
diff --git a/ui/xemu-input.c b/ui/xemu-input.c
index 4fbc863..cdb353e 100644
--- a/ui/xemu-input.c
+++ b/ui/xemu-input.c
@@ -31,9 +31,81 @@
 #include "xemu-input.h"
 #include "xemu-notifications.h"
 #include "xemu-settings.h"
+#include "xui/debug-tools/addons/diagnostics/controller-xid-trace-hook.h"
+#include "xui/debug-tools/addons/diagnostics/controller-input-poll-trace-hook.h"
 #include <stdio.h>
 #include <stdlib.h>
 
+static uint32_t xemu_debug_tools_input_button_getter(SDL_GamepadButton button)
+{
+    switch (button) {
+    case SDL_GAMEPAD_BUTTON_SOUTH: return XEMU_DEBUG_TOOLS_INPUT_GETTER_BUTTON_A;
+    case SDL_GAMEPAD_BUTTON_EAST: return XEMU_DEBUG_TOOLS_INPUT_GETTER_BUTTON_B;
+    case SDL_GAMEPAD_BUTTON_WEST: return XEMU_DEBUG_TOOLS_INPUT_GETTER_BUTTON_X;
+    case SDL_GAMEPAD_BUTTON_NORTH: return XEMU_DEBUG_TOOLS_INPUT_GETTER_BUTTON_Y;
+    case SDL_GAMEPAD_BUTTON_DPAD_LEFT: return XEMU_DEBUG_TOOLS_INPUT_GETTER_DPAD_LEFT;
+    case SDL_GAMEPAD_BUTTON_DPAD_UP: return XEMU_DEBUG_TOOLS_INPUT_GETTER_DPAD_UP;
+    case SDL_GAMEPAD_BUTTON_DPAD_RIGHT: return XEMU_DEBUG_TOOLS_INPUT_GETTER_DPAD_RIGHT;
+    case SDL_GAMEPAD_BUTTON_DPAD_DOWN: return XEMU_DEBUG_TOOLS_INPUT_GETTER_DPAD_DOWN;
+    case SDL_GAMEPAD_BUTTON_BACK: return XEMU_DEBUG_TOOLS_INPUT_GETTER_BACK;
+    case SDL_GAMEPAD_BUTTON_START: return XEMU_DEBUG_TOOLS_INPUT_GETTER_START;
+    case SDL_GAMEPAD_BUTTON_LEFT_SHOULDER: return XEMU_DEBUG_TOOLS_INPUT_GETTER_LEFT_SHOULDER;
+    case SDL_GAMEPAD_BUTTON_RIGHT_SHOULDER: return XEMU_DEBUG_TOOLS_INPUT_GETTER_RIGHT_SHOULDER;
+    case SDL_GAMEPAD_BUTTON_LEFT_STICK: return XEMU_DEBUG_TOOLS_INPUT_GETTER_LEFT_STICK_BUTTON;
+    case SDL_GAMEPAD_BUTTON_RIGHT_STICK: return XEMU_DEBUG_TOOLS_INPUT_GETTER_RIGHT_STICK_BUTTON;
+    case SDL_GAMEPAD_BUTTON_GUIDE: return XEMU_DEBUG_TOOLS_INPUT_GETTER_GUIDE;
+    default: return XEMU_DEBUG_TOOLS_INPUT_GETTER_NONE;
+    }
+}
+
+static uint32_t xemu_debug_tools_input_axis_getter(SDL_GamepadAxis axis)
+{
+    switch (axis) {
+    case SDL_GAMEPAD_AXIS_LEFT_TRIGGER: return XEMU_DEBUG_TOOLS_INPUT_GETTER_AXIS_TRIGGER_LEFT;
+    case SDL_GAMEPAD_AXIS_RIGHT_TRIGGER: return XEMU_DEBUG_TOOLS_INPUT_GETTER_AXIS_TRIGGER_RIGHT;
+    case SDL_GAMEPAD_AXIS_LEFTX: return XEMU_DEBUG_TOOLS_INPUT_GETTER_AXIS_LEFT_X;
+    case SDL_GAMEPAD_AXIS_LEFTY: return XEMU_DEBUG_TOOLS_INPUT_GETTER_AXIS_LEFT_Y;
+    case SDL_GAMEPAD_AXIS_RIGHTX: return XEMU_DEBUG_TOOLS_INPUT_GETTER_AXIS_RIGHT_X;
+    case SDL_GAMEPAD_AXIS_RIGHTY: return XEMU_DEBUG_TOOLS_INPUT_GETTER_AXIS_RIGHT_Y;
+    default: return XEMU_DEBUG_TOOLS_INPUT_GETTER_NONE;
+    }
+}
+
+static bool xemu_debug_tools_profiled_gamepad_button(SDL_Gamepad *gamepad,
+                                                       SDL_GamepadButton button)
+{
+    const bool trace =
+        xemu_debug_tools_controller_input_poll_trace_active() != 0;
+    const uint64_t start = trace
+        ? xemu_debug_tools_controller_input_poll_trace_stage_begin() : 0;
+    const bool value = SDL_GetGamepadButton(gamepad, button);
+    if (trace) {
+        xemu_debug_tools_controller_input_poll_trace_getter_end(
+            xemu_debug_tools_input_button_getter(button), start);
+    }
+    return value;
+}
+
+static Sint16 xemu_debug_tools_profiled_gamepad_axis(SDL_Gamepad *gamepad,
+                                                      SDL_GamepadAxis axis)
+{
+    const bool trace =
+        xemu_debug_tools_controller_input_poll_trace_active() != 0;
+    const uint64_t start = trace
+        ? xemu_debug_tools_controller_input_poll_trace_stage_begin() : 0;
+    const Sint16 value = SDL_GetGamepadAxis(gamepad, axis);
+    if (trace) {
+        xemu_debug_tools_controller_input_poll_trace_getter_end(
+            xemu_debug_tools_input_axis_getter(axis), start);
+    }
+    return value;
+}
+
+#define SDL_GetGamepadButton(gamepad, button) \
+    xemu_debug_tools_profiled_gamepad_button((gamepad), (button))
+#define SDL_GetGamepadAxis(gamepad, axis) \
+    xemu_debug_tools_profiled_gamepad_axis((gamepad), (axis))
+
 #include "system/blockdev.h"
 
 // #define DEBUG_INPUT
@@ -359,6 +431,15 @@ void xemu_save_peripheral_settings(int player_index, int peripheral_index,
 
 void xemu_input_process_sdl_events(const SDL_Event *event)
 {
+    if (event->type == SDL_EVENT_GAMEPAD_ADDED) {
+        xemu_debug_tools_controller_xid_trace_host_lifecycle(
+            XEMU_DEBUG_TOOLS_CONTROLLER_XID_HOST_GAMEPAD_ADDED, -1,
+            event->gdevice.which, 0, NULL, NULL);
+    } else if (event->type == SDL_EVENT_GAMEPAD_REMOVED) {
+        xemu_debug_tools_controller_xid_trace_host_lifecycle(
+            XEMU_DEBUG_TOOLS_CONTROLLER_XID_HOST_GAMEPAD_REMOVED, -1,
+            event->gdevice.which, 0, NULL, NULL);
+    }
     if (event->type == SDL_EVENT_GAMEPAD_ADDED) {
         DPRINTF("Controller Added: %d\n", event->gdevice.which);
 
@@ -366,6 +447,9 @@ void xemu_input_process_sdl_events(const SDL_Event *event)
         SDL_Gamepad *sdl_con;
         sdl_con = SDL_OpenGamepad(event->gdevice.which);
         if (sdl_con == NULL) {
+            xemu_debug_tools_controller_xid_trace_host_lifecycle(
+                XEMU_DEBUG_TOOLS_CONTROLLER_XID_HOST_GAMEPAD_OPEN_FAILED, -1,
+                event->gdevice.which, 0, NULL, NULL);
             DPRINTF("Could not open joystick %d as a Gamepad\n", event->gdevice.which);
             return;
         }
@@ -388,6 +472,9 @@ void xemu_input_process_sdl_events(const SDL_Event *event)
         char guid_buf[35] = { 0 };
         SDL_GUIDToString(new_con->sdl_joystick_guid, guid_buf, sizeof(guid_buf));
         DPRINTF("Opened %s (%s)\n", new_con->name, guid_buf);
+        xemu_debug_tools_controller_xid_trace_host_lifecycle(
+            XEMU_DEBUG_TOOLS_CONTROLLER_XID_HOST_GAMEPAD_OPENED, -1,
+            new_con->sdl_joystick_id, 0, new_con->name, guid_buf);
 
         QTAILQ_INSERT_TAIL(&available_controllers, new_con, entry);
         xemu_input_bindings_reload_map(new_con);
@@ -648,6 +735,9 @@ void xemu_input_update_sdl_controller_state(ControllerState *state)
 
 #undef INVERT_AXIS
 
+    xemu_debug_tools_controller_xid_trace_host_state(
+        state->bound, state->sdl_joystick_id, state->buttons, state->axis);
+
     // xemu_input_print_controller_state(state);
 }
 
@@ -683,7 +773,18 @@ void xemu_input_bind(int index, ControllerState *state, int save)
 
     // Unbind existing controller
     if (bound_controllers[index]) {
-        assert(bound_controllers[index]->device != NULL);
+        char trace_guid[35] = { 0 };
+        ControllerState *old_state = bound_controllers[index];
+        if (old_state->type == INPUT_DEVICE_SDL_GAMEPAD) {
+            SDL_GUIDToString(old_state->sdl_joystick_guid, trace_guid,
+                             sizeof(trace_guid));
+        }
+        assert(old_state->device != NULL);
+        xemu_debug_tools_controller_xid_trace_host_lifecycle(
+            XEMU_DEBUG_TOOLS_CONTROLLER_XID_HOST_UNBIND, index,
+            old_state->type == INPUT_DEVICE_SDL_GAMEPAD
+                ? old_state->sdl_joystick_id : 0,
+            save, old_state->name, trace_guid);
         Error *err = NULL;
 
         // Unbind any XMUs
@@ -776,6 +877,17 @@ void xemu_input_bind(int index, ControllerState *state, int save)
         object_unref(OBJECT(dev));
 
         state->device = usbhub_dev;
+
+        char trace_guid[35] = { 0 };
+        if (state->type == INPUT_DEVICE_SDL_GAMEPAD) {
+            SDL_GUIDToString(state->sdl_joystick_guid, trace_guid,
+                             sizeof(trace_guid));
+        }
+        xemu_debug_tools_controller_xid_trace_host_lifecycle(
+            XEMU_DEBUG_TOOLS_CONTROLLER_XID_HOST_BIND, index,
+            state->type == INPUT_DEVICE_SDL_GAMEPAD
+                ? state->sdl_joystick_id : 0,
+            save, state->name, trace_guid);
     }
 }
 
diff --git a/ui/xemu-notifications.h b/ui/xemu-notifications.h
index f023eb3..80c45e1 100644
--- a/ui/xemu-notifications.h
+++ b/ui/xemu-notifications.h
@@ -27,9 +27,15 @@
 extern "C" {
 #endif
 
+typedef void (*XemuNotificationAction)(void *opaque);
+
 // Simple API to show a message on the screen when some event happens
 void xemu_queue_notification(const char *msg);
 void xemu_queue_error_message(const char *msg);
+void xemu_queue_error_message_with_action(const char *msg,
+                                          const char *action_label,
+                                          XemuNotificationAction action,
+                                          void *opaque);
 
 #ifdef __cplusplus
 }
diff --git a/ui/xemu.c b/ui/xemu.c
index 18dc3b3..ba20fbb 100644
--- a/ui/xemu.c
+++ b/ui/xemu.c
@@ -43,11 +43,14 @@
 #include "system/runstate-action.h"
 #include "system/system.h"
 #include "xui/xemu-hud.h"
+#include "xui/debug-tools/addons/diagnostics/bql-trace-hook.h"
 #include "xemu-input.h"
 #include "xemu-settings.h"
 #include "xemu-snapshots.h"
 #include "xemu-version.h"
 #include "xemu-os-utils.h"
+#include "xui/debug-tools/addons/Video-Recorder/video-recorder-c.h"
+#include "xui/debug-tools/addons/diagnostics/av-sync-trace-hook.h"
 
 #include "data/xemu_64x64.png.h"
 
@@ -360,11 +363,19 @@ static void process_key(struct xemu_console *scon, SDL_KeyboardEvent *ev)
     qkbd_state_key_event(scon->kbd, qcode, ev->type == SDL_EVENT_KEY_DOWN);
 }
 
+static bool video_record_hotkey_held;
+
 static void handle_keydown(SDL_Event *ev)
 {
     int win;
     struct xemu_console *scon = get_scon_from_window(ev->key.windowID);
     if (scon == NULL) return;
+    if (!scon->ignore_hotkeys && !ev->key.repeat &&
+        ev->key.scancode == SDL_SCANCODE_F9) {
+        video_record_hotkey_held = true;
+        xemu_video_recorder_toggle();
+        return;
+    }
     int gui_key_modifier_pressed = get_mod_state();
     int gui_keysym = 0;
 
@@ -425,6 +436,11 @@ static void handle_keyup(SDL_Event *ev)
     struct xemu_console *scon = get_scon_from_window(ev->key.windowID);
     if (!scon) return;
 
+    if (ev->key.scancode == SDL_SCANCODE_F9 && video_record_hotkey_held) {
+        video_record_hotkey_held = false;
+        return;
+    }
+
     scon->ignore_hotkeys = false;
     process_key(scon, &ev->key);
 }
@@ -751,6 +767,7 @@ static void process_vblank(struct xemu_console *scon)
 {
     assert(bql_locked());
 
+    xemu_debug_tools_av_sync_trace_note_vblank(vblank_interval_ns);
     update_fps();
 
 #if 0
@@ -792,6 +809,8 @@ static void *vblank_timer_thread(void *opaque)
             next_vblank = now;
         }
 
+        xemu_debug_tools_bql_trace_set_next_context(
+            XEMU_DEBUG_TOOLS_BQL_CONTEXT_UI_VBLANK);
         if (!qatomic_read(&qemu_exiting)) {
             xemu_main_loop_lock();
             process_vblank(scon);
@@ -826,7 +845,7 @@ static void report_stats(void)
  * Renders the main interface. Usually called from the main thread,
  * but may sometimes be called from another thread.
  */
-static void gl_render_frame(struct xemu_console *scon)
+static void gl_render_frame_common(struct xemu_console *scon, bool playback_only)
 {
     static bool rendering;
     if (qatomic_xchg(&rendering, true) || qatomic_read(&qemu_exiting)) {
@@ -853,6 +872,8 @@ static void gl_render_frame(struct xemu_console *scon)
     assert(glGetError() == GL_NO_ERROR);
 
     if (tex == 0) {
+        xemu_debug_tools_bql_trace_set_next_context(
+            XEMU_DEBUG_TOOLS_BQL_CONTEXT_UI_FRAMEBUFFER_CREATE);
         xemu_main_loop_lock();
         // FIXME: Don't upload if notdirty
         xb_surface_gl_create_texture(scon->surface);
@@ -878,14 +899,27 @@ static void gl_render_frame(struct xemu_console *scon)
      * lock and perform rendering, but release before swap to avoid
      * possible lengthy blocking (for vsync).
      */
+    xemu_debug_tools_bql_trace_set_next_context(
+        XEMU_DEBUG_TOOLS_BQL_CONTEXT_UI_HUD_UPDATE);
     xemu_main_loop_lock();
-    xemu_hud_update();
+    if (playback_only) {
+        xemu_hud_render_playback_only();
+    } else {
+        xemu_hud_update();
+    }
     xemu_main_loop_unlock();
 
-    xemu_hud_render();
+    if (!playback_only) {
+        /* All detached Debug Tools ImGui frames are built only after the
+         * emulator-global main-loop/BQL critical section has ended. */
+        xemu_hud_build_detached_frames_unlocked();
+        xemu_hud_render();
+    }
     glFinish();
 
     if (release_surface_texture) {
+        xemu_debug_tools_bql_trace_set_next_context(
+            XEMU_DEBUG_TOOLS_BQL_CONTEXT_UI_FRAMEBUFFER_DESTROY);
         xemu_main_loop_lock();
         xb_surface_gl_destroy_texture(scon->surface);
         xemu_main_loop_unlock();
@@ -911,6 +945,10 @@ static void gl_render_frame(struct xemu_console *scon)
 #endif
     assert(glGetError() == GL_NO_ERROR);
 
+    if (!playback_only) {
+        xemu_hud_process_deferred_actions();
+    }
+
     qatomic_set(&rendering, false);
 
 #if DEBUG_XEMU_C
@@ -918,13 +956,28 @@ static void gl_render_frame(struct xemu_console *scon)
 #endif
 }
 
+static void gl_render_frame(struct xemu_console *scon)
+{
+    gl_render_frame_common(scon, false);
+}
+
+static void gl_render_playback_frame(struct xemu_console *scon)
+{
+    gl_render_frame_common(scon, true);
+}
+
 static bool event_watch_callback(void *userdata, SDL_Event *event)
 {
     struct xemu_console *scon = (struct xemu_console *)userdata;
 
-    if (event->type == SDL_EVENT_WINDOW_RESIZED) {
+    /* Info: Console expose/resize events keep the full redraw path. */
+    if ((event->type == SDL_EVENT_WINDOW_EXPOSED ||
+         event->type == SDL_EVENT_WINDOW_RESIZED) &&
+        scon->real_window &&
+        event->window.windowID == SDL_GetWindowID(scon->real_window)) {
 #ifdef _WIN32
-        if (win32_dxgi_present_is_active()) {
+        if (event->type == SDL_EVENT_WINDOW_RESIZED &&
+            win32_dxgi_present_is_active()) {
             int width;
             int height;
             if (!SDL_GetWindowSizeInPixels(scon->real_window, &width,
@@ -937,10 +990,19 @@ static bool event_watch_callback(void *userdata, SDL_Event *event)
         }
 #endif
         gl_render_frame(scon);
-    } else if (event->type == SDL_EVENT_WINDOW_EXPOSED) {
-        gl_render_frame(scon);
+        return true; // Ignored
     }
 
+#if defined(_WIN32)
+    /* Info: Modal Win32 move/resize callbacks present playback only; Debug Tools are not re-entered. */
+    if ((event->type == SDL_EVENT_WINDOW_MOVED ||
+         event->type == SDL_EVENT_WINDOW_RESIZED ||
+         event->type == SDL_EVENT_WINDOW_EXPOSED) &&
+        xemu_hud_is_detached_window_id(event->window.windowID)) {
+        gl_render_playback_frame(scon);
+    }
+#endif
+
     return true; // Ignored
 }
 
@@ -953,6 +1015,8 @@ static void poll_events(struct xemu_console *scon)
     xemu_hud_should_capture_kbd_mouse(&kbd, &mouse);
 
     while (SDL_PollEvent(ev)) {
+        xemu_debug_tools_bql_trace_set_next_context(
+            XEMU_DEBUG_TOOLS_BQL_CONTEXT_UI_SDL_EVENT);
         xemu_main_loop_lock();
 
         // HUD must process events first so that if a controller is detached,
@@ -1428,6 +1492,8 @@ int main(int argc, char **argv)
     tcg_register_init_ctx();
     qemu_set_current_aio_context(qemu_get_aio_context());
 
+    xemu_debug_tools_bql_trace_set_next_context(
+        XEMU_DEBUG_TOOLS_BQL_CONTEXT_UI_INPUT_INIT);
     xemu_main_loop_lock();
     xemu_input_init();
     xemu_main_loop_unlock();
diff --git a/ui/xui/actions.cc b/ui/xui/actions.cc
index 865b2eb..af7f075 100644
--- a/ui/xui/actions.cc
+++ b/ui/xui/actions.cc
@@ -23,8 +23,10 @@
 #include "../xemu-snapshots.h"
 #include "../xemu-notifications.h"
 #include "snapshot-manager.hh"
+#include "debug-tools/debug-tools.hh"
 #include <filesystem>
 
+
 void ActionEjectDisc(void)
 {
     Error *err = NULL;
@@ -78,6 +80,7 @@ void ActionTogglePause(void)
 
 void ActionReset(void)
 {
+    debug_tools_notify_game_reset();
     qemu_system_reset_request(SHUTDOWN_CAUSE_GUEST_RESET);
 }
 
diff --git a/ui/xui/gl-helpers.cc b/ui/xui/gl-helpers.cc
index cdef858..ae163a2 100644
--- a/ui/xui/gl-helpers.cc
+++ b/ui/xui/gl-helpers.cc
@@ -975,23 +975,26 @@ void RenderFramebuffer(GLint tex, int width, int height, bool flip)
     RenderFramebuffer(tex, width, height, flip, scale);
 }
 
-bool RenderFramebufferToPng(GLuint tex, bool flip, std::vector<uint8_t> &png, int max_width, int max_height)
+bool RenderFramebufferToRgb(GLuint tex, bool flip,
+                            std::vector<uint8_t> &pixels, int &width,
+                            int &height, int max_width, int max_height)
 {
-    int width, height;
-
     glActiveTexture(GL_TEXTURE0);
     glBindTexture(GL_TEXTURE_2D, tex);
     glGetTexLevelParameteriv(GL_TEXTURE_2D, 0, GL_TEXTURE_WIDTH, &width);
     glGetTexLevelParameteriv(GL_TEXTURE_2D, 0, GL_TEXTURE_HEIGHT, &height);
 
+    if (width <= 0 || height <= 0) {
+        return false;
+    }
+
     width = height * GetDisplayAspectRatio(width, height);
 
     if (!max_width) max_width = width;
     if (!max_height) max_height = height;
     ScaleDimensions(width, height, max_width, max_height, &width, &height);
 
-    std::vector<uint8_t> pixels;
-    pixels.resize(width * height * 3);
+    pixels.resize((size_t)width * (size_t)height * 3);
 
     Fbo fbo(width, height);
     fbo.Target();
@@ -1006,6 +1009,19 @@ bool RenderFramebufferToPng(GLuint tex, bool flip, std::vector<uint8_t> &png, in
     glReadPixels(0, 0, width, height, GL_RGB, GL_UNSIGNED_BYTE, pixels.data());
     fbo.Restore();
 
+    return glGetError() == GL_NO_ERROR;
+}
+
+bool RenderFramebufferToPng(GLuint tex, bool flip, std::vector<uint8_t> &png,
+                            int max_width, int max_height)
+{
+    int width = 0;
+    int height = 0;
+    std::vector<uint8_t> pixels;
+    if (!RenderFramebufferToRgb(tex, flip, pixels, width, height,
+                                max_width, max_height)) {
+        return false;
+    }
     return fpng::fpng_encode_image_to_memory(pixels.data(), width, height, 3, png);
 }
 
diff --git a/ui/xui/gl-helpers.hh b/ui/xui/gl-helpers.hh
index 82da963..81e52f3 100644
--- a/ui/xui/gl-helpers.hh
+++ b/ui/xui/gl-helpers.hh
@@ -51,6 +51,7 @@ void RenderXmu(float frame_x, float frame_y, uint32_t primary_color,
                uint32_t secondary_color);
 void RenderFramebuffer(GLint tex, int width, int height, bool flip);
 void RenderFramebuffer(GLint tex, int width, int height, bool flip, float scale[2]);
+bool RenderFramebufferToRgb(GLuint tex, bool flip, std::vector<uint8_t> &pixels, int &width, int &height, int max_width = 0, int max_height = 0);
 bool RenderFramebufferToPng(GLuint tex, bool flip, std::vector<uint8_t> &png, int max_width = 0, int max_height = 0);
 void SaveScreenshot(GLuint tex, bool flip);
 void ScaleDimensions(int src_width, int src_height, int max_width, int max_height, int *out_width, int *out_height);
diff --git a/ui/xui/input-manager.cc b/ui/xui/input-manager.cc
index a18aa9f..a0be5c7 100644
--- a/ui/xui/input-manager.cc
+++ b/ui/xui/input-manager.cc
@@ -2,6 +2,8 @@
 #include "input-manager.hh"
 #include "../xemu-input.h"
 #include "common.hh"
+#include "scene-manager.hh"
+#include "debug-tools/addons/diagnostics/controller-xid-trace-hook.h"
 
 InputManager g_input_mgr;
 
@@ -19,11 +21,13 @@ void InputManager::Update()
     m_buttons = 0;
     int16_t axis[CONTROLLER_AXIS__COUNT] = {0};
 
+    const bool input_rebinding = g_main_menu.IsInputRebinding();
+
     // If we are rebinding a controller, prevent navigation
-    if (!g_main_menu.IsInputRebinding()) {
+    if (!input_rebinding) {
         ControllerState *iter;
         QTAILQ_FOREACH (iter, &available_controllers, entry) {
-            if (iter->type != INPUT_DEVICE_SDL_GAMEPAD)
+            if (iter->type != INPUT_DEVICE_SDL_GAMEPAD || !iter->connected)
                 continue;
             m_buttons |= iter->buttons;
             // We simply take any axis that is >10 % activation
@@ -46,16 +50,35 @@ void InputManager::Update()
         m_navigating_with_controller = false;
     }
 
-    // If mouse capturing is enabled (we are in a dialog), ensure the UI is alive
-    bool controller_focus_capture = false;
-    if (io.NavActive) {
-        controller_focus_capture = true;
-        m_navigating_with_controller |= !!m_buttons;
+    bool controller_activity = !!m_buttons;
+    for (int i = 0; i < CONTROLLER_AXIS__COUNT; i++) {
+        if ((axis[i] > 3276) || (axis[i] < -3276)) {
+            controller_activity = true;
+            break;
+        }
     }
 
+    const bool main_scene_active = g_scene_mgr.IsDisplayingScene();
+
+    /* Info: Detached Debug Tools focus does not consume guest controller input. */
+    if (!io.NavActive || !main_scene_active) {
+        m_navigating_with_controller = false;
+    } else if (controller_activity) {
+        m_navigating_with_controller = true;
+    }
+
+    const bool controller_focus_capture =
+        input_rebinding ||
+        main_scene_active;
+
+    xemu_debug_tools_controller_xid_trace_input_mode(
+        controller_focus_capture ? 1 : 0,
+        io.NavActive ? 1 : 0,
+        controller_activity ? 1 : 0,
+        input_rebinding ? 1 : 0,
+        m_navigating_with_controller ? 1 : 0);
 
-    // Prevent controller events from going to the guest if they are being used
-    // to navigate the HUD
+    /* Info: Main xemu scenes own controller capture; detached tools do not. */
     xemu_input_set_test_mode(controller_focus_capture); // FIXME: Rename 'test mode'
 
     // Update gamepad inputs
diff --git a/ui/xui/main-menu.cc b/ui/xui/main-menu.cc
index 85997c1..9d74283 100644
--- a/ui/xui/main-menu.cc
+++ b/ui/xui/main-menu.cc
@@ -27,6 +27,8 @@
 #include "xemu-hud.h"
 #include "misc.hh"
 #include "gl-helpers.hh"
+#include "debug-tools/addons/Folder-HDD/folder-hdd-ui.hh"
+#include "debug-tools/addons/Video-Recorder/video-recorder-ui.hh"
 #include "reporting.hh"
 #include "qapi/error.h"
 #include "actions.hh"
@@ -79,6 +81,8 @@ void MainMenuGeneralView::Draw()
                [](const char *path) {
                    xemu_settings_set_string(&g_config.general.games_dir, path);
                });
+
+    cmp_video_recorder_draw_settings();
     // toggle("Throttle DVD/HDD speeds", &g_config.general.throttle_io,
     //        "Limit DVD/HDD throughput to approximate Xbox load times");
 }
@@ -1519,10 +1523,6 @@ void MainMenuSystemView::Draw()
         { ".rom Files", "rom" },
         { "All Files", "*" }
     };
-    static const SDL_DialogFileFilter qcow_file_filters[] = {
-        { ".qcow2 Files", "qcow2" },
-        { "All Files", "*" }
-    };
 
     if (m_dirty) {
         ImGui::TextColored(ImVec4(1, 0, 0, 1),
@@ -1563,11 +1563,7 @@ void MainMenuSystemView::Draw()
                    m_dirty = true;
                    g_main_menu.UpdateAboutViewConfigInfo();
                });
-    FilePicker("Hard Disk", g_config.sys.files.hdd_path,
-               qcow_file_filters, 2, false, [this](const char *path) {
-                   xemu_settings_set_string(&g_config.sys.files.hdd_path, path);
-                   m_dirty = true;
-               });
+    cmp_folder_hdd_draw_settings(&m_dirty);
     FilePicker("EEPROM", g_config.sys.files.eeprom_path,
                rom_file_filters, 3, false, [this](const char *path) {
                    xemu_settings_set_string(&g_config.sys.files.eeprom_path, path);
diff --git a/ui/xui/main.cc b/ui/xui/main.cc
index ace02bc..db9a9cd 100644
--- a/ui/xui/main.cc
+++ b/ui/xui/main.cc
@@ -39,6 +39,7 @@
 #include "input-manager.hh"
 #include "snapshot-manager.hh"
 #include "viewport-manager.hh"
+#include "debug-tools/addons/Video-Recorder/video-recorder-c.h"
 #include "font-manager.hh"
 #include "scene.hh"
 #include "scene-manager.hh"
@@ -47,6 +48,8 @@
 #include "notifications.hh"
 #include "monitor.hh"
 #include "debug.hh"
+#include "debug-tools/debug-tools.hh"
+#include "debug-tools/addons/diagnostics/bql-trace-hook.h"
 #include "welcome.hh"
 #include "menubar.hh"
 #include "compat.hh"
@@ -147,6 +150,7 @@ void xemu_hud_init(SDL_Window* window, void* sdl_gl_context)
     ImGui_ImplSDL3_InitForOpenGL(window, sdl_gl_context);
     ImGui_ImplOpenGL3_Init("#version 150");
     ImPlot::CreateContext();
+    debug_tools_init(window, sdl_gl_context);
 
 #if defined(_WIN32)
     if (!g_config.general.show_welcome && g_config.general.updates.check) {
@@ -161,6 +165,7 @@ void xemu_hud_init(SDL_Window* window, void* sdl_gl_context)
 
 void xemu_hud_cleanup(void)
 {
+    debug_tools_cleanup();
     ImGui_ImplOpenGL3_Shutdown();
     ImGui_ImplSDL3_Shutdown();
     ImGui::DestroyContext();
@@ -173,6 +178,13 @@ void xemu_hud_process_sdl_events(SDL_Event *event)
         return;
     }
 
+    xemu_debug_tools_bql_trace_owner_stage_begin(
+        XEMU_DEBUG_TOOLS_BQL_STAGE_DEBUG_TOOLS_SDL_EVENT);
+    const bool debug_tools_consumed = debug_tools_process_sdl_event(event);
+    xemu_debug_tools_bql_trace_owner_stage_end();
+    if (debug_tools_consumed) {
+        return;
+    }
     ImGui_ImplSDL3_ProcessEvent(event);
 }
 
@@ -189,8 +201,31 @@ void xemu_hud_set_framebuffer_texture(GLuint tex, bool flip)
     g_flip_req = flip;
 }
 
+int xemu_hud_is_detached_window_id(SDL_WindowID window_id)
+{
+    return debug_tools_owns_window_id(window_id) ? 1 : 0;
+}
+
+void xemu_hud_render_playback_only(void)
+{
+    if (first_boot_window.is_open) {
+        return;
+    }
+
+    int width = 0;
+    int height = 0;
+    SDL_GetWindowSizeInPixels(xemu_get_window(), &width, &height);
+    if (width <= 0 || height <= 0) {
+        return;
+    }
+
+    RenderFramebuffer(g_tex, width, height, g_flip_req);
+}
+
 void xemu_hud_update(void)
 {
+    xemu_debug_tools_bql_trace_owner_stage_begin(
+        XEMU_DEBUG_TOOLS_BQL_STAGE_HUD_CORE);
     ImGuiIO& io = ImGui::GetIO();
     uint32_t now = SDL_GetTicks();
 
@@ -306,6 +341,14 @@ void xemu_hud_update(void)
         }
     }
 
+    xemu_debug_tools_bql_trace_owner_stage_end();
+    xemu_debug_tools_bql_trace_owner_stage_begin(
+        XEMU_DEBUG_TOOLS_BQL_STAGE_DEBUG_TOOLS_TICK);
+    debug_tools_tick();
+    xemu_debug_tools_bql_trace_owner_stage_end();
+    xemu_debug_tools_bql_trace_owner_stage_begin(
+        XEMU_DEBUG_TOOLS_BQL_STAGE_HUD_CORE);
+
     first_boot_window.Draw();
     monitor_window.Draw();
     apu_window.Draw();
@@ -317,16 +360,34 @@ void xemu_hud_update(void)
     g_scene_mgr.Draw();
     if (!first_boot_window.is_open) notification_manager.Draw();
     g_snapshot_mgr.Draw();
+    xemu_debug_tools_bql_trace_owner_stage_end();
 
     // static bool show_demo = true;
     // if (show_demo) ImGui::ShowDemoWindow(&show_demo);
 }
 
+void xemu_hud_build_detached_frames_unlocked()
+{
+    debug_tools_build_unlocked_detached_frames();
+}
+
+void xemu_hud_process_deferred_actions()
+{
+    static bool detached_windows_precreated = false;
+    if (!detached_windows_precreated) {
+        debug_tools_precreate_detached_windows();
+        detached_windows_precreated = true;
+    }
+    debug_tools_process_deferred_actions();
+}
+
 void xemu_hud_render()
 {
     ImGui::Render();
     ImGui_ImplOpenGL3_RenderDrawData(ImGui::GetDrawData());
 
+    debug_tools_render_detached_frames();
+
     if (g_vsync != g_config.display.window.vsync) {
         g_vsync = g_config.display.window.vsync;
         SDL_GL_SetSwapInterval(g_vsync ? 1 : 0);
@@ -336,4 +397,6 @@ void xemu_hud_render()
         SaveScreenshot(g_tex, g_flip_req);
         g_screenshot_pending = false;
     }
+
+    xemu_video_recorder_capture(g_tex, g_flip_req ? 1 : 0);
 }
diff --git a/ui/xui/menubar.cc b/ui/xui/menubar.cc
index 38e2d1b..20efc22 100644
--- a/ui/xui/menubar.cc
+++ b/ui/xui/menubar.cc
@@ -26,6 +26,7 @@
 #include "debug.hh"
 #include "actions.hh"
 #include "compat.hh"
+#include "debug-tools/debug-tools.hh"
 #include "update.hh"
 #include "../xemu-os-utils.h"
 
@@ -220,6 +221,8 @@ void ShowMainMenu()
             ImGui::MenuItem("Monitor", "~", &monitor_window.is_open);
             ImGui::MenuItem("Audio", NULL, &apu_window.m_is_open);
             ImGui::MenuItem("Video", NULL, &video_window.m_is_open);
+            ImGui::Separator();
+            debug_tools_draw_menu_items();
 #ifdef CONFIG_RENDERDOC
             if (nv2a_dbg_renderdoc_available()) {
                 ImGui::MenuItem("RenderDoc: Capture", NULL, &g_capture_renderdoc_frame);
diff --git a/ui/xui/meson.build b/ui/xui/meson.build
index 95d50cb..42656dc 100644
--- a/ui/xui/meson.build
+++ b/ui/xui/meson.build
@@ -23,6 +23,8 @@ xemu_ss.add(files(
   'widgets.cc',
 ))
 
+# xemu RAW Cheat Engine / Memory / Debugger extensions live in their own subtree.
+subdir('debug-tools')
 if host_os == 'windows'
   xemu_ss.add(files('update.cc', 'win32-dxgi-present.cc'))
 endif
diff --git a/ui/xui/notifications.cc b/ui/xui/notifications.cc
index f4c5179..ec8424a 100644
--- a/ui/xui/notifications.cc
+++ b/ui/xui/notifications.cc
@@ -35,7 +35,15 @@ void NotificationManager::QueueNotification(const char *msg)
 
 void NotificationManager::QueueError(const char *msg)
 {
-    m_error_queue.push_back(strdup(msg));
+    m_error_queue.push_back({msg ? msg : "", "", nullptr, nullptr});
+}
+
+void NotificationManager::QueueErrorWithAction(
+    const char *msg, const char *action_label, XemuNotificationAction action,
+    void *opaque)
+{
+    m_error_queue.push_back({msg ? msg : "", action_label ? action_label : "",
+                             action, opaque});
 }
 
 void NotificationManager::Draw()
@@ -73,14 +81,30 @@ void NotificationManager::Draw()
     }
     if (ImGui::BeginPopupModal("Error", NULL, ImGuiWindowFlags_AlwaysAutoResize))
     {
-        ImGui::Text("%s", m_error_queue[0]);
+        ErrorEntry &entry = m_error_queue.front();
+        ImGui::Text("%s", entry.message.c_str());
         ImGui::Dummy(ImVec2(0,16));
-        ImGui::SetItemDefaultFocus();
         ImGuiStyle &style = ImGui::GetStyle();
-        ImGui::SetCursorPosX(ImGui::GetWindowWidth()-(120+2*style.FramePadding.x));
+        const bool has_action = entry.action && !entry.action_label.empty();
+        const float spacing = has_action ? style.ItemSpacing.x : 0.0f;
+        const float buttons_width = has_action ? 240.0f + spacing : 120.0f;
+        ImGui::SetCursorPosX(ImGui::GetWindowWidth() -
+                             (buttons_width + 2 * style.FramePadding.x));
+        if (has_action) {
+            if (ImGui::Button(entry.action_label.c_str(), ImVec2(120, 0))) {
+                XemuNotificationAction action = entry.action;
+                void *opaque = entry.opaque;
+                ImGui::CloseCurrentPopup();
+                m_error_queue.pop_front();
+                action(opaque);
+                ImGui::EndPopup();
+                return;
+            }
+            ImGui::SameLine();
+        }
+        ImGui::SetItemDefaultFocus();
         if (ImGui::Button("Ok", ImVec2(120, 0))) {
             ImGui::CloseCurrentPopup();
-            free((void*)m_error_queue[0]);
             m_error_queue.pop_front();
         }
         ImGui::EndPopup();
@@ -157,3 +181,10 @@ void xemu_queue_error_message(const char *msg)
 {
     notification_manager.QueueError(msg);
 }
+
+void xemu_queue_error_message_with_action(
+    const char *msg, const char *action_label, XemuNotificationAction action,
+    void *opaque)
+{
+    notification_manager.QueueErrorWithAction(msg, action_label, action, opaque);
+}
diff --git a/ui/xui/notifications.hh b/ui/xui/notifications.hh
index 77a97d5..9b09922 100644
--- a/ui/xui/notifications.hh
+++ b/ui/xui/notifications.hh
@@ -19,6 +19,7 @@
 #pragma once
 #include <stdint.h>
 #include <deque>
+#include <string>
 
 #include "../xemu-notifications.h"
 
@@ -26,7 +27,13 @@ class NotificationManager
 {
 private:
     std::deque<const char *> m_notification_queue;
-    std::deque<const char *> m_error_queue;
+    struct ErrorEntry {
+        std::string message;
+        std::string action_label;
+        XemuNotificationAction action = nullptr;
+        void *opaque = nullptr;
+    };
+    std::deque<ErrorEntry> m_error_queue;
 
     const int kNotificationDuration = 4000;
     uint32_t m_notification_end_time;
@@ -37,6 +44,8 @@ public:
     NotificationManager();
     void QueueNotification(const char *msg);
     void QueueError(const char *msg);
+    void QueueErrorWithAction(const char *msg, const char *action_label,
+                              XemuNotificationAction action, void *opaque);
     void Draw();
 
 private:
diff --git a/ui/xui/popup-menu.cc b/ui/xui/popup-menu.cc
index f80ce9f..7773143 100644
--- a/ui/xui/popup-menu.cc
+++ b/ui/xui/popup-menu.cc
@@ -32,6 +32,7 @@
 #include "IconsFontAwesome6.h"
 #include "../xemu-snapshots.h"
 #include "main-menu.hh"
+#include "debug-tools/addons/game-library/game-library-menu-hook.h"
 
 PopupMenuItemDelegate::~PopupMenuItemDelegate() {}
 void PopupMenuItemDelegate::PushMenu(PopupMenu &menu) {}
@@ -348,7 +349,11 @@ public:
     void Show(const ImVec2 &direction) override
     {
         PopupMenu::Show(direction);
-        PopulateGameList();
+        if (xemu_debug_tools_game_library_menu_available()) {
+            xemu_debug_tools_game_library_menu_prepare();
+        } else {
+            PopulateGameList();
+        }
     }
 
     bool DrawItems(PopupMenuItemDelegate &nav) override
@@ -359,18 +364,56 @@ public:
             ImGui::SetKeyboardFocusHere();
         }
 
-        for (const auto &[label, file_path] : sorted_file_names) {
-            if (PopupMenuButton(label, ICON_FA_COMPACT_DISC)) {
-                ActionLoadDiscFile(file_path.c_str());
+        if (xemu_debug_tools_game_library_menu_available()) {
+            const size_t count = xemu_debug_tools_game_library_menu_count();
+            char label[512] = {};
+            for (size_t i = 0; i < count; ++i) {
+                if (!xemu_debug_tools_game_library_menu_get_label(
+                        i, label, sizeof(label))) {
+                    continue;
+                }
+                if (PopupMenuButton(label, ICON_FA_COMPACT_DISC)) {
+                    if (xemu_debug_tools_game_library_menu_launch(i)) {
+                        nav.ClearMenuStack();
+                        pop = true;
+                    }
+                    break;
+                }
+            }
+
+            if (count == 0) {
+                ImGui::PushFont(g_font_mgr.m_menu_font);
+                ImGui::TextDisabled(
+                    xemu_debug_tools_game_library_menu_scanning()
+                        ? "Scanning game library..."
+                        : "No games found");
+                ImGui::PopFont();
+            }
+
+            ImGui::Separator();
+            if (PopupMenuButton("Open Full Game Library", ICON_FA_GAMEPAD)) {
+                xemu_debug_tools_game_library_menu_open_full();
                 nav.ClearMenuStack();
                 pop = true;
             }
-        }
+            if (!pop &&
+                PopupMenuButton("Refresh Library", ICON_FA_ARROWS_ROTATE)) {
+                xemu_debug_tools_game_library_menu_refresh();
+            }
+        } else {
+            for (const auto &[label, file_path] : sorted_file_names) {
+                if (PopupMenuButton(label, ICON_FA_COMPACT_DISC)) {
+                    ActionLoadDiscFile(file_path.c_str());
+                    nav.ClearMenuStack();
+                    pop = true;
+                }
+            }
 
-        if (sorted_file_names.size() == 0) {
-            if (PopupMenuButton("No games found", ICON_FA_SLIDERS)) {
-                nav.ClearMenuStack();
-                g_scene_mgr.PushScene(g_main_menu);
+            if (sorted_file_names.size() == 0) {
+                if (PopupMenuButton("No games found", ICON_FA_SLIDERS)) {
+                    nav.ClearMenuStack();
+                    g_scene_mgr.PushScene(g_main_menu);
+                }
             }
         }
 
@@ -380,7 +423,8 @@ public:
         return pop;
     }
 
-    void PopulateGameList() {
+    void PopulateGameList()
+    {
         sorted_file_names.clear();
         std::filesystem::path directory(g_config.general.games_dir);
         std::error_code ec;
diff --git a/ui/xui/xemu-hud.h b/ui/xui/xemu-hud.h
index 0e812fe..85b7826 100644
--- a/ui/xui/xemu-hud.h
+++ b/ui/xui/xemu-hud.h
@@ -43,10 +43,14 @@ void xemu_main_loop_unlock(void);
 void xemu_hud_init(SDL_Window *window, void *sdl_gl_context);
 void xemu_hud_cleanup(void);
 void xemu_hud_update(void);
+void xemu_hud_build_detached_frames_unlocked(void);
 void xemu_hud_render(void);
+void xemu_hud_process_deferred_actions(void);
 void xemu_hud_process_sdl_events(SDL_Event *event);
 void xemu_hud_should_capture_kbd_mouse(int *kbd, int *mouse);
 void xemu_hud_set_framebuffer_texture(GLuint tex, bool flip);
+int xemu_hud_is_detached_window_id(SDL_WindowID window_id);
+void xemu_hud_render_playback_only(void);
 
 #ifdef __cplusplus
 }
diff --git a/util/main-loop.c b/util/main-loop.c
index 42ecfb8..5c35913 100644
--- a/util/main-loop.c
+++ b/util/main-loop.c
@@ -35,6 +35,9 @@
 #include "qemu/error-report.h"
 #include "qemu/queue.h"
 #include "qom/object.h"
+#include "../ui/xui/debug-tools/addons/diagnostics/qemu-timer-trace-hook.h"
+#include "../ui/xui/debug-tools/addons/diagnostics/main-loop-lock-trace-hook.h"
+#include "../ui/xui/debug-tools/addons/diagnostics/host-stall-trace.h"
 
 #ifdef XBOX
 /* FIXME: This is a slightly incomplete implementation of moving
@@ -185,14 +188,18 @@ void qemu_init_main_loop_lock(void)
     qemu_mutex_init(&qemu_main_loop_lock);
 }
 
-void qemu_mutex_lock_main_loop(void)
+void qemu_mutex_lock_main_loop_impl(const char *file, int line)
 {
+    xemu_debug_tools_main_loop_lock_trace_lock_attempt(file, line);
     qemu_mutex_lock(&qemu_main_loop_lock);
+    xemu_debug_tools_main_loop_lock_trace_lock_acquired(file, line);
 }
 
 void qemu_mutex_unlock_main_loop(void)
 {
+    xemu_debug_tools_main_loop_lock_trace_unlock_begin();
     qemu_mutex_unlock(&qemu_main_loop_lock);
+    xemu_debug_tools_main_loop_lock_trace_unlock_end();
 }
 #endif
 
@@ -364,9 +371,17 @@ static int os_host_main_loop_wait(int64_t timeout)
 #endif
     int ret;
 
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_GLIB_CONTEXT_ACQUIRE_ENTER, timeout, 0);
     g_main_context_acquire(context);
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_GLIB_CONTEXT_ACQUIRE_EXIT, timeout, 0);
 
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_GLIB_PREPARE_ENTER, timeout, 0);
     glib_pollfds_fill(&timeout);
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_GLIB_PREPARE_EXIT, timeout, 0);
 
     bql_unlock();
     replay_mutex_unlock();
@@ -374,15 +389,35 @@ static int os_host_main_loop_wait(int64_t timeout)
 #ifdef XBOX
     qemu_mutex_unlock_main_loop();
 #endif
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_POLL_ENTER, timeout, 0);
     ret = qemu_poll_ns((GPollFD *)gpollfds->data, gpollfds->len, timeout);
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_POLL_EXIT, timeout, ret);
 #ifdef XBOX
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_MAIN_LOOP_LOCK_ENTER, timeout, 0);
     qemu_mutex_lock_main_loop();
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_MAIN_LOOP_LOCK_EXIT, timeout, 0);
 #endif
 
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_REPLAY_LOCK_ENTER, timeout, 0);
     replay_mutex_lock();
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_REPLAY_LOCK_EXIT, timeout, 0);
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_BQL_LOCK_ENTER, timeout, 0);
     bql_lock();
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_BQL_LOCK_EXIT, timeout, 0);
 
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_GLIB_DISPATCH_ENTER, timeout, 0);
     glib_pollfds_poll();
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_GLIB_DISPATCH_EXIT, timeout, 0);
 
     g_main_context_release(context);
 
@@ -541,6 +576,7 @@ static int os_host_main_loop_wait(int64_t timeout)
     GPollFD poll_fds[1024 * 2]; /* this is probably overkill */
     int select_ret = 0;
     int g_poll_ret, ret, i, n_poll_fds;
+    gboolean glib_ready;
     PollingEntry *pe;
     WaitObjects *w = &wait_objects;
     gint poll_timeout;
@@ -549,7 +585,11 @@ static int os_host_main_loop_wait(int64_t timeout)
     fd_set rfds, wfds, xfds;
     int nfds;
 
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_GLIB_CONTEXT_ACQUIRE_ENTER, timeout, 0);
     g_main_context_acquire(context);
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_GLIB_CONTEXT_ACQUIRE_EXIT, timeout, 0);
 
     FD_ZERO(&rfds);
     FD_ZERO(&wfds);
@@ -565,9 +605,13 @@ static int os_host_main_loop_wait(int64_t timeout)
         }
     }
 
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_GLIB_PREPARE_ENTER, timeout, 0);
     g_main_context_prepare(context, &max_priority);
     n_poll_fds = g_main_context_query(context, max_priority, &poll_timeout,
                                       poll_fds, ARRAY_SIZE(poll_fds));
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_GLIB_PREPARE_EXIT, timeout, n_poll_fds);
     g_assert(n_poll_fds + w->num <= ARRAY_SIZE(poll_fds));
 
     for (i = 0; i < w->num; i++) {
@@ -590,14 +634,35 @@ static int os_host_main_loop_wait(int64_t timeout)
 #ifdef XBOX
     qemu_mutex_unlock_main_loop();
 #endif
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_POLL_ENTER, poll_timeout_ns, 0);
+    xemu_debug_tools_host_stall_main_loop_wait_begin(poll_timeout_ns);
     g_poll_ret = qemu_poll_ns(poll_fds, n_poll_fds + w->num, poll_timeout_ns);
+    xemu_debug_tools_host_stall_main_loop_wait_end(poll_timeout_ns);
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_POLL_EXIT, poll_timeout_ns, g_poll_ret);
 #ifdef XBOX
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_MAIN_LOOP_LOCK_ENTER, poll_timeout_ns, 0);
     qemu_mutex_lock_main_loop();
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_MAIN_LOOP_LOCK_EXIT, poll_timeout_ns, 0);
 #endif
 
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_REPLAY_LOCK_ENTER, poll_timeout_ns, 0);
     replay_mutex_lock();
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_REPLAY_LOCK_EXIT, poll_timeout_ns, 0);
 
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_BQL_LOCK_ENTER, poll_timeout_ns, 0);
     bql_lock();
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_BQL_LOCK_EXIT, poll_timeout_ns, 0);
+
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_WAIT_OBJECTS_ENTER, poll_timeout_ns, g_poll_ret);
     if (g_poll_ret > 0) {
         for (i = 0; i < w->num; i++) {
             w->revents[i] = poll_fds[n_poll_fds + i].revents;
@@ -608,18 +673,38 @@ static int os_host_main_loop_wait(int64_t timeout)
             }
         }
     }
-
-    if (g_main_context_check(context, max_priority, poll_fds, n_poll_fds)) {
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_WAIT_OBJECTS_EXIT, poll_timeout_ns, g_poll_ret);
+
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_GLIB_CHECK_ENTER, poll_timeout_ns, 0);
+    glib_ready =
+        g_main_context_check(context, max_priority, poll_fds, n_poll_fds);
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_GLIB_CHECK_EXIT, poll_timeout_ns, glib_ready);
+    if (glib_ready) {
+        xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+            XEMU_DEBUG_TOOLS_QTIMER_GLIB_DISPATCH_ENTER, poll_timeout_ns, 0);
         g_main_context_dispatch(context);
+        xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+            XEMU_DEBUG_TOOLS_QTIMER_GLIB_DISPATCH_EXIT, poll_timeout_ns, 0);
     }
 
     /* XXX: need to suppress polling by better using win32 events */
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_POLLING_CALLBACKS_ENTER, poll_timeout_ns, 0);
     ret = 0;
     for (pe = first_polling_entry; pe != NULL; pe = pe->next) {
         ret |= pe->func(pe->opaque);
     }
-    
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_POLLING_CALLBACKS_EXIT, poll_timeout_ns, ret);
+
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_GLIB_CONTEXT_RELEASE_ENTER, poll_timeout_ns, 0);
     g_main_context_release(context);
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_GLIB_CONTEXT_RELEASE_EXIT, poll_timeout_ns, 0);
     
     if (ret != 0) {
         return ret;
@@ -671,7 +756,11 @@ void main_loop_wait(int nonblocking)
                                       timerlistgroup_deadline_ns(
                                           &main_loop_tlg));
 
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_MAIN_WAIT_ENTER, timeout_ns, 0);
     ret = os_host_main_loop_wait(timeout_ns);
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_MAIN_WAIT_EXIT, timeout_ns, ret);
     mlpoll.state = ret < 0 ? MAIN_LOOP_POLL_ERR : MAIN_LOOP_POLL_OK;
     notifier_list_notify(&main_loop_poll_notifiers, &mlpoll);
 
@@ -682,7 +771,12 @@ void main_loop_wait(int nonblocking)
          */
         icount_start_warp_timer();
     }
-    qemu_clock_run_all_timers();
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_RUN_TIMERS_ENTER, timeout_ns, 0);
+    const bool timer_progress = qemu_clock_run_all_timers();
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_RUN_TIMERS_EXIT, timeout_ns,
+        timer_progress ? 1 : 0);
 }
 
 /* Functions to operate on the main QEMU AioContext.  */
diff --git a/util/qemu-timer.c b/util/qemu-timer.c
index 650030c..bb91fe9 100644
--- a/util/qemu-timer.c
+++ b/util/qemu-timer.c
@@ -30,6 +30,8 @@
 #include "exec/icount.h"
 #include "system/replay.h"
 #include "system/cpus.h"
+#include "../ui/xui/debug-tools/addons/diagnostics/qemu-timer-trace-hook.h"
+#include "../ui/xui/debug-tools/addons/diagnostics/slow-timer-trace-hook.h"
 
 #ifdef CONFIG_POSIX
 #include <pthread.h>
@@ -57,6 +59,59 @@ typedef struct QEMUClock {
 QEMUTimerListGroup main_loop_tlg;
 static QEMUClock qemu_clocks[QEMU_CLOCK_MAX];
 
+/* Info: Optional Diagnostics timer observation bridge. */
+static XemuDebugToolsQemuTimerObserver xemu_debug_tools_timer_observer;
+static XemuDebugToolsQemuMainLoopObserver xemu_debug_tools_main_loop_observer;
+static XemuDebugToolsSlowTimerObserver xemu_debug_tools_slow_timer_observer;
+static int xemu_debug_tools_slow_timer_enabled;
+static uint64_t xemu_debug_tools_slow_timer_threshold_ns = 1000000ULL;
+
+void xemu_debug_tools_qemu_timer_hook_install(
+    XemuDebugToolsQemuTimerObserver timer_observer,
+    XemuDebugToolsQemuMainLoopObserver main_loop_observer)
+{
+    xemu_debug_tools_timer_observer = timer_observer;
+    xemu_debug_tools_main_loop_observer = main_loop_observer;
+}
+
+void xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+    XemuDebugToolsQemuTimerHookEvent event_type,
+    int64_t timeout_ns,
+    int64_t value)
+{
+    if (xemu_debug_tools_main_loop_observer) {
+        xemu_debug_tools_main_loop_observer(event_type, timeout_ns, value);
+    }
+}
+
+void xemu_debug_tools_slow_timer_hook_install(
+    XemuDebugToolsSlowTimerObserver observer)
+{
+    /* Info: Keep the observer stable while runtime thresholds change. */
+    xemu_debug_tools_slow_timer_observer = observer;
+}
+
+void xemu_debug_tools_slow_timer_hook_configure(
+    int enabled, uint64_t threshold_ns)
+{
+    qatomic_set(&xemu_debug_tools_slow_timer_threshold_ns,
+                threshold_ns != 0 ? threshold_ns : 1);
+    qatomic_set(&xemu_debug_tools_slow_timer_enabled, enabled != 0);
+}
+
+static inline void xemu_debug_tools_qemu_timer_hook_emit_timer(
+    QEMUTimer *timer,
+    XemuDebugToolsQemuTimerHookEvent event_type,
+    int clock_type,
+    int64_t expire_ns,
+    int64_t value)
+{
+    if (xemu_debug_tools_timer_observer) {
+        xemu_debug_tools_timer_observer(timer, event_type, clock_type,
+                                        expire_ns, value);
+    }
+}
+
 /* A QEMUTimerList is a list of timers attached to a clock. More
  * than one QEMUTimerList can be attached to each clock, for instance
  * used by different AioContexts / threads. Each clock also has
@@ -143,6 +198,8 @@ void qemu_clock_notify(QEMUClockType type)
 {
     QEMUTimerList *timer_list;
     QEMUClock *clock = qemu_clock_ptr(type);
+    xemu_debug_tools_qemu_timer_hook_emit_timer(
+        NULL, XEMU_DEBUG_TOOLS_QTIMER_CLOCK_NOTIFY, type, -1, 0);
     QLIST_FOREACH(timer_list, &clock->timerlists, list) {
         timerlist_notify(timer_list);
     }
@@ -325,9 +382,12 @@ int qemu_timeout_ns_to_ms(int64_t ns)
  */
 int qemu_poll_ns(GPollFD *fds, guint nfds, int64_t timeout)
 {
+    int ret;
 #ifdef CONFIG_PPOLL
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_POLL_BACKEND_ENTER, timeout, 1);
     if (timeout < 0) {
-        return ppoll((struct pollfd *)fds, nfds, NULL, NULL);
+        ret = ppoll((struct pollfd *)fds, nfds, NULL, NULL);
     } else {
         struct timespec ts;
         int64_t tvsec = timeout / 1000000000LL;
@@ -339,8 +399,11 @@ int qemu_poll_ns(GPollFD *fds, guint nfds, int64_t timeout)
         }
         ts.tv_sec = tvsec;
         ts.tv_nsec = timeout % 1000000000LL;
-        return ppoll((struct pollfd *)fds, nfds, &ts, NULL);
+        ret = ppoll((struct pollfd *)fds, nfds, &ts, NULL);
     }
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_POLL_BACKEND_EXIT, timeout, ret);
+    return ret;
 #else
 
 #ifdef XBOX
@@ -349,16 +412,27 @@ int qemu_poll_ns(GPollFD *fds, guint nfds, int64_t timeout)
      */
     #define XBOX_BUSYWAIT_THRESHOLD_NS 1250000
     if ((0 < timeout) && (timeout < XBOX_BUSYWAIT_THRESHOLD_NS)) {
-        int64_t now = qemu_clock_get_ns(QEMU_CLOCK_REALTIME);
-        int64_t end = now + timeout;
+        int64_t now;
+        int64_t end;
+        xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+            XEMU_DEBUG_TOOLS_QTIMER_POLL_BUSYWAIT_ENTER, timeout, 0);
+        now = qemu_clock_get_ns(QEMU_CLOCK_REALTIME);
+        end = now + timeout;
         while (now < end) {
             now = qemu_clock_get_ns(QEMU_CLOCK_REALTIME);
         }
+        xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+            XEMU_DEBUG_TOOLS_QTIMER_POLL_BUSYWAIT_EXIT, timeout, 0);
         timeout = 0;
     }
 #endif
 
-    return g_poll(fds, nfds, qemu_timeout_ns_to_ms(timeout));
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_POLL_BACKEND_ENTER, timeout, 0);
+    ret = g_poll(fds, nfds, qemu_timeout_ns_to_ms(timeout));
+    xemu_debug_tools_qemu_timer_hook_emit_main_loop(
+        XEMU_DEBUG_TOOLS_QTIMER_POLL_BACKEND_EXIT, timeout, ret);
+    return ret;
 #endif
 }
 
@@ -377,6 +451,9 @@ void timer_init_full(QEMUTimer *ts,
     ts->scale = scale;
     ts->attributes = attributes;
     ts->expire_time = -1;
+    ts->xemu_debug_cb_name = NULL;
+    ts->xemu_debug_cb_file = NULL;
+    ts->xemu_debug_cb_line = 0;
 }
 
 void timer_deinit(QEMUTimer *ts)
@@ -433,11 +510,15 @@ static void timerlist_rearm(QEMUTimerList *timer_list)
 void timer_del(QEMUTimer *ts)
 {
     QEMUTimerList *timer_list = ts->timer_list;
+    const int64_t previous_expire_ns = ts->expire_time;
 
     if (timer_list) {
         qemu_mutex_lock(&timer_list->active_timers_lock);
         timer_del_locked(timer_list, ts);
         qemu_mutex_unlock(&timer_list->active_timers_lock);
+        xemu_debug_tools_qemu_timer_hook_emit_timer(
+            ts, XEMU_DEBUG_TOOLS_QTIMER_TIMER_DEL,
+            timer_list->clock->type, previous_expire_ns, 0);
     }
 }
 
@@ -456,6 +537,9 @@ void timer_mod_ns(QEMUTimer *ts, int64_t expire_time)
     if (rearm) {
         timerlist_rearm(timer_list);
     }
+    xemu_debug_tools_qemu_timer_hook_emit_timer(
+        ts, XEMU_DEBUG_TOOLS_QTIMER_TIMER_MOD, timer_list->clock->type,
+        ts->expire_time, rearm ? 1 : 0);
 }
 
 /* modify the current timer so that it will be fired when current_time
@@ -568,6 +652,7 @@ bool timerlist_run_timers(QEMUTimerList *timer_list)
         }
 
         /* remove timer from the list before calling the callback */
+        const int64_t scheduled_expire_ns = ts->expire_time;
         timer_list->active_timers = ts->next;
         ts->next = NULL;
         ts->expire_time = -1;
@@ -576,7 +661,48 @@ bool timerlist_run_timers(QEMUTimerList *timer_list)
 
         /* run the callback (the timer list can be modified) */
         qemu_mutex_unlock(&timer_list->active_timers_lock);
-        cb(opaque);
+        xemu_debug_tools_qemu_timer_hook_emit_timer(
+            ts, XEMU_DEBUG_TOOLS_QTIMER_DISPATCH_BEGIN,
+            timer_list->clock->type, scheduled_expire_ns, 0);
+        if (xemu_debug_tools_slow_timer_observer &&
+            qatomic_read(&xemu_debug_tools_slow_timer_enabled)) {
+            const uint64_t threshold_ns =
+                qatomic_read(&xemu_debug_tools_slow_timer_threshold_ns);
+            const uint64_t host_start_ns =
+                qemu_clock_get_ns(QEMU_CLOCK_REALTIME);
+            const uint64_t virtual_start_ns =
+                qemu_clock_get_ns(QEMU_CLOCK_VIRTUAL);
+            const int64_t timer_clock_start_ns =
+                qemu_clock_get_ns(timer_list->clock->type);
+            const int bql_held_before = bql_locked() ? 1 : 0;
+            uint64_t host_end_ns;
+            uint64_t virtual_end_ns;
+            int64_t timer_clock_end_ns;
+            int bql_held_after;
+
+            cb(opaque);
+
+            bql_held_after = bql_locked() ? 1 : 0;
+            timer_clock_end_ns = qemu_clock_get_ns(timer_list->clock->type);
+            virtual_end_ns = qemu_clock_get_ns(QEMU_CLOCK_VIRTUAL);
+            host_end_ns = qemu_clock_get_ns(QEMU_CLOCK_REALTIME);
+            if (host_end_ns >= host_start_ns &&
+                host_end_ns - host_start_ns >= threshold_ns) {
+                xemu_debug_tools_slow_timer_observer(
+                    ts, (uintptr_t)cb, opaque, timer_list->clock->type,
+                    scheduled_expire_ns, timer_clock_start_ns,
+                    timer_clock_end_ns, host_start_ns, host_end_ns,
+                    virtual_start_ns, virtual_end_ns, threshold_ns,
+                    bql_held_before, bql_held_after,
+                    ts->xemu_debug_cb_name, ts->xemu_debug_cb_file,
+                    ts->xemu_debug_cb_line);
+            }
+        } else {
+            cb(opaque);
+        }
+        xemu_debug_tools_qemu_timer_hook_emit_timer(
+            ts, XEMU_DEBUG_TOOLS_QTIMER_DISPATCH_END,
+            timer_list->clock->type, scheduled_expire_ns, 0);
         qemu_mutex_lock(&timer_list->active_timers_lock);
 
         progress = true;
diff --git a/hw/ide/core.c b/hw/ide/core.c
--- a/hw/ide/core.c
+++ b/hw/ide/core.c
@@ -42,6 +42,7 @@
 #include "system/replay.h"
 #include "system/runstate.h"
 #include "ide-internal.h"
+#include "../../ui/xui/debug-tools/addons/file-replace/file-replace-block.h"
 #include "trace.h"
 
 /* These values were based on a Seagate ST3500418AS but have been modified
@@ -720,8 +721,14 @@
     qemu_iovec_init_buf(&req->qiov, blk_blockalign(s->blk, iov->size),
                         iov->size);
 
-    aioreq = blk_aio_preadv(s->blk, sector_num << BDRV_SECTOR_BITS,
-                            &req->qiov, 0, ide_buffered_readv_cb, req);
+    if (s->drive_kind == IDE_CD) {
+        aioreq = xemu_file_replace_aio_preadv(
+            s->blk, sector_num << BDRV_SECTOR_BITS, &req->qiov, 0,
+            ide_buffered_readv_cb, req);
+    } else {
+        aioreq = blk_aio_preadv(s->blk, sector_num << BDRV_SECTOR_BITS,
+                              &req->qiov, 0, ide_buffered_readv_cb, req);
+    }
 
     QLIST_INSERT_HEAD(&s->buffered_requests, req, list);
     return aioreq;
