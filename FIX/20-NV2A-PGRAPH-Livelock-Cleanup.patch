diff --git a/hw/xbox/nv2a/nv2a.c b/hw/xbox/nv2a/nv2a.c
index 3595d8b..c1faaa0 100644
--- a/hw/xbox/nv2a/nv2a.c
+++ b/hw/xbox/nv2a/nv2a.c
@@ -43,7 +43,8 @@ void nv2a_update_irq(NV2AState *d)
     }
 
     /* PGRAPH */
-    if (d->pgraph.pending_interrupts & d->pgraph.enabled_interrupts) {
+    if (qatomic_read(&d->pgraph.pending_interrupts) &
+        qatomic_read(&d->pgraph.enabled_interrupts)) {
         d->pmc.pending_interrupts |= NV_PMC_INTR_0_PGRAPH;
     } else {
         d->pmc.pending_interrupts &= ~NV_PMC_INTR_0_PGRAPH;
@@ -351,9 +352,9 @@ static void nv2a_reset(NV2AState *d)
     /* seems to start in color mode */
     d->vga.msr = VGA_MIS_COLOR;
 
-    d->pgraph.waiting_for_nop = false;
+    qatomic_set(&d->pgraph.waiting_for_nop, false);
     d->pgraph.waiting_for_flip = false;
-    d->pgraph.waiting_for_context_switch = false;
+    qatomic_set(&d->pgraph.waiting_for_context_switch, false);
 
     d->pmc.pending_interrupts = 0;
     d->pfifo.pending_interrupts = 0;
diff --git a/hw/xbox/nv2a/pfifo.c b/hw/xbox/nv2a/pfifo.c
index 8690916..0071320 100644
--- a/hw/xbox/nv2a/pfifo.c
+++ b/hw/xbox/nv2a/pfifo.c
@@ -92,7 +92,7 @@ void pfifo_write(void *opaque, hwaddr addr, uint64_t val, unsigned int size)
 
 void pfifo_kick(NV2AState *d)
 {
-    d->pfifo.fifo_kick = true;
+    qatomic_set(&d->pfifo.fifo_kick, true);
     qemu_cond_broadcast(&d->pfifo.fifo_cond);
 }
 
@@ -108,7 +108,7 @@ static bool is_flip_stall_complete(NV2AState *d)
 {
     PGRAPHState *pg = &d->pgraph;
 
-    uint32_t s = pgraph_reg_r(pg, NV_PGRAPH_SURFACE);
+    uint32_t s = qatomic_read(&pg->regs_[NV_PGRAPH_SURFACE]);
 
     NV2A_DPRINTF("flip stall read: %d, write: %d, modulo: %d\n",
         GET_MASK(s, NV_PGRAPH_SURFACE_READ_3D),
@@ -191,12 +191,13 @@ static ssize_t pfifo_run_puller(NV2AState *d, uint32_t method_entry,
 
         // TODO: this is fucked
         qemu_mutex_unlock(&d->pfifo.lock);
+        pgraph_wait_for_cpu_mmio_accesses(&d->pgraph);
         PGRAPH_LOCK(&d->pgraph);
 
         // Switch contexts if necessary
         if (can_fifo_access(d)) {
             pgraph_context_switch(d, entry.channel_id);
-            if (!d->pgraph.waiting_for_context_switch) {
+            if (!qatomic_read(&d->pgraph.waiting_for_context_switch)) {
                 num_proc =
                     pgraph_method(d, subchannel, 0, entry.instance, parameters,
                                   num_words_available, max_lookahead_words, inc);
@@ -226,6 +227,7 @@ static ssize_t pfifo_run_puller(NV2AState *d, uint32_t method_entry,
 
         // TODO: this is fucked
         qemu_mutex_unlock(&d->pfifo.lock);
+        pgraph_wait_for_cpu_mmio_accesses(&d->pgraph);
         PGRAPH_LOCK(&d->pgraph);
 
         if (can_fifo_access(d)) {
@@ -464,7 +466,7 @@ void *pfifo_thread(void *arg)
 
     qemu_mutex_lock(&d->pfifo.lock);
     while (true) {
-        d->pfifo.fifo_kick = false;
+        qatomic_set(&d->pfifo.fifo_kick, false);
 
         pgraph_process_pending(d);
 
@@ -474,7 +476,7 @@ void *pfifo_thread(void *arg)
 
         pgraph_process_pending_reports(d);
 
-        if (!d->pfifo.fifo_kick) {
+        if (!qatomic_read(&d->pfifo.fifo_kick)) {
             qemu_cond_broadcast(&d->pfifo.fifo_idle_cond);
 
             // Both the pusher and puller are waiting for some action
diff --git a/hw/xbox/nv2a/pgraph/gl/renderer.c b/hw/xbox/nv2a/pgraph/gl/renderer.c
index fbfe23b..54da4ed 100644
--- a/hw/xbox/nv2a/pgraph/gl/renderer.c
+++ b/hw/xbox/nv2a/pgraph/gl/renderer.c
@@ -127,6 +127,7 @@ static void pgraph_gl_process_pending(NV2AState *d)
         qatomic_read(&d->pgraph.flush_pending) ||
         qatomic_read(&r->shader_cache_writeback_pending)) {
         qemu_mutex_unlock(&d->pfifo.lock);
+        pgraph_wait_for_cpu_mmio_accesses(&d->pgraph);
         PGRAPH_LOCK(&d->pgraph);
         if (qatomic_read(&r->downloads_pending)) {
             pgraph_gl_process_pending_downloads(d);
diff --git a/hw/xbox/nv2a/pgraph/null/renderer.c b/hw/xbox/nv2a/pgraph/null/renderer.c
index 6454927..feb0324 100644
--- a/hw/xbox/nv2a/pgraph/null/renderer.c
+++ b/hw/xbox/nv2a/pgraph/null/renderer.c
@@ -41,6 +41,7 @@ static void pgraph_null_process_pending(NV2AState *d)
         qatomic_read(&d->pgraph.flush_pending)
         ) {
         qemu_mutex_unlock(&d->pfifo.lock);
+        pgraph_wait_for_cpu_mmio_accesses(&d->pgraph);
         PGRAPH_LOCK(&d->pgraph);
         if (qatomic_read(&d->pgraph.sync_pending)) {
             pgraph_null_sync(d);
diff --git a/hw/xbox/nv2a/pgraph/pgraph.c b/hw/xbox/nv2a/pgraph/pgraph.c
index 84650c7..8314b8a 100644
--- a/hw/xbox/nv2a/pgraph/pgraph.c
+++ b/hw/xbox/nv2a/pgraph/pgraph.c
@@ -43,21 +43,84 @@
 
 NV2AState *g_nv2a;
 
+static inline uint32_t pgraph_surface_read(PGRAPHState *pg)
+{
+    return qatomic_read(&pg->regs_[NV_PGRAPH_SURFACE]);
+}
+
+static inline void pgraph_surface_write_locked(PGRAPHState *pg, uint32_t value)
+{
+    uint32_t old = qatomic_read(&pg->regs_[NV_PGRAPH_SURFACE]);
+    if (old != value) {
+        set_bit_atomic(NV_PGRAPH_SURFACE / sizeof(uint32_t), pg->regs_dirty);
+    }
+    qatomic_set(&pg->regs_[NV_PGRAPH_SURFACE], value);
+}
+
+static inline void pgraph_surface_set_mask_locked(PGRAPHState *pg,
+                                                   uint32_t mask,
+                                                   uint32_t value)
+{
+    uint32_t surface = pgraph_surface_read(pg);
+    SET_MASK(surface, mask, value);
+    pgraph_surface_write_locked(pg, surface);
+}
+
+/*
+ * Set fifo_kick before trylock: an active PFIFO thread will not sleep, while a
+ * sleeping PFIFO has released the mutex and receives the condition wakeup.
+ */
+static inline void pgraph_pfifo_kick_nonblocking(NV2AState *d)
+{
+    qatomic_set(&d->pfifo.fifo_kick, true);
+
+    for (unsigned int i = 0; i < 64; i++) {
+        if (qemu_mutex_trylock(&d->pfifo.lock) == 0) {
+            pfifo_kick(d);
+            qemu_mutex_unlock(&d->pfifo.lock);
+            return;
+        }
+        cpu_relax();
+    }
+}
+
 uint64_t pgraph_read(void *opaque, hwaddr addr, unsigned int size)
 {
     NV2AState *d = (NV2AState *)opaque;
     PGRAPHState *pg = &d->pgraph;
 
+    switch (addr) {
+    case NV_PGRAPH_INTR: {
+        uint64_t r = qatomic_read(&pg->pending_interrupts);
+        nv2a_reg_log_read(NV_PGRAPH, addr, size, r);
+        return r;
+    }
+    case NV_PGRAPH_INTR_EN: {
+        uint64_t r = qatomic_read(&pg->enabled_interrupts);
+        nv2a_reg_log_read(NV_PGRAPH, addr, size, r);
+        return r;
+    }
+    case NV_PGRAPH_RDI_DATA:
+        break;
+    default: {
+        assert(addr % 4 == 0);
+        uint64_t r = qatomic_read(&pg->regs_[addr]);
+        nv2a_reg_log_read(NV_PGRAPH, addr, size, r);
+        return r;
+    }
+    }
+
+    /* Do not wait for pgraph.lock while holding BQL. */
+    bool had_bql = bql_locked();
+    if (had_bql) {
+        pgraph_cpu_mmio_access_begin(pg);
+        bql_unlock();
+    }
+
     PGRAPH_LOCK(pg);
 
     uint64_t r = 0;
     switch (addr) {
-    case NV_PGRAPH_INTR:
-        r = pg->pending_interrupts;
-        break;
-    case NV_PGRAPH_INTR_EN:
-        r = pg->enabled_interrupts;
-        break;
     case NV_PGRAPH_RDI_DATA: {
         unsigned int select = PG_GET_MASK(NV_PGRAPH_RDI_INDEX,
                                        NV_PGRAPH_RDI_INDEX_SELECT);
@@ -80,6 +143,12 @@ uint64_t pgraph_read(void *opaque, hwaddr addr, unsigned int size)
 
     PGRAPH_UNLOCK(pg);
 
+    /* Drop pgraph.lock before reacquiring BQL. */
+    if (had_bql) {
+        pgraph_cpu_mmio_access_end(pg);
+        bql_lock();
+    }
+
     nv2a_reg_log_read(NV_PGRAPH, addr, size, r);
     return r;
 }
@@ -89,95 +158,179 @@ void pgraph_write(void *opaque, hwaddr addr, uint64_t val, unsigned int size)
     NV2AState *d = (NV2AState *)opaque;
     PGRAPHState *pg = &d->pgraph;
 
-    /* Diagnostics fallback for the stock PGRAPH path. Patch 20 replaces this
-     * with its lock-decoupled increment path, but the same trace ABI remains
-     * available when Patch 20 is intentionally omitted for isolation tests. */
-    const uint64_t trace_threshold =
-        (addr == NV_PGRAPH_INCREMENT)
-            ? xemu_debug_tools_pgraph_increment_trace_threshold_ns() : 0;
-    XemuDebugToolsPgraphIncrementEvent trace = { 0 };
-    uint64_t trace_stage_start = 0;
-    if (trace_threshold != 0) {
-        trace.threshold_ns = trace_threshold;
-        trace.host_start_ns = qemu_clock_get_ns(QEMU_CLOCK_HOST);
-        trace.virtual_start_ns = qemu_clock_get_ns(QEMU_CLOCK_VIRTUAL);
-        trace.value = (uint32_t)val;
-        trace.had_bql = bql_locked() ? 1 : 0;
-    }
-
     nv2a_reg_log_write(NV_PGRAPH, addr, size, val);
 
-    qemu_mutex_lock(&d->pfifo.lock); // FIXME: Factor out fifo lock here
-    if (trace_threshold != 0) {
-        trace_stage_start = qemu_clock_get_ns(QEMU_CLOCK_HOST);
-    }
-    PGRAPH_LOCK(pg);
-    if (trace_threshold != 0) {
-        /* Stock has one renderer lock rather than Patch 20's surface_lock.
-         * Preserve the stage field by measuring the equivalent serialization
-         * wait here; Patch 20 restores the dedicated surface-lock timing. */
-        trace.surface_lock_wait_ns =
-            qemu_clock_get_ns(QEMU_CLOCK_HOST) - trace_stage_start;
+    if (addr == NV_PGRAPH_FIFO) {
+        bool had_bql = bql_locked();
+        if (had_bql) {
+            pgraph_cpu_mmio_access_begin(pg);
+            bql_unlock();
+        }
+
+        qemu_mutex_lock(&d->pfifo.lock);
+
+        uint32_t old = qatomic_read(&pg->regs_[NV_PGRAPH_FIFO]);
+        uint32_t value = (uint32_t)val;
+        if (old != value) {
+            set_bit_atomic(NV_PGRAPH_FIFO / sizeof(uint32_t),
+                           pg->regs_dirty);
+        }
+        qatomic_set(&pg->regs_[NV_PGRAPH_FIFO], value);
+        pfifo_kick(d);
+
+        qemu_mutex_unlock(&d->pfifo.lock);
+
+        if (had_bql) {
+            pgraph_cpu_mmio_access_end(pg);
+            bql_lock();
+        }
+        return;
     }
 
-    switch (addr) {
-    case NV_PGRAPH_INTR:
-        pg->pending_interrupts &= ~val;
+    /*
+     * Interrupt acknowledgement must not wait for renderer-owned pgraph.lock.
+     */
+    if (addr == NV_PGRAPH_INTR) {
+        bool had_bql = bql_locked();
+        if (had_bql) {
+            bql_unlock();
+        }
+
+        qemu_mutex_lock(&pg->interrupt_lock);
 
-        if (!(pg->pending_interrupts & NV_PGRAPH_INTR_ERROR)) {
-            pg->waiting_for_nop = false;
+        uint32_t clear_mask = ~(uint32_t)val;
+        uint32_t pending =
+            qatomic_fetch_and(&pg->pending_interrupts, clear_mask) & clear_mask;
+
+        if (!(pending & NV_PGRAPH_INTR_ERROR)) {
+            qatomic_set(&pg->waiting_for_nop, false);
         }
-        if (!(pg->pending_interrupts & NV_PGRAPH_INTR_CONTEXT_SWITCH)) {
-            pg->waiting_for_context_switch = false;
+        if (!(pending & NV_PGRAPH_INTR_CONTEXT_SWITCH)) {
+            qatomic_set(&pg->waiting_for_context_switch, false);
         }
+
+        qemu_mutex_unlock(&pg->interrupt_lock);
+
+        qemu_mutex_lock(&d->pfifo.lock);
         pfifo_kick(d);
-        break;
-    case NV_PGRAPH_INTR_EN:
-        pg->enabled_interrupts = val;
-        break;
-    case NV_PGRAPH_INCREMENT:
+        qemu_mutex_unlock(&d->pfifo.lock);
+
+        if (had_bql) {
+            bql_lock();
+        }
+        return;
+    }
+
+    /* Flip-ring consumption must not wait for renderer-owned pgraph.lock. */
+    if (addr == NV_PGRAPH_INCREMENT) {
+        const uint64_t trace_threshold =
+            xemu_debug_tools_pgraph_increment_trace_threshold_ns();
+        XemuDebugToolsPgraphIncrementEvent trace = { 0 };
+        uint64_t stage_start = 0;
+        bool had_bql = bql_locked();
+
+        if (trace_threshold != 0) {
+            trace.threshold_ns = trace_threshold;
+            trace.host_start_ns = qemu_clock_get_ns(QEMU_CLOCK_HOST);
+            trace.virtual_start_ns = qemu_clock_get_ns(QEMU_CLOCK_VIRTUAL);
+            trace.value = (uint32_t)val;
+            trace.had_bql = had_bql ? 1 : 0;
+        }
+        if (had_bql) {
+            if (trace_threshold != 0) {
+                stage_start = qemu_clock_get_ns(QEMU_CLOCK_HOST);
+            }
+            bql_unlock();
+            if (trace_threshold != 0) {
+                trace.bql_release_ns =
+                    qemu_clock_get_ns(QEMU_CLOCK_HOST) - stage_start;
+            }
+        }
+
         if (val & NV_PGRAPH_INCREMENT_READ_3D) {
-            const uint32_t surface_before = pgraph_reg_r(pg, NV_PGRAPH_SURFACE);
-            const uint32_t read_before =
-                GET_MASK(surface_before, NV_PGRAPH_SURFACE_READ_3D);
-            const uint32_t modulo =
-                GET_MASK(surface_before, NV_PGRAPH_SURFACE_MODULO_3D);
             if (trace_threshold != 0) {
-                trace.surface_before = surface_before;
-                trace.read_before = read_before;
-                trace.modulo = modulo;
-                trace_stage_start = qemu_clock_get_ns(QEMU_CLOCK_HOST);
+                stage_start = qemu_clock_get_ns(QEMU_CLOCK_HOST);
+            }
+            qemu_mutex_lock(&pg->surface_lock);
+            if (trace_threshold != 0) {
+                trace.surface_lock_wait_ns =
+                    qemu_clock_get_ns(QEMU_CLOCK_HOST) - stage_start;
+                stage_start = qemu_clock_get_ns(QEMU_CLOCK_HOST);
             }
 
-            PG_SET_MASK(NV_PGRAPH_SURFACE,
-                     NV_PGRAPH_SURFACE_READ_3D,
-                     (read_before + 1) % modulo);
+            uint32_t surface = pgraph_surface_read(pg);
+            uint32_t read = GET_MASK(surface, NV_PGRAPH_SURFACE_READ_3D);
+            uint32_t modulo = GET_MASK(surface, NV_PGRAPH_SURFACE_MODULO_3D);
+            if (trace_threshold != 0) {
+                trace.surface_before = surface;
+                trace.read_before = read;
+                trace.modulo = modulo;
+            }
 
-            const uint32_t surface_after = pgraph_reg_r(pg, NV_PGRAPH_SURFACE);
-            const uint32_t read_after =
-                GET_MASK(surface_after, NV_PGRAPH_SURFACE_READ_3D);
+            SET_MASK(surface, NV_PGRAPH_SURFACE_READ_3D,
+                     (read + 1) % modulo);
+            pgraph_surface_write_locked(pg, surface);
             if (trace_threshold != 0) {
-                trace.surface_after = surface_after;
-                trace.read_after = read_after;
+                trace.surface_after = surface;
+                trace.read_after =
+                    GET_MASK(surface, NV_PGRAPH_SURFACE_READ_3D);
                 trace.surface_update_ns =
-                    qemu_clock_get_ns(QEMU_CLOCK_HOST) - trace_stage_start;
+                    qemu_clock_get_ns(QEMU_CLOCK_HOST) - stage_start;
             }
 
+            qemu_mutex_unlock(&pg->surface_lock);
+
             xemu_debug_tools_flip_stall_trace_record(
                 XEMU_DEBUG_TOOLS_FLIP_VBLANK_READ_INCREMENT, pg,
-                NV097_SET_FLIP_READ, (uint32_t)val, read_before,
-                read_after, 0);
+                NV097_SET_FLIP_READ, (uint32_t)val, read,
+                GET_MASK(surface, NV_PGRAPH_SURFACE_READ_3D), 0);
 
             nv2a_profile_increment();
             if (trace_threshold != 0) {
-                trace_stage_start = qemu_clock_get_ns(QEMU_CLOCK_HOST);
+                stage_start = qemu_clock_get_ns(QEMU_CLOCK_HOST);
             }
-            pfifo_kick(d);
+            pgraph_pfifo_kick_nonblocking(d);
             if (trace_threshold != 0) {
                 trace.pfifo_kick_ns =
-                    qemu_clock_get_ns(QEMU_CLOCK_HOST) - trace_stage_start;
+                    qemu_clock_get_ns(QEMU_CLOCK_HOST) - stage_start;
             }
         }
+
+        if (had_bql) {
+            if (trace_threshold != 0) {
+                stage_start = qemu_clock_get_ns(QEMU_CLOCK_HOST);
+            }
+            bql_lock();
+            if (trace_threshold != 0) {
+                trace.bql_reacquire_wait_ns =
+                    qemu_clock_get_ns(QEMU_CLOCK_HOST) - stage_start;
+            }
+        }
+        if (trace_threshold != 0) {
+            trace.host_end_ns = qemu_clock_get_ns(QEMU_CLOCK_HOST);
+            trace.virtual_end_ns = qemu_clock_get_ns(QEMU_CLOCK_VIRTUAL);
+            trace.host_duration_ns = trace.host_end_ns - trace.host_start_ns;
+            trace.virtual_duration_ns =
+                (int64_t)trace.virtual_end_ns -
+                (int64_t)trace.virtual_start_ns;
+            xemu_debug_tools_pgraph_increment_trace_record(&trace);
+        }
+        return;
+    }
+
+    /* Do not wait for PFIFO or PGRAPH while holding BQL. */
+    bool had_bql = bql_locked();
+    if (had_bql) {
+        pgraph_cpu_mmio_access_begin(pg);
+        bql_unlock();
+    }
+
+    qemu_mutex_lock(&d->pfifo.lock); // FIXME: Factor out fifo lock here
+    PGRAPH_LOCK(pg);
+
+    switch (addr) {
+    case NV_PGRAPH_INTR_EN:
+        qatomic_set(&pg->enabled_interrupts, (uint32_t)val);
         break;
     case NV_PGRAPH_RDI_DATA: {
         unsigned int select = PG_GET_MASK(NV_PGRAPH_RDI_INDEX,
@@ -223,6 +376,11 @@ void pgraph_write(void *opaque, hwaddr addr, uint64_t val, unsigned int size)
 
         break;
     }
+    case NV_PGRAPH_SURFACE:
+        qemu_mutex_lock(&pg->surface_lock);
+        pgraph_surface_write_locked(pg, (uint32_t)val);
+        qemu_mutex_unlock(&pg->surface_lock);
+        break;
     default:
         pgraph_reg_w(pg, addr, val);
         break;
@@ -238,17 +396,9 @@ void pgraph_write(void *opaque, hwaddr addr, uint64_t val, unsigned int size)
     PGRAPH_UNLOCK(pg);
     qemu_mutex_unlock(&d->pfifo.lock);
 
-    if (trace_threshold != 0) {
-        /* Stock never drops BQL in this path, so there is no reacquire wait.
-         * Keep the field explicit so the profiler output remains comparable. */
-        trace.bql_reacquire_wait_ns = 0;
-        trace.bql_held_after = bql_locked() ? 1 : 0;
-        trace.host_end_ns = qemu_clock_get_ns(QEMU_CLOCK_HOST);
-        trace.virtual_end_ns = qemu_clock_get_ns(QEMU_CLOCK_VIRTUAL);
-        trace.host_duration_ns = trace.host_end_ns - trace.host_start_ns;
-        trace.virtual_duration_ns =
-            (int64_t)trace.virtual_end_ns - (int64_t)trace.virtual_start_ns;
-        xemu_debug_tools_pgraph_increment_trace_record(&trace);
+    if (had_bql) {
+        pgraph_cpu_mmio_access_end(pg);
+        bql_lock();
     }
 }
 
@@ -272,10 +422,13 @@ void pgraph_context_switch(NV2AState *d, unsigned int channel_id)
         assert(!PG_GET_MASK(NV_PGRAPH_DEBUG_3,
                             NV_PGRAPH_DEBUG_3_HW_CONTEXT_SWITCH));
 
-        pg->waiting_for_context_switch = true;
+        qemu_mutex_lock(&pg->interrupt_lock);
+        qatomic_set(&pg->waiting_for_context_switch, true);
+        qatomic_or(&pg->pending_interrupts, NV_PGRAPH_INTR_CONTEXT_SWITCH);
+        qemu_mutex_unlock(&pg->interrupt_lock);
+
         PGRAPH_UNLOCK(pg);
         bql_lock();
-        pg->pending_interrupts |= NV_PGRAPH_INTR_CONTEXT_SWITCH;
         nv2a_update_irq(d);
         bql_unlock();
         PGRAPH_LOCK(pg);
@@ -297,6 +450,11 @@ void pgraph_init(NV2AState *d)
     PGRAPHState *pg = &d->pgraph;
     qemu_mutex_init(&pg->lock);
     qemu_mutex_init(&pg->renderer_lock);
+    qemu_mutex_init(&pg->interrupt_lock);
+    qemu_mutex_init(&pg->surface_lock);
+    qemu_mutex_init(&pg->cpu_mmio_access_lock);
+    qemu_cond_init(&pg->cpu_mmio_access_cond);
+    pg->cpu_mmio_accesses_waiting = 0;
     qemu_event_init(&pg->sync_complete, false);
     qemu_event_init(&pg->flush_complete, false);
     qemu_cond_init(&pg->framebuffer_released);
@@ -426,6 +584,10 @@ void pgraph_destroy(PGRAPHState *pg)
        pg->renderer->ops.finalize(d);
     }
 
+    qemu_cond_destroy(&pg->cpu_mmio_access_cond);
+    qemu_mutex_destroy(&pg->cpu_mmio_access_lock);
+    qemu_mutex_destroy(&pg->surface_lock);
+    qemu_mutex_destroy(&pg->interrupt_lock);
     qemu_mutex_destroy(&pg->lock);
 }
 
@@ -914,7 +1076,7 @@ DEF_METHOD(NV097, NO_OPERATION)
     unsigned channel_id =
         PG_GET_MASK(NV_PGRAPH_CTX_USER, NV_PGRAPH_CTX_USER_CHID);
 
-    assert(!(pg->pending_interrupts & NV_PGRAPH_INTR_ERROR));
+    assert(!(qatomic_read(&pg->pending_interrupts) & NV_PGRAPH_INTR_ERROR));
 
     PG_SET_MASK(NV_PGRAPH_TRAPPED_ADDR, NV_PGRAPH_TRAPPED_ADDR_CHID,
              channel_id);
@@ -925,8 +1087,10 @@ DEF_METHOD(NV097, NO_OPERATION)
     pgraph_reg_w(pg, NV_PGRAPH_TRAPPED_DATA_LOW, parameter);
     pgraph_reg_w(pg, NV_PGRAPH_NSOURCE,
                  NV_PGRAPH_NSOURCE_NOTIFICATION); /* TODO: check this */
-    pg->pending_interrupts |= NV_PGRAPH_INTR_ERROR;
-    pg->waiting_for_nop = true;
+    qemu_mutex_lock(&pg->interrupt_lock);
+    qatomic_set(&pg->waiting_for_nop, true);
+    qatomic_or(&pg->pending_interrupts, NV_PGRAPH_INTR_ERROR);
+    qemu_mutex_unlock(&pg->interrupt_lock);
 
     PGRAPH_UNLOCK(pg);
     bql_lock();
@@ -942,42 +1106,48 @@ DEF_METHOD(NV097, WAIT_FOR_IDLE)
 
 DEF_METHOD(NV097, SET_FLIP_READ)
 {
+    qemu_mutex_lock(&pg->surface_lock);
+    pgraph_surface_set_mask_locked(pg, NV_PGRAPH_SURFACE_READ_3D, parameter);
+    qemu_mutex_unlock(&pg->surface_lock);
     xemu_debug_tools_flip_stall_trace_record(
-        XEMU_DEBUG_TOOLS_FLIP_SET_READ, pg, NV097_SET_FLIP_READ, parameter, 0, 0, 0);
-    PG_SET_MASK(NV_PGRAPH_SURFACE, NV_PGRAPH_SURFACE_READ_3D,
-             parameter);
+        XEMU_DEBUG_TOOLS_FLIP_SET_READ, pg, NV097_SET_FLIP_READ,
+        parameter, 0, 0, 0);
 }
 
 DEF_METHOD(NV097, SET_FLIP_WRITE)
 {
+    qemu_mutex_lock(&pg->surface_lock);
+    pgraph_surface_set_mask_locked(pg, NV_PGRAPH_SURFACE_WRITE_3D, parameter);
+    qemu_mutex_unlock(&pg->surface_lock);
     xemu_debug_tools_flip_stall_trace_record(
-        XEMU_DEBUG_TOOLS_FLIP_SET_WRITE, pg, NV097_SET_FLIP_WRITE, parameter, 0, 0, 0);
-    PG_SET_MASK(NV_PGRAPH_SURFACE, NV_PGRAPH_SURFACE_WRITE_3D,
-             parameter);
+        XEMU_DEBUG_TOOLS_FLIP_SET_WRITE, pg, NV097_SET_FLIP_WRITE,
+        parameter, 0, 0, 0);
 }
 
 DEF_METHOD(NV097, SET_FLIP_MODULO)
 {
+    qemu_mutex_lock(&pg->surface_lock);
+    pgraph_surface_set_mask_locked(pg, NV_PGRAPH_SURFACE_MODULO_3D, parameter);
+    qemu_mutex_unlock(&pg->surface_lock);
     xemu_debug_tools_flip_stall_trace_record(
-        XEMU_DEBUG_TOOLS_FLIP_SET_MODULO, pg, NV097_SET_FLIP_MODULO, parameter, 0, 0, 0);
-    PG_SET_MASK(NV_PGRAPH_SURFACE, NV_PGRAPH_SURFACE_MODULO_3D,
-             parameter);
+        XEMU_DEBUG_TOOLS_FLIP_SET_MODULO, pg, NV097_SET_FLIP_MODULO,
+        parameter, 0, 0, 0);
 }
 
 DEF_METHOD(NV097, FLIP_INCREMENT_WRITE)
 {
-    uint32_t old =
-        PG_GET_MASK(NV_PGRAPH_SURFACE, NV_PGRAPH_SURFACE_WRITE_3D);
+    qemu_mutex_lock(&pg->surface_lock);
+
+    uint32_t surface = pgraph_surface_read(pg);
+    uint32_t old = GET_MASK(surface, NV_PGRAPH_SURFACE_WRITE_3D);
+    uint32_t modulo = GET_MASK(surface, NV_PGRAPH_SURFACE_MODULO_3D);
+
+    SET_MASK(surface, NV_PGRAPH_SURFACE_WRITE_3D, (old + 1) % modulo);
+    pgraph_surface_write_locked(pg, surface);
 
-    PG_SET_MASK(NV_PGRAPH_SURFACE,
-             NV_PGRAPH_SURFACE_WRITE_3D,
-             (PG_GET_MASK(NV_PGRAPH_SURFACE,
-                      NV_PGRAPH_SURFACE_WRITE_3D)+1)
-                % PG_GET_MASK(NV_PGRAPH_SURFACE,
-                           NV_PGRAPH_SURFACE_MODULO_3D) );
+    uint32_t new = GET_MASK(surface, NV_PGRAPH_SURFACE_WRITE_3D);
 
-    uint32_t new =
-        PG_GET_MASK(NV_PGRAPH_SURFACE, NV_PGRAPH_SURFACE_WRITE_3D);
+    qemu_mutex_unlock(&pg->surface_lock);
 
     xemu_debug_tools_flip_stall_trace_record(
         XEMU_DEBUG_TOOLS_FLIP_INCREMENT_WRITE, pg,
@@ -2951,7 +3121,7 @@ DEF_METHOD(NV097, BACK_END_WRITE_SEMAPHORE_RELEASE)
 {
     d->pgraph.renderer->ops.surface_update(d, false, true, true);
 
-    //PGRAPH_UNLOCK(&d->pgraph);
+    //qemu_mutex_unlock(&d->pgraph.lock);
     //bql_lock();
 
     uint32_t semaphore_offset = pgraph_reg_r(pg, NV_PGRAPH_SEMAPHOREOFFSET);
@@ -2964,7 +3134,7 @@ DEF_METHOD(NV097, BACK_END_WRITE_SEMAPHORE_RELEASE)
 
     stl_le_p((uint32_t*)semaphore_data, parameter);
 
-    //PGRAPH_LOCK(&d->pgraph);
+    //qemu_mutex_lock(&d->pgraph.lock);
     //bql_unlock();
 }
 
diff --git a/hw/xbox/nv2a/pgraph/pgraph.h b/hw/xbox/nv2a/pgraph/pgraph.h
index 0660a3d..025684c 100644
--- a/hw/xbox/nv2a/pgraph/pgraph.h
+++ b/hw/xbox/nv2a/pgraph/pgraph.h
@@ -140,6 +140,14 @@ typedef struct PGRAPHState {
     QemuMutex lock;
     QemuMutex renderer_lock;
 
+    QemuMutex interrupt_lock;
+    QemuMutex surface_lock;
+
+    /* Serializes the CPU-MMIO/PFIFO handoff without nesting pgraph.lock. */
+    QemuMutex cpu_mmio_access_lock;
+    QemuCond cpu_mmio_access_cond;
+    unsigned int cpu_mmio_accesses_waiting;
+
     uint32_t pending_interrupts;
     uint32_t enabled_interrupts;
 
@@ -292,6 +300,33 @@ static inline void pgraph_unlock_observed(PGRAPHState *pg, const char *file,
 #define PGRAPH_LOCK(pg) pgraph_lock_observed((pg), __FILE__, __LINE__)
 #define PGRAPH_UNLOCK(pg) pgraph_unlock_observed((pg), __FILE__, __LINE__)
 
+static inline void pgraph_cpu_mmio_access_begin(PGRAPHState *pg)
+{
+    qemu_mutex_lock(&pg->cpu_mmio_access_lock);
+    pg->cpu_mmio_accesses_waiting++;
+    qemu_mutex_unlock(&pg->cpu_mmio_access_lock);
+}
+
+static inline void pgraph_cpu_mmio_access_end(PGRAPHState *pg)
+{
+    qemu_mutex_lock(&pg->cpu_mmio_access_lock);
+    assert(pg->cpu_mmio_accesses_waiting > 0);
+    pg->cpu_mmio_accesses_waiting--;
+    if (pg->cpu_mmio_accesses_waiting == 0) {
+        qemu_cond_broadcast(&pg->cpu_mmio_access_cond);
+    }
+    qemu_mutex_unlock(&pg->cpu_mmio_access_lock);
+}
+
+static inline void pgraph_wait_for_cpu_mmio_accesses(PGRAPHState *pg)
+{
+    qemu_mutex_lock(&pg->cpu_mmio_access_lock);
+    while (pg->cpu_mmio_accesses_waiting != 0) {
+        qemu_cond_wait(&pg->cpu_mmio_access_cond, &pg->cpu_mmio_access_lock);
+    }
+    qemu_mutex_unlock(&pg->cpu_mmio_access_lock);
+}
+
 void pgraph_init(NV2AState *d);
 void pgraph_init_thread(NV2AState *d);
 void pgraph_destroy(PGRAPHState *pg);
diff --git a/hw/xbox/nv2a/pgraph/vk/renderer.c b/hw/xbox/nv2a/pgraph/vk/renderer.c
index 89c9779..c43276a 100644
--- a/hw/xbox/nv2a/pgraph/vk/renderer.c
+++ b/hw/xbox/nv2a/pgraph/vk/renderer.c
@@ -123,6 +123,7 @@ static void pgraph_vk_process_pending(NV2AState *d)
         qatomic_read(&d->pgraph.flush_pending)
     ) {
         qemu_mutex_unlock(&d->pfifo.lock);
+        pgraph_wait_for_cpu_mmio_accesses(&d->pgraph);
         PGRAPH_LOCK(&d->pgraph);
         if (qatomic_read(&r->downloads_pending)) {
             pgraph_vk_process_pending_downloads(d);
